<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>从栈到信号：操作系统如何操作、切换和修改执行流</title>
      <link href="/posts/stack_and_signal.html"/>
      <url>/posts/stack_and_signal.html</url>
      
        <content type="html"><![CDATA[<h1 id="零-引言"><a href="#零-引言" class="headerlink" title="零.引言"></a>零.引言</h1><p>在操作系统中有很多的概念：线程、进程、调度、中断、信号、系统调用……教材分别给它们下出定义：线程是CPU调度的单位，进程是资源分配的基本单位…..但当这些机制同时出现时，一个问题很容易产生：我只知道它们本身是什么，但是它们之间到底是什么关系？为什么要有这些概念？</p><p>再具体到实现层面，可能出现下面的问题：为什么线程切换需要保存寄存器和栈？信号处理函数是怎么“插入”到用户执行程序中的？为什么系统调用需要切换到内核栈？为什么调度的核心操作是保存和恢复上下文？</p><p>实际上，这些问题都在围绕着同一个事情：暂停一个正在执行的程序，然后在某个时刻恢复它。什么意思？无论是线程切换调度还是中断处理，操作系统都必须完成一个基本的操作：在某个时刻“冻结”当前程序的执行状态，并在未来恢复它。</p><p>这就引入了另一个更基础的问题：**“程序的执行状态”到底是由什么状态构成的？**换句话说，如果我们想暂停一个程序，然后在未来继续执行，究竟要保存哪些信息？</p><p>从计算机的体系结构来看，这些状态主要包括三部分：</p><ul><li><p>程序计数器（PC）：当前&#x2F;下一步执行哪条指令</p></li><li><p>寄存器：当前计算的中间结果</p></li><li><p>调用栈：记录函数的调用链</p></li></ul><p>只要保存并恢复这三部分，程序就可以在任意时刻被暂停，并继续执行。把程序的运行看作是这三种状态在时间上的连续变化，那么这条随时间推进的状态轨迹，就是一条<strong>执行流</strong>。<strong>总结一下，执行流就是“程序运行时，CPU做了什么、在做什么以及接下来要做什么”的动态轨迹</strong></p><p><strong>而操作系统中的很多机制，本质上都是在创建、暂停、恢复、切换这样的执行流。</strong></p><p><strong>此时，可能会有疑问：为什么一个程序的执行状态恰好可以由PC、寄存器和调用栈来描述呢？</strong> 其中PC和寄存器来自于处理器自身，前者负责读指令，后者负责存一些计算的中间结果，比较好理解；但是调用栈为什么会存在呢？</p><p>  换句话说，<strong>为什么程序运行时需要专门一段内存来记录函数的调用历史？ 此外，为什么偏偏是栈，而不是队列、数组等等又或是其他的数据结构呢？</strong></p><br><h1 id="一-执行流、函数调用与栈"><a href="#一-执行流、函数调用与栈" class="headerlink" title="一.执行流、函数调用与栈"></a>一.执行流、函数调用与栈</h1><p>要回答这两个问题，我们需要暂时离开操作系统，回到计算机最基本的执行模型——冯诺依曼机器。因为函数调用本质上是一种执行流的变化，而执行流的行为最终是由处理器的执行方式决定的</p><p>在经典的冯诺依曼体系中，程序被存储为一段连续的指令序列，CPU不断重复以下过程：取指、译码、执行、访存、写回。处理器通过PC指向当前要执行的命令，每执行一条指令，PC就移动到下一条指令：</p><pre><code>PC-&gt;PC+1-&gt;PC+2....</code></pre><p>如果程序始终这样执行下去，那么就非常简单了。然而，现实的程序中很少只是简单地顺序执行，为了实现代码复用和模块化，程序往往包含大量的函数调用。</p><p>函数调用听起来简单，只要把函数的第一条指令的地址记住，加载到PC就可以跳转过去了。但是，怎么回来呢？所以，在跳转前，最好先把下一条执行的地址保存起来，等函数执行完成后，再根据该地址回到之前的保存的位置继续执行，不就可以了吗？</p><p>整体的流程如下：</p><pre><code>保存返回位置    ↓跳转到函数    ↓函数执行    ↓跳回返回位置</code></pre><p>那把这个返回地址保存在哪比较好呢？可能会想到放在CPU寄存器中，有些体系结构确实会使用一个专门的寄存器保存返回地址，比如ARM。但一旦出现函数嵌套调用的情况，A-&gt;B-&gt;C-&gt;D-&gt;…-&gt;Z，这么多函数，每个函数都要有返回地址，哪有那么多寄存器呀？如果将所有返回地址保存在同一个寄存器中，则会出现覆盖的问题——A函数先保存自己的返回地址，B也保存，C也保存…因此，程序需要一种结构来保存这些返回地址。此外，还有个要求：<strong>后调用函数不会破坏之前函数存储的返回地址，保证一个函数存的返回地址和拿出来的必须是一样的。</strong></p><p>而这就要求，**后存的数据必须先拿，先存的数据必须后拿，**而这就是LIFO。因此，用于保存函数调用信息最自然的结构就是栈。</p><p>不过，这里还可以再追问一个问题，为什么函数的调用结构恰好符合这种“后进先出”的规律，看起来和栈天生一对？</p><p>  换句话说，为什么函数调用天然符合嵌套结构：</p><pre><code>[A   [B      [C]   ]]</code></pre><p>而不是下图中的这种结构？</p><pre><code>[A   [B   ]      [C]]</code></pre><p>要理解这一点，就需要回到计算机最基本的执行模型——冯诺依曼机器。在冯诺依曼体系中，CPU在任意时刻只有一个程序计数器，因此机器一次只有一条执行流，程序执行的流程是：取指、执行、更新PC、再取指….也就是说，程序本质上一条单线推进的执行流程。所以，当函数A调用B，执行流就必须从A跳转到B，当B调用C，执行流就又要跳转到C。并且，再C返回前，A和B都不会继续执行，处于“暂停”状态。于是，这就形成了一个典型的结构：</p><pre><code>A └─ B     └─ C         └─ D</code></pre><p>从拓扑结构来看，这实际上是一颗函数调用树，而程序的实际执行顺序，本质上就是调用这颗树的深度优先遍历（DFS），而DFS的经典实现方式，正是栈。</p><p><strong>所以，从更深层的角度来看，栈并不是某种偶然的设计，不是一拍脑门觉得正好符合函数调用结构所以选的栈，而是在冯诺依曼的这种顺序执行机器在面对嵌套控制调用流程的必然选择</strong></p><p>而一旦从这个角度来看，栈的意义就不仅仅是“保存返回地址了”，<strong>栈实际上记录了程序执行流的结构</strong></p><p>从执行流的角度来看：执行流本身是一个抽象的概念，而栈则是执行流在内存中的一种具体编码，是记录执行流历史的物理容器</p><p>一层函数调用——一层函数栈帧</p><p>返回地址作为执行流未来的回退地址，对应存储在栈帧的顶部</p><p>局部变量作为当前执行阶段的状态，参数作为传递给下一层的执行信息，都对应存储在栈帧中</p><p><strong>而PC描述了当前执行流的位置，寄存器则记录了当前的计算状态，其中，RSP栈指针则记录了执行流当前的调用深度</strong></p><p>而这再次回答了上一节提到的问题：为什么程序的执行状态可以由PC、寄存器和调用栈来描述。因为在冯诺依曼机器中，<strong>程序的运行本质是：一条执行流沿着指令的执行不断向前推进（PC），并在推进的过程中不断产生新的状态（寄存器），并在函数调用时形成嵌套的控制结构（调用栈）。</strong> 而操作系统所谓的暂停程序、恢复程序、切换程序，实际上也正是对这三部分状态进行保存切换和恢复。换句话说，实际上操作系统真正的管理对象，并不是”程序“本身，而是程序所对应的执行流状态</p><br><h1 id="二-多条执行流：线程与调度"><a href="#二-多条执行流：线程与调度" class="headerlink" title="二.多条执行流：线程与调度"></a>二.多条执行流：线程与调度</h1><p>上一节我们看到，程序运行可以理解为一条执行流沿着执行的执行不断地前进，而其状态可以通过PC、寄存器和调用栈来表示。</p><p>从计算机的视角来看，一个”正在运行的程序“其实只是一组状态的集合而已。只要可以做到保存并恢复这三者，程序就可以在任意时刻被暂停，并在任意时刻继续执行。<strong>那如果系统中保存多组这样的状态，并在不同的时刻运行不同组的状态，是不是就相当于存在多条执行流了？</strong> 在CPU运行某一组状态时，其对应的执行流就处于”运行“状态，当CPU切换去运行另外一组状态时，原先的执行流就被暂停，而新的执行流恢复，开始继续向前推进。</p><p>在外部看来，好像是多个程序同时在运行一样，这种运行方式被称为<strong>并发</strong>，而这种被操作系统管理、可以被暂停与恢复的执行流，就是我们熟悉的<strong>线程</strong>；</p><p><strong>因此，从执行流的角度来看，线程就是一条被操作系统管理的执行流。</strong></p><p>既然系统中存在多条执行流，单核处理器下在任意时刻下只能选其中的一条来执行，那么操作系统就必须决定下一刻让哪条执行流继续运行，这个选择的过程，就称为<strong>调度</strong>。在选择完成后，操作系统需要真正进行执行流的切换，而这就是<strong>线程切换</strong></p><br><h1 id="三-执行流的保存与切换"><a href="#三-执行流的保存与切换" class="headerlink" title="三.执行流的保存与切换"></a>三.执行流的保存与切换</h1><p>上一小节说到，既然线程可以被理解为一条被操作系统管理的执行流，那么线程之间的切换，本质上就是执行流之间的切换。</p><p>此时就会出现一个问题：为了能够让执行流在未来时被恢复，究竟需要保存什么？还记得第一节中的结论吗？程序在任意时刻的运行状态都可以由三部分描述：</p><ul><li><p>程序计数器（PC）：当前执行流正在执行的位置</p></li><li><p>寄存器：当前计算过程中的中间状态</p></li><li><p>调用栈：记录函数调用链</p></li></ul><p>因此，当操作系统准备暂停一条执行流时，为了之后能够重新将这些状态恢复到处理器中，必须将这些状态保存起来。只有这样，执行流才能从直接被打断的地方继续执行，就好像没被打断过一样</p><p>实际上，这组用于描述一条执行流当前状态的信息，就被称为<strong>上下文</strong>。而在当前执行流在被切换时，可以分解为三步：保存当前执行流的上下文-&gt;选择要运行的下一条执行流-&gt;恢复该执行流的上下文，而这个过程就被称为<strong>上下文切换</strong>。</p><p>在具体实现层面上，可以概括为：保存寄存器、切换栈指针、更新计数器，之后CPU就可以继续执行新执行流了。但在更抽象的角度上来看，CPU完成了一次执行流的切换。</p><p>我们已经清楚了什么是切换，但是，什么时候需要切换呢？有可能是当前执行流因为阻塞主动让出CPU，也有可能是被迫让出CPU并切换到其他执行流，比如因为定时器中断。</p><p><strong>所以，从执行流的角度，操作系统中的许多机制——调度、阻塞、中断处理等，本质上都是围绕着“改变当前正在运行的执行流”这件事展开的</strong></p><p>进一步思考，如果当前执行流主动让出了CPU，比如等待IO或者调用了sleep()，那么操作系统可以在合适的时机保存它的上下文并切换到其他执行流。但是，像定时器中断这样的情况就不同了——执行流完全不知道什么时候会被打断，其是异步的，那么问题就变成了：操作系统是如何在一个程序运行的过程中，突然”插入“自己的代码的？</p><p>要理解这一点，就需要引入另一个非常关键的机制——中断。</p><br><h1 id="四-中断与内核栈"><a href="#四-中断与内核栈" class="headerlink" title="四.中断与内核栈"></a>四.中断与内核栈</h1><p>上一节中，提到了需要通过中断机制，在程序运行过程中“插入”内核自己的代码。在中断发生时，CPU会暂停当前正在执行的程序，并跳转到操作系统预先设置好的中断处理代码。</p><p>从执行流的角度来看，当前执行流被暂时打断，并进入了一段新的执行路径。不过，在开始执行这段新的路径之前，CPU必须先保存当前执行流的上下文信息，否则之后就无法恢复到原来的执行位置。<strong>问题是，当执行流从用户程序进入操作系统时，是否应该继续使用原先的栈？</strong></p><p>在执行用户代码时，执行流一直运行在用户栈上，函数返回地址、局部变量以及寄存器保存等信息，都会被压入到其中。如果CPU要执行中断处理代码，即执行内核代码时依然将这些信息压入到用户栈中，就会导致一个问题——<strong>用户执行流的状态和内核执行流的状态被混在了同一个栈上。</strong> 此外，更重要的是会导致安全问题，但由于本文重点在于”栈与执行流”，所以就不展开了。</p><p>因此，在中断发生并进入操作系统代码前，CPU必须先切换到一块由内核自己管理的栈空间下，而这块专门用于执行内核代码的栈，就被称为<strong>内核栈</strong></p><p>此时，CPU 实际上仍然在继续同一条执行流，只不过这条执行流的运行环境已经从用户空间切换到了内核空间。当中断处理完成后，操作系统再恢复之前保存的执行状态，并切换回用户栈，执行流便可以继续向前推进。</p><p>从执行流的视角来看，<strong>内核栈的作用其实非常简单：它为执行流进入内核之后提供了一个新的运行栈环境，使得可以独立、安全地继续下去</strong></p><p>到目前为止，我们讨论的主要是执行流<strong>被动</strong>进入内核的情况，比如定时器中断或设备中断，统称为硬件中断，这些事件都是异步发生的。但在程序运行过程中，程序往往还需要<strong>主动</strong>请求操作系统提供某些服务，比如读取文件、分配内存、创建进程等等，其都涉及到了硬件资源或者内存资源，不能由用户程序完成，必须交给操作系统来实现。</p><p>于是，程序需要一种机制，能够让当前执行流<strong>主动</strong>进入内核并执行一段内核代码，而这种机制就是<strong>系统调用</strong></p><p>从执行流的角度来看，系统调用和中断的行为非常类似，只不过前者是程序主动发起的，后者是硬件事件被动触发的。<strong>二者本质上都完成了一件事：让操作系统接管当前执行流。</strong></p><p>注意，无论是系统调用还是中断，执行流本身都没有被真正被改变。 具体来说，在系统调用和中断发生时，CPU只是“暂停”执行当前的用户代码，并切换到内核栈上执行一段内核代码，当执行完成后，会恢复之前保存的上下文，并返回到原来的用户代码，从上次“暂停”的地方继续执行。</p><p>但是在某些情况下，操作系统做的可不仅仅是接管执行流，而是会在程序运行过程中改变执行流本身。</p><p>比如，当进程受到某些事件通知时，操作系统可能需要让程序执行一段用户提前定义好的代码，用于处理该通知，而不是立刻继续原先被“暂停”的执行路径，在将用户定义好的代码执行完成后，再“继续”原先的执行路径。</p><p>要实现这种效果，操作系统就必须在从内核空间返回到用户空间前，修改用户执行流的状态——特别是用户栈上的内容。</p><p>而这种能够在内核运行过程中，动态修改用户栈的机制，就是<strong>信号</strong></p><br><h1 id="五-信号：操作系统如何修改执行流"><a href="#五-信号：操作系统如何修改执行流" class="headerlink" title="五.信号：操作系统如何修改执行流"></a>五.信号：操作系统如何修改执行流</h1><p>上一节中最后提到了信号，此时可能会有这样的疑问：<strong>如果操作系统修改了用户栈，在用户栈上插入了新的执行路径，那么原本的调用栈结构不会被破坏吗？还能从最开始“暂停”的地方继续执行吗？</strong></p><p>例如，假设程序正在执行某个函数 func()，此时用户栈上已经存在一系列函数调用形成的栈帧。此时因为中断&#x2F;系统调用进入内核空间，并在从内核空间返回到用户空间前，发现需要处理信号，那么需要让程序跳转到用户定义的信号处理函数 handler()，而 handler() 也需要使用用户栈来执行。这样一来，它似乎就会覆盖原有的栈内容。</p><p>如果真的发生这种情况，那么当信号处理函数返回时，程序显然已经无法回到原来的执行位置。</p><p>然而事实并非如此。信号处理函数执行完成后，程序依然可以准确地回到被打断的位置继续运行，就像什么都没有发生过一样。</p><p>这是怎么做到的？在从内核空间准备返回用户空间时，发现需要处理信号。此时，内核是已经保存好了被打断时的上下文信息的。接下来，内核会在用户栈上额外开辟一小段内存空间，称为single frame，并会向其中写入被打断时的上下文信息。</p><blockquote><p> 注意，不是在收到信号时就立刻处理，而是因为系统调用&#x2F;中断进入内核空间后，在从内核空间准备返回到用户空间时才主动检查是否有需要处理的信号</p></blockquote><p>这些准备工作完成后，内核会对执行流的状态做两处关键调整：</p><p>1.内核修改程序计数器，使得当前执行流重新回到用户空间时，不再回到原先被打断的位置，而是跳转到用户注册的信号处理函数。</p><p>2.内核调整RSP，使信号处理函数执行时，使用刚刚构造好的那段栈空间。这样一来，从用户程序的角度来看，执行流就像是“突然”调用了一个新的函数</p><p>随后，信号处理函数就像普通函数一样开始运行，它也会在single frame中继续建立自己的函数栈帧，并处理对应的逻辑。在处理完成后，执行流不会直接回到原先被打断的位置，而是进入一个由系统提供的特殊恢复过程。这个过程会重新进入内核，读取之间保存在single frame中的上下文信息并重新加载到内核栈中。此时，执行流才会从内核空间返回到用户空间，并将上下文信息加载到寄存器中。于是，执行流就可以继续从原来被打断的位置继续运行。</p><p>从执行流的角度，整个过程发生了下面这样的一次“插入式调用”：</p><p>信号处理前：</p><p>用户栈 (高地址 ↓)</p><pre><code>+------------------+| foo() frame      |+------------------+| bar() frame      |  ← 当前正在执行+------------------+| ...              |+------------------+</code></pre><p>内核插入single frame：</p><pre><code>用户栈 (高地址 ↓)+------------------+| foo() frame      |+------------------+| bar() frame      |+------------------+| signal frame     |  ← 保存原寄存器/返回信息+------------------+| handler frame    |  ← 信号处理函数开始执行+------------------+</code></pre><p>最后返回：</p><p>用户栈 (高地址 ↓)</p><pre><code>+------------------+| foo() frame      |+------------------+| bar() frame      |  ← 恢复+------------------+| ...              |+------------------+</code></pre><p>可以看到，信号处理函数并不会破坏原有的栈结构</p><p>而信号机制的本质就是，操作系统在用户栈上通过single frame，构造了一次“伪函数调用”，并在处理完成后恢复原先的上下文，从而实现<strong>对用户执行流的动态修改</strong></p><pre><code>原来的执行流    ↓foo()    ↓bar()   ← 信号在这里发生    ↓handler()   ← 临时插入的执行路径    ↓bar()       ← 恢复原来的上下文    ↓foo()</code></pre><br><h1 id="六-总结"><a href="#六-总结" class="headerlink" title="六.总结"></a>六.总结</h1><p>本文从最开始的函数调用，到线程与线程调度，再到中断与信号，这些看似分散的机制，实际上都可以从一个统一的视角来理解——执行流。</p><p>在程序运行过程中，执行流描述了当前代码的执行路径，而栈则记录了这条路径的结构。函数调用通过在栈上创建新的栈帧，使执行流能够进入新的函数并在结束后返回原来的位置。线程出现后，其作为基本调度单位，操作系统通过保存和恢复上下文，使得CPU可以在不同的执行流之间切换，从而实现了并发。</p><p>而中断和系统调用则进一步扩展，允许操作系统接管当前执行流，进入内核空间执行内核代码。信号则更进一步，不仅能让操作系统接管执行流，还可以在返回用户空间前修改用户执行流本身。实际上，这其中还有很多没有展开的细节，考虑到篇幅有限以及复杂性，就不细说了</p><p>所以，从执行流的视角来看，操作系统中调度很多机制，本质上都在围绕着一个问题展开：如何保存、切换和恢复执行流</p>]]></content>
      
      
      <categories>
          
          <category> 操作系统 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>回调 委托</title>
      <link href="/posts/delegate.html"/>
      <url>/posts/delegate.html</url>
      
        <content type="html"><![CDATA[<p>本篇文章主要讲解委托的底层原理，但在此之前，我们必须先理清委托究竟是什么——其不过是 callback 在 C# 中的具象化体现。​因此，在真正进入委托的底层实现之前，我们需要先回到 callback 本身：它是如何工作的、为什么需要它、以及它在异步与解耦中的角色。理解了这些，再看委托的内部结构、多播机制和调用流程，就会变得顺理成章。</p><h1 id="理解callback"><a href="#理解callback" class="headerlink" title="理解callback"></a>理解callback</h1><p>【callback是什么？】callback是一种思想，其核心原理是：你定义一个函数，将其作为参数交给某个框架、库，对方将其保存起来，等到某个条件满足时，比如资源加载完成、状态发生变化时，再执行该函数。</p><p>【为什么要有callback？】<strong>而之所以要有callback，其根本动机是为了实现控制权的反转</strong>，callback函数就像是一个钩子函数一样，你将你自己的逻辑“挂到”一个更大的系统中，将逻辑的调用权从你手里，转换到了框架手里，由框架在它自己的某个生命周期节点调用你的函数。从callback这个名字中也能感受到这层意思：不是我主动call你，而是你在合适的时机call back我</p><p><strong>这样做的意义是可以让模块间解耦，这也是callback的首要目标。</strong> 比如一个资源下载系统，用户需要在下载完成后执行一些逻辑，比如将下载的资源显示出来，或是打印日志等，这些逻辑不应该由资源下载系统决定，资源下载系统也不可能知道你用户到底想干什么，此时就需要callback——由用户将“资源下载完成后的逻辑”，通过参数的方式传递给资源下载函数，此时该函数的调用权就到了资源下载系统手中，等到资源下载完成时则会调用该逻辑</p><pre><code>//资源下载系统：void Download(Action onComplete){    // 下载逻辑    //.....    //下载完成 调用用户传入的逻辑    onComplete?.Invoke();}//用户：Download(() =&gt; {    Debug.Log(&quot;UI更新&quot;);});</code></pre><p>【什么场景下需要callback呢？】在callback核心原理中提到，函数逻辑“等到某个条件满足时”再被调用。这里”等“的主体是帮你做事的系统、框架等，而不是要你去等，你在将callback函数交给系统、框架后就继续向后执行了，称其为异步，<strong>异步是callback最经典的应用场景</strong>。比如网络请求、IO操作、事件系统等，其都不能立马返回结果，只能在未来条件满足时通知你，此时callback在时序上进行解耦</p><p>相对地，如果“等”的主体是你，即同步，那么没必要使用callback（实际上，在同步下使用callback可以理解为”策略注入“，这里说没必要指的是没必要为了在时序上解耦而使用callback，因为不存在耦合）。如果条件都满足了，比如资源都加载完成了，那为什么不直接将资源作为返回值直接给你呢？直接根据结果进行调用不就好了？但这种方法闲置了CPU资源，在你等待期间，主线程一直被阻塞，难以处理高并发需求</p><p>如果我们从栈与函数调用的角度来看：同步下，函数的后续执行可以依赖于栈机制自动恢复，继续调用你提供的函数；但是在异步下，函数在获取到结果返回前就已经结束了，调用栈被销毁，因此必须将”后续的逻辑“从栈中“剥离”出来，以callback的方式保存，以在未来由完成方调用。</p><p>【callback的语言级别的实现？】C中的函数指针、C++中的lambda、function、C#中的委托等等，本质上都是在实现callback，只不过其具有的能力不同</p><p>C的函数指针，本质上就是告诉框架、系统一段函数地址，条件满足后你条过去执行就可以，这是最基本、最本质的实现；而C++中的lambda&#x2F;C#的委托则通过闭包来支持上下文的捕获，此时的callback就不只是函数地址了，还有一份上下文数据；而C++中的function则是将多种callback的形式做了一个统一，其可以支持装载函数对象、lambda、bind等</p><blockquote><p>这里提到了闭包，实际上闭包的好处不止是提供了上下文数据，其在内存管理上同样起到了十分重要的作用。在没有GC的语言下，比如C++，其通过RAII的思想，通过作用域来管理资源的生命周期。而在异步编程下，作用域起不到较好的资源管理作用，容易出现资源提前释放或泄露的问题，所以可以让闭包持有资源的所有权，将栈上的资源提升到堆，让资源脱离原有的作用域并延续其生命周期，再利用闭包的作用域机制将资源安全释放</p></blockquote><p>【callback的缺点在哪？】callback最严重的问题在于异步链条过长，callback层层嵌套，导致代码可读性差。而为了解决这个问题，从而出现了async&#x2F;await这样的语法糖，允许我们以同步的方式写异步，通过底层的状态机对象以及回调注册机制，自动帮我们实现callback的效果</p><pre><code>getUser(id, function(user) {  getOrders(user, function(orders) {    getDetails(orders, function(details) {      console.log(details);    });  });});</code></pre><h1 id="理解委托-事件"><a href="#理解委托-事件" class="headerlink" title="理解委托 事件"></a>理解委托 事件</h1><p><strong>委托是一个面向对象的，类型安全的函数指针。</strong> 具体来说，其相比于C&#x2F;C++中的函数指针有三个不一样的地方：</p><ul><li><p>面向对象。当我们通过delegate void Demo()声明一个委托时，本质上就是定义了一个叫做Demo的类型；而当我们创建委托实例时，比如Demo ptr &#x3D; this.DoSomething时，本质上就是实例化该委托类并将其中包含的函数指针、对象指针赋值，和C&#x2F;C++中获取函数指针类似；当我们只有委托时，比如ptr.Invoke()，本质上就是C&#x2F;C++中通过函数指针对函数进行调用。所以说，委托是一个面向对象的函数指针，其将各种对函数指针的操作都封装在了类中</p></li><li><p>类型安全。委托作为一个托管类，其同样也要遵守一般类的规则，哪怕返回值、参数列表相同，但是名称不同，在CLR看来就是两个相互独立的类，不允许相互转型</p></li><li><p>多播。这是委托相较于函数指针独有的特点，允许为一个委托对象绑定多个方法，底层通过数组承载，在执行委托时，会依次执行这些方法</p></li></ul><p><strong>事件和委托？</strong></p><p><strong>事件是对委托的一次封装。</strong> 委托本质上是一个类，所以委托字段就和其他字段一样，最好被封装为属性，不要直接暴露给外界。而对于委托类型的字段来说，其就是被封装为事件——属性通过get&#x2F;set方法提供对字段的访问，而事件则提供add&#x2F;remove方法，提供对委托的访问</p><h1 id="委托底层原理"><a href="#委托底层原理" class="headerlink" title="委托底层原理"></a>委托底层原理</h1><p>当我们定义一个委托类型时，编译器会为我们生成一个对应的类，类中主要会维护三个指针：一个是对象指针，指向this(当是成员函数时)或者委托实例自己(当是静态函数时)，另外两个是方法指针，分别指向成员函数和静态函数。</p><pre><code>public abstract partial class Delegate : ICloneable, ISerializable{    internal object? _target; //如果注册的是实例方法，则是this指针，如果是静态则是delegate实例自己。    internal object? _methodBase; //缓存    internal IntPtr _methodPtr;//实例方法的入口，看到IntPtr关键字就知道要与非托管堆交互，必然就是函数指针了，    internal IntPtr _methodPtrAux;//静态方法的入口}public abstract class MulticastDelegate : Delegate{ //多播委托的底层基石    private object? _invocationList;     private nint _invocationCount; //实例委托调用此方法 private void CtorClosed(object target, IntPtr methodPtr)    {        if (target == null)            ThrowNullThisInDelegateToInstance();        this._target = target;        this._methodPtr = methodPtr;//函数指针被指向_methodPtrAux    } //静态委托调用此方法 private void CtorOpened(object target, IntPtr methodPtr, IntPtr shuffleThunk)    {        this._target = this;//上面说到，_target的注释不对的判断就在此        this._methodPtr = shuffleThunk;//与实例委托不同，这里被指向一个桩函数        this._methodPtrAux = methodPtr;//函数指针被指向_methodPtrAux    }}</code></pre><p>在为委托赋值时，底层会根据该方法对应的<strong>MethodDesc运行时对象</strong>中的信息（但是根本的信息来源是MethodDef元数据），判断该对象是一个静态方法还是一个成员方法（实际上会更复杂，这里简化），并调用对应的函数：</p><ul><li><p>如果是静态方法：则调用CtorOpened，让对象指针指向委托自己，并为对应的函数指针赋值</p></li><li><p>如果是成员方法：则调用CtorClosed，让对象指针指向实例对象，同样为对应的函数指针赋值</p></li></ul><p>此时，三个指针都已经初始化完成，之后就可以通过使用委托时，就可以在底层通过对象指针调用到对应的成员方法了。<strong>不过，成员方法的调用和静态方法的调用并不相同：前者需要传入this指针，而后者不需要。所以为了统一调用模型，会在静态方法的调用上额外做一层适配</strong></p><pre><code>//实例方法：delegate.Invoke()  → _methodPtr(target, args...)//静态方法：delegate.Invoke()   → _methodPtr (thunk)        → 调用 _methodPtrAux (真正静态函数)_methodPtr指向的适配函数：void ShuffleThunk(object target, int x){    // 忽略 target（因为静态方法不需要 this）    // 调用 _methodPtrAux 指向的静态函数    RealStaticMethod(x);}</code></pre><h1 id="多播委托的实现"><a href="#多播委托的实现" class="headerlink" title="多播委托的实现"></a>多播委托的实现</h1><p><img src="https://img.aixheee.top/6202def1-318b-4d89-935f-b2b37617794d.png"></p><p><img src="https://img.aixheee.top/f7768824-d870-4180-b3e2-c9770e4e2464.png"></p><p>多播委托的实现依赖于MulticastDelegate中的两个字段：</p><ul><li><p>_invocationList：其是一个object[]，专门用于存储委托调用列表，其使用方式和扩容策略类似于List</p></li><li><p>_invocationCount：代表了列表中委托的数量</p></li></ul><p>+&#x3D;时所依赖的关键函数CombineImpl：</p><p>在调用 +&#x3D; 时，其负责将当前delegate和其他delegate组合为一个新的delegate。在要放入新的delegate时，会尝试将delegate放入到原先的object[]中。但如果要放入的位置上已经有delegate且不相同，或者说obejct[]中根本没地方放，则会通过二倍扩容的方式，新分配一个object[]并拷贝数据（对于引用类型来说是浅拷贝）、追加新delegate。在放入完成后，会分配一个新的delegate对象并返回，其引用的是一个新的object[]，并更新_invocationCount</p><blockquote><p>注意，是返回一个新的delegate对象，而不会复用同一个delegate对象(为了优化，会尝试复用同一个object[])。因为委托属于“不可变对象”，有点像值语义对象，类似于C#的string类型，以保证行为可预测，防止出现意外，同时多线程访问安全。比如以下场景：</p><pre><code>void Register(Action handler){    // 如果委托可变    handler += AnotherMethod;  // 修改了传入的委托！    // 外部调用方会惊讶地发现自己的委托被改变了}</code></pre></blockquote><p>之所以有可能要放入的位置上已经有了delegate，是因为不同的delegate对象可能共享同一个object[]，只不过通过_invocationCount来代表其所拥有的delegate，以在遍历调用时确定实际范围，比如以下场景：</p><pre><code>Action a = A + B + C;Action b = a;a += D;//object[]可能是这样的：[A, B, C, D, null, null]//a和b共享同一个object[]（注意，a和b只是共享object[]，而不共享Action对象），但是 a._invocationCount = 4，b._invocationCount = 3b: count=3 → A, B, Ca: count=4 → A, B, C, D</code></pre><p>CombineImpl具体逻辑：<a href="https://github.com/dotnet/runtime/blob/main/src/coreclr/System.Private.CoreLib/src/System/MulticastDelegate.CoreCLR.cs#L212">runtime&#x2F;src&#x2F;coreclr&#x2F;System.Private.CoreLib&#x2F;src&#x2F;System&#x2F;MulticastDelegate.CoreCLR.cs at main · dotnet&#x2F;runtime</a></p><pre><code>protected sealed override Delegate CombineImpl(Delegate? follow){    // 1. 空值检查：如果 follow 是 null，直接返回当前委托本身    if (follow is null)        return this;    // 2. 类型安全检查：确保要合并的两个委托的类型签名完全一致    if (!InternalEqualTypes(this, follow))        throw new ArgumentException(SR.Arg_DlgtTypeMis);    // 3. 转换为多播委托类型以便访问内部字段    MulticastDelegate dFollow = (MulticastDelegate)follow;    // 4. 分析 follow 委托的结构    object[]? resultList;           // 结果列表，将存储合并后的所有委托    int followCount = 1;            // follow 中的委托数量，默认是单播委托    object[]? followList = dFollow._invocationList as object[];  // 获取 follow 的调用列表    // 如果 followList 不为 null，说明 follow 是一个多播委托    if (followList != null)        followCount = (int)dFollow._invocationCount;  // 从字段中获取实际的委托数量    int resultCount;  // 合并后的总委托数量    // 5. 情况1：当前委托是单播委托（_invocationList 不是 object[]）    if (_invocationList is not object[] invocationList)    {        // 5.1 计算合并后的总数量 = 当前委托(1) + follow 中的委托数量        resultCount = 1 + followCount;        // 5.2 创建结果数组，长度为总数量        resultList = new object[resultCount];        // 5.3 将当前委托放入数组首位        resultList[0] = this;        // 5.4 将 follow 的委托列表复制到结果数组中        if (followList == null)        {            // 如果 follow 是单播，直接放入第二个位置            resultList[1] = dFollow;        }        else        {            // 如果 follow 是多播，遍历其调用列表            for (int i = 0; i &lt; followCount; i++)                resultList[1 + i] = followList[i];        }        // 5.5 创建新的多播委托（当前是单播，所以 thisIsMultiCastAlready = false）        return NewMulticastDelegate(resultList, resultCount);    }    // 6. 情况2：当前委托已经是多播委托    else    {        // 6.1 获取当前多播委托的委托数量和调用列表        int invocationCount = (int)_invocationCount;        resultCount = invocationCount + followCount;  // 计算合并后的总数量        resultList = null;  // 结果列表初始化为空        // 6.2 尝试复用当前委托的调用列表数组（空间足够时才尝试复用）        if (resultCount &lt;= invocationList.Length)        {            resultList = invocationList;  // 复用现有数组            if (followList == null)            {                // 如果 follow 是单播委托，尝试将其设置到数组的可用位置                if (!TrySetSlot(resultList, invocationCount, dFollow))                    resultList = null;  // 设置失败（如重复委托），放弃复用            }            else            {                // 如果 follow 是多播委托，尝试设置其中的每个委托                for (int i = 0; i &lt; followCount; i++)                {                    if (!TrySetSlot(resultList, invocationCount + i, followList[i]))                    {                        resultList = null;  // 任何一个设置失败，就放弃复用                        break;                    }                }            }        }        // 6.3 如果无法复用原数组（空间不足或TrySetSlot失败），创建新数组        if (resultList == null)        {            // 6.3.1 计算新数组大小：至少是原来的2倍，但必须能容纳所有委托            int allocCount = invocationList.Length;            while (allocCount &lt; resultCount)                allocCount *= 2;  // 按2倍扩容，减少后续扩容次数            // 6.3.2 创建新数组            resultList = new object[allocCount];            // 6.3.3 复制当前委托的所有委托到新数组            for (int i = 0; i &lt; invocationCount; i++)                resultList[i] = invocationList[i];            // 6.3.4 复制 follow 的委托到新数组            if (followList == null)            {                resultList[invocationCount] = dFollow;            }            else            {                for (int i = 0; i &lt; followCount; i++)                    resultList[invocationCount + i] = followList[i];            }        }        // 6.4 创建新的多播委托（当前已是多播，所以 thisIsMultiCastAlready = true）        return NewMulticastDelegate(resultList, resultCount, true);    }}/// &lt;summary&gt;/// 尝试在指定数组索引处设置委托对象/// 此方法用于在合并多播委托时，将新的委托添加到调用列表数组的指定位置/// &lt;/summary&gt;/// &lt;param name=&quot;a&quot;&gt;调用列表数组，可能包含null或已有的委托引用&lt;/param&gt;/// &lt;param name=&quot;index&quot;&gt;要设置的数组索引位置&lt;/param&gt;/// &lt;param name=&quot;o&quot;&gt;要设置的委托对象，必须是MulticastDelegate类型&lt;/param&gt;/// &lt;returns&gt;如果成功设置或委托已存在，则返回true；如果位置已被不同委托占用，则返回false&lt;/returns&gt;private static bool TrySetSlot(object?[] a, int index, object o){    // 第一阶段：原子性尝试设置（处理并发场景）    // 1. 检查指定索引位置是否为空    // 2. 如果是空的，则使用Interlocked.CompareExchange原子性地尝试设置委托对象    // 3. CompareExchange的作用：比较a[index]当前是否为null，如果是则用o替换，返回原始值    // 4. 如果原始值为null，说明我们成功设置了该位置，返回true    if (a[index] == null &amp;&amp; Interlocked.CompareExchange(ref a[index], o, null) == null)        return true;    // 第二阶段：重复委托检测（处理非并发场景或设置失败后的检查）    // 如果原子设置失败，可能是因为：    // 1. 位置已被其他线程设置了不同的委托    // 2. 位置已经有委托（可能是之前添加过但又被移除了）    // 3. 当前线程之前已经设置过相同的委托    // 检查指定位置是否已经有对象存在    if (a[index] is object ai)    {        // 将待设置的委托和数组中现有的委托都转换为MulticastDelegate        MulticastDelegate d = (MulticastDelegate)o;    // 要添加的新委托        MulticastDelegate dd = (MulticastDelegate)ai;  // 数组中已存在的委托        // 比较两个委托是否表示相同的方法调用        // 通过比较三个核心字段来确定委托的完全相等性：        // 1. _methodPtr: 方法指针，指向实际执行的方法        // 2. _target: 目标对象，对于实例方法是对象实例，对于静态方法是null        // 3. _methodPtrAux: 辅助方法指针，在多播委托中用于处理调用逻辑        if (dd._methodPtr == d._methodPtr &amp;&amp;            dd._target == d._target &amp;&amp;6            dd._methodPtrAux == d._methodPtrAux)        {            // 如果两个委托完全相等，则视为&quot;成功设置&quot;            // 这发生在：同一个方法被移除后又重新添加，或者多个线程尝试添加相同委托的情况            return true;        }    }    // 设置失败：指定位置已被不同的委托占用    return false;}</code></pre><p><strong>对于多播委托，在Invoke时是如何调用的呢？</strong> 依据上面的分析，可知会通过_invocationCount控制遍历范围，仅调用_invocationList的前_invocationCount个元素，从而允许多个delegate共享同一个object[]但保持逻辑隔离，是按内存共享</p><p>具体的多播调用逻辑可以在<a href="https://github.com/dotnet/runtime/blob/main/src/coreclr/vm/comdelegate.cpp#L2184">runtime&#x2F;src&#x2F;coreclr&#x2F;vm&#x2F;comdelegate.cpp at main · dotnet&#x2F;runtime</a>查看，其是一个非托管方法</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>至此，我们从 callback 的思想一路走到了委托的底层实现。你会发现，所谓的“高级特性”，往往只是基础思想在语言层面的精巧包装。委托没有创造新的概念，它只是让 callback 变得更安全、更强大，也更面向对象。​ 看清了这一点，剩下的 API 细节，也就只是顺理成章的延伸了。</p>]]></content>
      
      
      <categories>
          
          <category> C# </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>【内存分配与管理】Unity GC</title>
      <link href="/posts/UnityGC.html"/>
      <url>/posts/UnityGC.html</url>
      
        <content type="html"><![CDATA[<p>在上一篇文章中，我们深入剖析了Unity托管内存的分配机制，从Boehm GC的ok_freelist与GC_hblkfreelist两级结构，到小对象与大对象的差异化分配策略，再到hblk与hblkhdr分离设计的精妙之处。我们看到了Unity如何在C++引擎层搭建起一套高效的内存分配基础设施，为C#层的对象创建提供支撑。</p><p>然而，分配只是内存管理的一半,另一半，也是我们更为关切的痛点——垃圾回收。当托管堆上的对象不再被引用时，它们占据的内存需要被回收以供后续分配使用。Unity采用的Boehm GC是一个基于Mark-Sweep的保守式GC，它的工作原理与传统GC有着显著的不同：它无法精确区分指针与非指针数据，无法对内存进行整理压缩，也无法像.NET的GC那样实现分代回收。这些特性决定了Unity GC的性能特征和局限性。</p><p>本文将从GC的触发条件入手，深入剖析Mark阶段与Reclaim阶段的完整流程，包括根节点扫描、地址到header的映射机制、标记位的管理与回收策略。通过理解这些底层原理，我们将能更好地把握Unity内存管理的本质，为后续的优化实践打下坚实基础。</p><h1 id="托管内存管理机制"><a href="#托管内存管理机制" class="headerlink" title="托管内存管理机制"></a>托管内存管理机制</h1><p><strong>对于托管内存，其依赖于Unity的Boehm GC管理。Boehm GC是一个基于mark-sweep的保守式的GC，在Unity 2019以后，引入了增量模式以降低卡顿，其会将标记阶段均分到N帧，每帧只执行3ms，以避免单次长时间中断程序运行。</strong></p><p>【实现GC的必要准备？】在对象被分配创建时，Unity的内存分配器会按照GC的需要组织内存布局，比如维护标记位和块信息等。</p><p>几个注意点：</p><ul><li><p>.NET的GC和Unity的Boehm GC不是一个东西，Boehm GC的功能和复杂度弱于.NET的GC，跨平台性更强</p></li><li><p>无论是Mono还是IL2CPP，使用的都是Boehm GC</p></li></ul><p>【保守式GC，精确式GC？】<br>保守式GC指的是，GC 在扫描栈或寄存器时，无法准确判断某个值是否是对象引用，只能”猜测”——如果某个字（word）的值恰好等于堆中某个对象的起始地址，就保守地把它当成引用；精确式GC指的是，GC 确切知道栈&#x2F;寄存器中每个槽位的类型：是对象引用还是基本类型。</p><p><mark>保守式GC和精准式GC相比，其缺点在哪？</mark></p><p><strong>一是无法对内存做整理和压缩，即把对象移动到别的地址上来整理内存，压缩碎片，其内存碎片问题严重，缓存局部性较差。</strong></p><p>关于内存碎片问题严重：由于无法整理压缩，所以<strong>在小对象</strong>，比如大量的临时小对象（向量、UI时间、GameObject对象等），<strong>频繁地分配与回收场景下，会导致大量的外部碎片——由于内存不连续，回收后形成“空洞”，导致无法分配连续的大块内存。</strong> 【Boehm GC是如何处理内存碎片的？】Boehm GC无法从根本上消除外部碎片，只能通过一些方法缓解：</p><ul><li><p>通过空闲链表ok_freelist来管理小内存块，并且这些小内存块来自于一整块完整的大内存块，尽可能的避免频繁分配销毁小对象导致过多外部碎片</p></li><li><p>在回收hblk时，会检查连续内存前后的hblk是否空闲，如果空闲会进行合并再回收到GC_hblkfreelist中</p></li></ul><p>关于缓存局部性较差：<strong>整理压缩后存活对象都集中在一片连续的内存区域上，使得逻辑上相互引用的对象在物理上更加接近，提高了缓存局部性。</strong> 这使得在后续的Mark阶段中，对对象节点，即hblk进行扫描标记时的缓存命中率提高，从大量分散下的pointer chasing转变为了更接近于顺序访问</p><p>【为什么保守GC做不到整理压缩？】之所以无法做到这一点，是因为如果要做整理压缩，就必须知道指向对象的指针在哪，否则在进行整理时，无法对原先指针的指向进行更新，其依然指向旧地址</p><p>在精准GC下，编译器明确地知道内存中哪些数据是指针，绝对不会把一个整数或是其他内容解释为地址，所以可以直接枚举所有的引用，并安全地移动对象。<strong>而保守式GC缺乏精确的指针信息，只能按照数值范围、内存块状态来”猜测“，这个值是不是一个内存地址，无法精准地判断。为了避免出现整理时，漏更新指针或是误判指针的问题，所以干脆完全不整理，对象一旦分配，其地址完全固定，GC只是做标记与回收</strong></p><p><strong>二是存在“内存泄露”，如果一个整数值、字符串数据恰好等于一个已死亡对象的地址，那么GC会认为该值引用了该死亡对象，从而错误的认为其是可达的，该对象无法被回收。</strong> 为了避免该问题，Unity提出来几个策略：</p><ul><li><p>尽量避免GC。<a href="https://docs.unity3d.com/cn/2021.1/Manual/UnderstandingAutomaticMemoryManagement.html">了解自动内存管理 - Unity 手册</a>Unity官方强调要内存重用，而不是一味地依赖于GC。常见的做法比如使用对象池、避免频繁new、重用List、Array等</p></li><li><p>尽量避免”看起来像指针的数据“、控制堆布局等。Unity在引擎层会避免看起来像指针的数据，并尽可能地控制heap区间，从而降低随机值命中堆地址的概率</p></li><li><p>将重要大块资源完全由手动管理。对于纹理、音频、动画等核心资源对象，需要用户显示地管理，只有脚本层的小对象才由Boehm GC管理</p></li></ul><p><mark>为什么Unity选择了Boehm GC？</mark></p><p>在纯托管语言下，每个变量的类型信息、对象布局完全可以通过运行时系统得知，所以可以做到精确GC，但是在Unity引擎核心是C++，其根本不会存储这些信息。如果想要实现精确GC，那么需要给每个栈帧提供metadata，记录类型信息，但这在C++引擎下几乎不可能实现，更别提外部插件了。所以，Unity只能选择Boehm GC，只要一个值满足一系列的判断要求，就认定其为一个对象指针，其大大简化了GC的实现以及脚本层和引擎内部的交互，无需对C++引擎进行大规模改造</p><br><h1 id="RAII与GC"><a href="#RAII与GC" class="headerlink" title="RAII与GC"></a>RAII与GC</h1><p><mark>GC下的资源对象管理和C++下的资源对象管理：</mark></p><p><strong>对象可以理解为“物理内存+生命周期”，这一点无论是在C++下，还是在Unity下都是共通的</strong>。二者的区别在于，谁来负责内存的管理，谁来负责生命周期的管理？</p><p>关于内存的管理，二者都是通过底层的内存分配器来分配内存。比如C++下默认的operator new使用malloc来分配内存，operator delete使用free来释放内存，而Unity同样有相应的底层分配器来进行管理，后续会看到这一点</p><p>对于生命周期的管理是二者的主要差异。<strong>C++中，通过将资源的生命周期与对象的生命周期相绑定来进行管理，即RAII</strong>。</p><p><strong>而在Unity的托管内存模型下，内存资源的生命周期不再通过”对象的生命周期“来管理，而是通过“可达性分析”来决定，生命周期从”对象自身属性“转变为了“引用图属性”</strong></p><p><mark>进一步的，为什么发展出了基于“可达性”分析的托管内存，其相较于基于“作用域”管理方式有什么优点吗？</mark></p><p>基于对象的生命周期来管理，其隐含了一个前提——<strong>对象的生命周期是相互嵌套的，但如果一个资源对象想要被共享呢，那资源对象应该由谁来释放呢？</strong> 从数据结构的角度来看，对象间的关系从树结构转变为了图结构，这该怎么解决？</p><p><strong>C++的”补救方案“是shared_ptr + weak_ptr，前者通过”引用计数“让一个资源对象被共享，后者通过”弱引用“解决”循环引用“的问题，其在一定程度上缓解了问题</strong>，但没有彻底的解决，在复杂场景下依然难以对资源对象进行管理。此外，在多线程环境下，其需要需要额外的原子操作，以及可能出现递归式地资源释放，进而导致卡顿。<strong>但是，其没法解决“动态环”的问题，只能解决“静态环”</strong></p><p><strong>而在现代GC下，不再通过作用域来约束，而是基于“可达性”来分析，其也不再问一个资源被谁所有，而是问一个对象能不能从root访问到。</strong> 这种做法降低了资源管理的复杂度，解决了循环引用的问题，同时可以通过一系列的优化手段，比如分代与增量来优化，避免突然卡顿</p><p><strong>那RAII相比于GC，其对于资源管理有什么优势吗？</strong></p><ul><li><p>资源释放的可控性。RAII是基于栈机制+作用域来管理资源的，可以明确地知道资源什么时候释放，相比之下，我们无法完全控制GC的时机，有可能导致卡顿问题</p></li><li><p>运行时开销。RAII成本较低，比如对于unique_ptr来说，几乎是零额外抽象成本；而GC需要Stop-The-World+递归扫描，运行时开销较大</p></li><li><p>资源类型的支持。资源实际上包括很多种——内存、文件、锁、网络连接等等，GC仅可以对向内存资源进行管理，而RAII的思路则适用于所有类型的资源管理。比如C#下，就需要通过using来实现类似于RAII的效果，以管理其他类型的资源。</p></li></ul><br><h1 id="垃圾回收"><a href="#垃圾回收" class="headerlink" title="垃圾回收"></a>垃圾回收</h1><p>在展开GC前，首先明确<mark>何时触发垃圾回收？</mark></p><p>在Boehm GC尝试从GC_hblkfreelist中获取hblk失败时，则会尝试进行垃圾回收或扩展堆内存，具体采用哪种方式的判断方式：</p><pre><code>/* Have we allocated enough to amortize a collection? */GC_INNER GC_bool GC_should_collect(void){    ...    return(GC_adj_bytes_allocd() &gt;= last_min_bytes_allocd           || GC_heapsize &gt;= GC_collect_at_heapsize);}</code></pre><p>两个条件：</p><p>一是“自从上次GC后已经分配的新内存，是否超过了一个阈值”，其中GC_adj_bytes_allocd代表了累计分配的字节数，其会为不同内存区域赋予不同的权重，<strong>逻辑是“如果已经给你分配了足够多的内存，那么就不会再分配新内存了，而应该回收一次；如果没有分配足够多的内存，其选择扩展堆”</strong>；</p><p>二是“当前堆的总大小，是否超过了一个阈值”，<strong>逻辑是“哪怕你分配速度不快，但是堆已经过大了，则应该回收一次，而不是继续扩充堆，以防止碎片问题严重”</strong></p><p>这实际上是在尽可能地保证：在垃圾过多而不是堆内存少时进行GC，在堆内存少时扩充堆</p><br><p>垃圾回收主要分为两个阶段：</p><ul><li><p>Mark标记阶段。首先重置内存节点的标记位（内存结点即hblk中的一个内存块对象），然后从内存的根节点开始遍历扫描内存空间，将托管堆上被引用的内存节点标记为“引用”</p></li><li><p>Reclaim回收阶段。遍历托管堆上的所有内存节点，然后将未标记为“引用”的内存节点回收</p><pre><code>GC_INNER GC_bool GC_try_to_collect_inner(GC_stop_func stop_func){    ...    //控制停止    if (GC_dont_gc || (*stop_func)()) return FALSE;    //抛出事件    if (GC_on_collection_event) GC_on_collection_event(GC_EVENT_START);    ...    //清空标记位    GC_clear_marks();    ...    //开始追踪内存节点，标记“引用”标记位    if (!GC_stopped_mark(stop_func)) {        ...    }    //结束标记，开始回收没有被引用的内存节点    GC_finish_collection();    ...    //抛出事件    if (GC_on_collection_event) GC_on_collection_event(GC_EVENT_END);}</code></pre></li></ul><br><h2 id="Mark阶段"><a href="#Mark阶段" class="headerlink" title="Mark阶段"></a>Mark阶段</h2><p><strong>GC是如何组织这些hblkhdr和hblk的，以支持在Mark阶段快速查找对应的hblkhdr？换句话说，Unity是怎么管理的托管内存？</strong> 依赖于两个结构，一个是_top_index二级索引，另一个是一维双向侵入式链表</p><ul><li><p>_top_index全局数组：为了实现地址到header的快速映射，以快速得到hblkhdr的信息，所以GC使用了<mark>二级索引结构</mark>，原理类似于虚拟内存物理内存映射的二级页表，<strong>_top_index则作为这套映射体系中的一级目录，而_bottom_index中的index[]则是二级目录，二者在物理上都是连续的数组，查找效率较高</strong>。</p><pre><code>GC_arrays └── _top_index[TOP_SZ]        // 一级索引（稀疏）       ↓     bottom_index              // 二级索引（按块）         ├── index[BOTTOM_SZ]  // 存 hdr*         ├── key               // 对应地址高位         ├── asc/desc_link     // 排序链 即侵入式链表         └── hash_link         // 哈希链（可选）</code></pre><p>具体来说，<strong>_top_index是一个稀疏数组</strong>（即不是所有的_top_index[i]都有值，只有_top_index[i]映射的地址区间内的地址被使用过，才会分配bottom_index），<strong>专门负责映射管理一大段地址空间，比如_top_index[42]代表从0x42000-0x42FFFF；而在这个范围内，bottom_index内的hdr指针数组index[]具体存放了hblkhdr信息，会一步对地址进行范围上的细分</strong></p><p><img src="https://img.aixheee.top/50e290b0-00bf-4789-b9f4-87d7448effb1.png"></p><p>通过内存节点的地址p所应到hblkhdr的流程：通过高42位+hash算法来索引_top_index，得到对应的bottom_index；再通过12-22位置索引bottom_index中的hdr指针数组，得到hblkhdr；最后通过低12位索引到该内存节点是hblk中的第几个对象</p><p><img src="https://img.aixheee.top/ca81a3d3-2a2b-477f-9f31-a6183615f8cb.png"></p></li><li><p>辅助链表：Unity还维护了一个一维的侵入式双向链表来辅助管理所有的hblkhdr数据，其将_top_index二级结构“摊平展开”，将链表节点内嵌在bottom_index中实现。其是一个有序链表，按照地址的高42位来排序。</p><p><img src="https://img.aixheee.top/b1bbe012-3812-4513-af85-4772650893a6.png"></p><p><strong>通过遍历此链表，就可以得到所有的hblkhdr以及相应的hblk地址，从而进一步遍历hblk中的内存节点</strong></p></li></ul><br><h3 id="标志位重置"><a href="#标志位重置" class="headerlink" title="标志位重置"></a>标志位重置</h3><p>在正式开始扫描前，需要通过GC_clear_marks方法将所有内存节点的标记位重置，其会遍历上述提到的一维双向侵入式链表，将所有的hblkhdr中的关于内存节点的标记位都清零来重置</p><p><strong>为什么在正式Mark前，需要先进行重置？</strong></p><p>为了防止脏缓存影响，如果一个内存节点在第一轮GC中被引用，标记位了1，但是随着程序运行已经没有被引用了，结果在第二轮GC中其标记位依然为1，则GC会误以为其还被引用，从而永远不会被回收。</p><p>所以，在每一轮GC开始时，都先需要清除所有的mark，再从roots开始重新标记。而之所以不实时维护引用关系，主要是其成本过高，比如反向依赖追踪、写屏障管理等</p><br><h3 id="线程暂停"><a href="#线程暂停" class="headerlink" title="线程暂停"></a>线程暂停</h3><p>在标记重置完成后，会将所有正在运行的线程暂停，形成一个全局一直的“内存快照”。具体来说，会暂停正在运行用户代码的线程，因为其可能会修改内存，导致引用关系发生变化，但是向当前的GC线程、阻塞线程等，则不会暂停。</p><p><strong>【具体如何暂停线程？】</strong></p><p>GC线程通过信号机制，给所有的目标线程发送signal，随后其他线程被被内核打断，进入GC自定义的signal handler，并在其中记录下寄存器中的状态，让线程卡在handler中不再返回</p><p><strong>【为什么首先需要线程暂停？】</strong></p><p>而之所以需要暂停线程，是为了正确获取到roots。<strong>roots包括静态变量、寄存器变量、每个线程的线程栈中的变量以及GC Handle。其中，静态变量和GC Handle属于uncollectable对象</strong></p><blockquote><p>GCHandle一般用于C++端对象引用C#对象，防止C#对象被回收</p></blockquote><p>如果不暂停线程，那么对于线程栈来说，此时的线程栈依然在发生变化，可能漏掉引用或错误回收；此外，有的指针存储在寄存器中，还没来得及压入栈中进行写入，就很可能把对象当作垃圾回收。所以，需要将线程暂停，等完成标记后再重新恢复</p><p>【什么时候恢复？】</p><p>在下一阶段的扫描完成后，会相应的给被暂停的线程发送一个恢复信号</p><br><h3 id="内存扫描"><a href="#内存扫描" class="headerlink" title="内存扫描"></a>内存扫描</h3><pre><code>GC_INNER GC_bool GC_mark_some(ptr_t cold_gc_frame){    switch(GC_mark_state) {    case MS_NONE:        break;    case MS_PUSH_UNCOLLECTABLE: {        ...        //        GC_push_roots(FALSE, cold_gc_frame);        GC_objects_are_marked = TRUE;        if (GC_mark_state != MS_INVALID) {            GC_mark_state = MS_ROOTS_PUSHED;        }    }    case MS_ROOTS_PUSHED: {        ...        MARK_FROM_MARK_STACK();        break;    }    ...    case MS_INVALID: {        ...    }    break;}</code></pre><p>【大致流程】</p><p>内存扫描通过GC_mark_some来完成，整体流程分为四个状态，算法根据不同的状态进行相应的处理：（<strong>以下描述中，根节点内存指的是“根节点所在的内存区域”，即栈内存、寄存器保存区和全局数据段；托管堆内存节点指的是，从hblk中等分出的一个个小内存块，其存储在托管堆上并代表着一个个对象所占用的内存</strong>）</p><ul><li><p>MS_INVALID：说明内存空间未开始进行扫描标记，将状态切换到MS_PUSH_UNCOLLECTABLE图中绿色方块表示未开始扫描的根节点内存，按照固定的扫描步长进行划分，64位下一般为8字节</p><p><img src="https://img.aixheee.top/cd4a556c-d72f-4b91-9e62-434455241983.png"></p></li><li><p><mark>MS_PUSH_UNCOLLECTABLE：</mark> <strong>将GC内部维护的uncollectable对象直接加入到mark stack中，同时通过GC_push_roots()来扫描栈、寄存器和全局变量，对其中疑似指针进行验证，如果认定其是一个指针，则将对应的内存结点打上标记，并将其加入到mark stack（mark stack是用数组实现的显式 DFS 工作栈）中</strong>。 完成后，将状态切换到MS_ROOTS_PUSHED如图，从根节点内存的起始地址开始，按照固定步长扫描，蓝色代表已经被扫描的内存空间，且被引用的“托管堆内存节点”会被标记为蓝色，为指向的全部标记为白色（会被回收）</p><p><img src="https://img.aixheee.top/5be495ac-9cec-45b7-a61b-453ce0278b83.png"></p></li><li><p><mark>MS_ROOTS_PUSHED</mark>：<strong>继续扫描之前被引用的“托管堆内存节点”，对mark stack进行“pop-&gt;扫描验证-&gt;push”的方式，递归式地为所有被引用的内存节点打上标记</strong>，随后切换到MS_NONE状态。所有未被标记的内存节点会被GC回收</p><p><img src="https://img.aixheee.top/6a90fabc-6237-41d6-99b7-833dd9a999ac.png"></p></li><li><p>MS_NONE：表示扫描全部结束</p></li></ul><br><p>MS_PUSH_UNCOLLECTABLE阶段的具体逻辑是什么？具体怎么扫描的？</p><p><mark>核心扫描逻辑GC_push_all_eager：</mark></p><pre><code>GC_API void GC_CALL GC_push_all_eager(void *bottom, void *top) {    ...    lim = t - 1    for (p = b; (word)p &lt;= (word)lim;        p = (word *)(((ptr_t)p) + ALIGNMENT)) {    REGISTER word q = *p;    GC_PUSH_ONE_STACK(q, p);   }}</code></pre><p>扫描范围即<strong>栈内存区、寄存器保存区和全局数据区</strong>，通过bottom和top将该内存区域标识出来，并按照word步长从bottom开始进行扫描，直到top结束。可以理解为将整个连续的内存区域按照word大小等分为若干个“内存块”，将存储的数据交给GC_PUSH_ONE_STACK进行检查，看其是否“像一个指针”</p><pre><code>for (p = bottom; p &lt;= top; p += 8) {    q = *p; //q即}</code></pre><p><mark>核心检查逻辑GC_PUSH_ONE_STACK：</mark></p><pre><code>GC_INNER void GC_mark_and_push_stack(ptr_t p, ptr_t source) {    hdr * hhdr;    ptr_t r = p;    PREFETCH(p);    GET_HDR(p, hhdr);    //能取到header信息    if (NULL == hhdr|| (r = (ptr_t)GC_base(p)) == NULL|| (hhdr = HDR(r)) == NULL)) {        return;    }    //判断空闲状态    if (HBLK_IS_FREE(hhdr) {        return;    }    ...    //标记并加入临时数组    PUSH_CONTENTS_HDR(r, GC_mark_stack_top, GC_mark_stack_limit,           source, hhdr, FALSE);}</code></pre><p><strong>GC_PUSH_ONE_STACK接收到”内存块“中的值，分为多步进行检查判断，只有通过了检查，才会将该值作为一个内存地址并将对应的hblk进行标记，防止被回收。</strong> 对于绝大多数的值来说，其根本不是一个内存地址，需要被过滤：</p><ul><li><p><strong>首先判断”该值代表的内存地址“是否在托管堆范围内。</strong> 托管堆的范围会在GC_hblklist中无可分配内存块时，调用内存分配接口时更新。这一步可以过滤掉绝大多数的垃圾值，即不代表内存地址的值</p></li><li><p><strong>NULL &#x3D;&#x3D; hhdr 判断能否通过该内存地址获取到hblkhdr信息。</strong> 具体来说，以内存地址索引全局数组_top_index，类似于虚拟地址和物理地址映射的流程，尝试获取到header信息。</p><pre><code>p 的高位 → 查 _top_index          → 得到 bottom_indexp 的中间位 → 在 bottom_index 里找 slot          → 得到 hblk 对应的 header</code></pre><p><strong>这一步是因为第一步的范围限制只是一个粗略区间，托管堆并不是完全连续的，中间可能存在未分配区域。</strong> 如果值代表的内存地址命中了这些空洞区域，同样会将其排除在外，不认为其是一个被引用的对象，不会被打上标记</p><pre><code>[heap chunk A]      [heap chunk B]     [heap chunk C]        ↑                ↑                 ↑    有 header        有 header         有 header</code></pre></li><li><p><strong>GC_base(p) 用于根据给定的地址，找出其属于哪个对象，并返回该对象的地址。</strong> 这一步主要通过header提供的信息来完成，比如hb_size。如果可以对应到一个对象的起始地址，则不会return，会进入后续流程比如，下面的q存储了对象内部的地址，其不应该被回收。<strong>总结一下，这一步检测的意义在于，即使程序中只保存了对象内部地址，该对象依然可以被正确标记为存活</strong></p><pre><code>struct Obj {    int a;    int b;    int c;};Obj* o = new Obj();// 栈里可能出现：q = &amp;o-&gt;b;</code></pre></li><li><p><strong>HBLK_IS_FREE判断当前对应的hblk是否是空闲的，如果hblk为空闲状态，则不会被标记，需要被GC回收。</strong> 这一步通过hblkhdr中的hb_flags信息来判断，该信息一般会在两处被更改——</p><ul><li><p>GC扫描后发现该hblk中没有一个存活对象时，调用GC_freehblk将hb_flags变为FREE空闲（hhdr-&gt;hb_flags |&#x3D; FREE_BLK;），在hblk从系统底层分配出来时，同样也会被设置为FREE空闲</p></li><li><p>将hblk中的内存结点提供给上层使用时，将hb_flags变为USED被使用</p></li></ul></li><li><p>标记逻辑PUSH_CONTENTS_HDR</p><pre><code># define PUSH_CONTENTS_HDR(current, mark_stack_top, mark_stack_limit, source, hhdr, do_offset_check) { \    //计算内存节点位于的hblk内存块地址的偏移值    size_t displ = HBLKDISPL(current); \    size_t gran_displ = BYTES_TO_GRANULES(displ); \    ...    //将header中对应的标记位设置为1，表示这个节点被“引用”    SET_MARK_BIT_EXIT_IF_SET(hhdr, gran_displ); \    ...    //增加引用个数hb_n_marks    INCR_MARKS(hhdr); \    ...    //将内存节点存入临时数组中    PUSH_OBJ(base, hhdr, mark_stack_top, mark_stack_limit); \    ...}</code></pre><p>**至此，我们确定了该值是一个内存地址，所以需要计算该内存地址引用的是hblk中的哪一个内存结点，以在对应的hblkhdr中做标记。**例如hblk内存块的地址是0x122c86900，hb_sz&#x3D;16（节点大小16字节），内存节点A的地址是0x122c86920，则偏移32字节，Header-&gt;hb_marks[1]存储A的“引用”状态标记位的值0或1。如果内存地址对应是节点A的地址，则先调用调用SET_MARK_BIT_EXIT_IF_SET，将对应的hb_marks[1]和hb_marks[2]标记为1，<strong>代表该内存节点正在被引用</strong>；再调用INCR_MARKS方法将hb_n_marks加一，<strong>表示这个hblk内存块中被“引用”的节点总数加一</strong>。如图所示。蓝色表示被“引用”的2个内存节点，</p><p><img src="https://img.aixheee.top/8769776a-f971-44c9-b8d4-d30e0492452e.png"></p><p>最后，再调用PUSH_OBJ，<strong>将“引用”的内存结点的地址加入到一个临时数组mark stack中，为接下来针对“托管堆内存节点”递归扫描做准备</strong></p></li></ul><br><h2 id="Reclaim阶段"><a href="#Reclaim阶段" class="headerlink" title="Reclaim阶段"></a>Reclaim阶段</h2><pre><code>STATIC void GC_finish_collection(void){    ...    //检测内存泄漏逻辑    if (GC_find_leak) {        for (kind = 0; kind &lt; GC_n_kinds; kind++) {            for (size = 1; size &lt;= MAXOBJGRANULES; size++) {                q = (ptr_t)GC_obj_kinds[kind].ok_freelist[size];                if (q != NULL)                GC_set_fl_marks(q);            }        }    //不实际回收    GC_start_reclaim(TRUE);    }    ...    //清空ok_freelist链表    for (kind = 0; kind &lt; GC_n_kinds; kind++) {        for (size = 1; size &lt;= MAXOBJGRANULES; size++) {            q = (ptr_t)GC_obj_kinds[kind].ok_freelist[size];            if (q != NULL)            GC_clear_fl_marks(q);        }    }    //开始回收    GC_start_reclaim(FALSE);    ...}</code></pre><p>主要的GC逻辑集中于GC_start_reclaim：</p><pre><code>GC_INNER void GC_start_reclaim(GC_bool report_if_found){    for (kind = 0; kind &lt; GC_n_kinds; kind++) {        struct hblk ** rlist = GC_obj_kinds[kind].ok_reclaim_list;        ...        //清空ok_freelist和ok_reclaim_list        void **lim = &amp;(GC_obj_kinds[kind].ok_freelist[MAXOBJGRANULES+1]);        for (fop = GC_obj_kinds[kind].ok_freelist; (word)fop &lt; (word)lim; (*(word **)&amp;fop)++) {            ...            GC_clear_fl_links(fop);        }        ...        BZERO(rlist, (MAXOBJGRANULES + 1) * sizeof(void *));    }    ...    //加入ok_reclaim_list链表    GC_apply_to_all_blocks(GC_reclaim_block, (word)report_if_found);    ...    //回收内存    GC_reclaim_all((GC_stop_func)0, FALSE);    ...}</code></pre><ul><li>首先通过GC_clear_fl_links<strong>将 ok_freelist 中的节点全部清空</strong>，为后续新的free对象加入到ok_freelist中腾出空间。同时，将ok_reclaim_list清空，为佳能free对象回收到其中做好准备</li><li>随后，通过GC_apply_to_all_blocks遍历之前提到的bottomIndex一维链表，其中存储了指向hblkhdr的指针。<strong>其针对每个hblk，调用GC_reclaim_block来预处理要回收的内存块——大对象直接回收，小对象则暂时加入到ok_reclaim_list中，并通过GC_reclaim_all实际回收，将其加入到ok_freelist下</strong></li><li>最后，会遍历GC_hblkfreelist以尝试将hblk归还给操作系统。是否归还取决于该hblk有多久没有被使用过了，<strong>如果经过了6次GC都没有被分配出去过，那么Boehm GC会将该hblk对应的物理内存归还给操作系统</strong>，但其虚拟地址会被保留，日后再次申请这块内存时，hblk地址不变</li></ul><p><mark>GC_reclaim_block：</mark></p><pre><code>STATIC void GC_reclaim_block(struct hblk *hbp, word report_if_found){    hdr * hhdr = HDR(hbp);    word sz = hhdr -&gt; hb_sz; /* size of objects in current block */    struct obj_kind * ok = &amp;GC_obj_kinds[hhdr -&gt; hb_obj_kind];    if( sz &gt; MAXOBJBYTES ) { /* 1 big object */    if( !mark_bit_from_hdr(hhdr, 0) ) {        ...        //直接释放hbp到GC_hblkfreelist链表中        GC_freehblk(hbp);    }    else {        ...        GC_bool empty = GC_block_empty(hhdr);        if (empty) {            GC_freehblk(hbp);        } else if (!GC_block_nearly_full(hhdr)) {            struct hblk **rlh = ok -&gt; ok_reclaim_list;            if (rlh != NULL) {                rlh += BYTES_TO_GRANULES(sz);                hhdr -&gt; hb_next = *rlh;                *rlh = hbp;            }        }    }    ...}</code></pre><p><strong>对于大对象</strong>：大对象较为特殊，一个内存节点直接由1-N个hblk内存块构成，不像小对象，一个hblk会等分为多个小内存节点供上层使用。所以，如果通过hblkhdr检查出该hblk未被引用，则<strong>直接通过GC_freehblk方法，将其加入到GC_hblkfreelist中，同时把内存中的数据清空，以供下次大内存分配使用</strong></p><blockquote><p>需要注意，GC_freehblk会尝试进行内存块的合并：如果要归还的hblk前后的hblk是空闲的，则会进行合并，再归还给GC_hblkfreelist</p></blockquote><p>对于小对象需要分类讨论：</p><ul><li><p>如果整个hblk为empty，即hblk中没有任何一个节点被引用，则直接通过GC_freehblk将整个hblk归还给GC_hblkfreelist，并清空数据</p></li><li><p>通过GC_block_nearly_full方法判断，当前hblk中是否大多数节点被引用（如果当前heap block中被标记的object数量 &gt; (7&#x2F;8) * heap block能容纳的总object数量，则认定为大多数被引用）。如果不是，即<strong>大多数内存结点都没有被引用，则将该hblk加入到ok_reclaim_list中</strong>，其在大小上的结构和ok_freelist一致</p></li><li><p>如果当前hblk中大多数内存节点被引用，则不加入到ok_reclaim_list中，什么也不做</p></li></ul><p><strong>GC_reclaim_all：真正将hblk内存块回收。</strong></p><p>遍历所有的ok_reclaim_list链表，取出hblk内存块遍历hblk内存中的每个内存节点，如果mark_bit_from_hdr为1，表示被标记“引用”，不需要回收，否则将其加入list中，即ok_freelist链表。如图所示：</p><p><img src="https://img.aixheee.top/7c4b9804-6bff-4339-9694-fd79d50e2b6e.png"></p><p>至此，垃圾回收全部完成。<strong>梳理一下非增量BoehmGC的整体流程：</strong></p><ul><li><p>清空所有标记位</p></li><li><p>暂停用户线程（stop-the-word）</p></li><li><p>【Mark】从root触发做可达性分析。将uncollectable对象加入mark stack，并扫描根节点内存，判定值是否是指针，如果是则加入mark stack并打上标记</p></li><li><p>【Mark】对mark stack中的托管堆对象进行递归式地扫描并标记</p></li><li><p>【Sweep】恢复线程执行，遍历所有的堆对象（bottomIndex全局双向链表），对于小对象，如果其所在的hblk整体空闲，则回收到GC_hblkfreelist中，如果hblk大部分空闲，则回收到ok_freelist中，否则不做处理；对于大对象则直接回收到GC_hblkfreelist。如果某个GC_hblkfreelist中的内存块连续六次GC都未被使用，则物理内存被回收</p></li></ul><br><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>本文详细拆解了Unity GC的完整工作流程，从触发条件到Mark阶段的根节点扫描与递归标记，再到Reclaim阶段的分级回收策略。我们可以看到，Boehm GC作为一款保守式GC，虽然在功能复杂度上不及.NET的精确式GC，但通过精心设计的数据结构与分配策略，在跨平台兼容性与实现简洁性之间找到了平衡。<br>在下一篇文章中，我们将进一步探讨增量式GC的工作原理——Unity如何将原本一次性的标记阶段分摊到多帧执行，从而将长时间的STW停顿转化为多次短暂的微停顿，大幅改善游戏的帧率稳定性。</p>]]></content>
      
      
      <categories>
          
          <category> 内存分配与管理 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>【内存分配与管理】Unity 增量式GC</title>
      <link href="/posts/UnityGC_2.html"/>
      <url>/posts/UnityGC_2.html</url>
      
        <content type="html"><![CDATA[<p>在上一篇文章中，我们深入剖析了 Unity 传统 Boehm GC 的完整工作机制——从触发条件、STW（Stop-The-World）线程暂停，到 Mark 阶段的递归可达性分析，再到 Sweep 阶段的分级回收策略。我们看到了保守式 GC 在设计与实现上的诸多权衡：无法整理内存、无法精确区分指针与数据、依赖全局扫描来完成回收。这些特性使得传统 GC 在一次回收中往往带来不可忽视的卡顿，尤其是在托管堆较大、对象存活率较高的游戏场景下。</p><p>然而，游戏对帧率稳定性的要求极为苛刻。一次 50ms 甚至 100ms 的主线程停顿，足以让玩家明显感知到“掉帧”或“卡一下”。为了解决这个问题，Unity 引入了增量式 GC（Incremental GC）。它并没有改变 Boehm GC 的基本算法，而是通过一种精巧的工程手段：将原本一次性完成的 Mark 阶段拆分成多个短时间的增量步骤，穿插在多帧中执行，从而把一次长时间的 STW 卡顿，转化为多次几乎不可感知的微停顿。</p><p>增量式 GC 的核心挑战在于：如何在 GC 与用户线程并发运行的前提下，仍然保证标记结果的正确性？​ 答案就是本文要重点讲解的 Write Barrier（写屏障）机制，以及与之配套的 dirty page 跟踪、状态机驱动的增量标记流程</p><h1 id="WriteBarrier"><a href="#WriteBarrier" class="headerlink" title="WriteBarrier"></a>WriteBarrier</h1><pre><code>// 赋值field1生成的il2cpp代码    __this-&gt;___field1 = (RuntimeObject*)L_4;    Il2CppCodeGenWriteBarrier((void**)(&amp;__this-&gt;___field1), (void*)(RuntimeObject*)L_4); // 调用WriteBarriervoid Il2CppCodeGenWriteBarrier(void** targetAddress, void* object){    il2cpp::gc::GarbageCollector::SetWriteBarrier(targetAddress);}il2cpp::gc::GarbageCollector::SetWriteBarrier(void **ptr){    GC_END_STUBBORN_CHANGE(ptr);}GC_API void GC_CALL GC_end_stubborn_change(const void *p){    GC_dirty(p); /* entire object */}</code></pre><p>相较于普通GC，增量式GC引入了<strong>Write Barrier写屏障</strong>，在每次修改引用字段所引用的对象时，都会对该字段执行WriteBarrier代码，<strong>将被修改引用字段的对象所对应的hblk的dirty位设置为1，修改位置位于一个单独的GC_dirty_pages数组中，从而告诉增量GC——这个对象所在的内存区域的指针指向可能发生变化了，需要重新扫描</strong></p><p><strong>为什么需要依赖于WriteBarrier？</strong></p><p><strong>因为增量式GC和用户线程是并发的，GC期间用户线程依然有可能修改对象间的引用关系，而WriteBarrier负责记录”发生的引用变化“，从而保证Mark阶段扫描时，可对先前的扫描结果进行”更正补救“以符合当前最新的引用关系</strong></p><p>而之所以普通GC&#x2F;full GC不需要依赖于WriteBarrier，是因为二者不会和用户线程并发执行，而是完全暂停用户线程后才进行mark-and-sweep，其扫描对象时获取的引用关系是实时最新的</p><h1 id="触发回收"><a href="#触发回收" class="headerlink" title="触发回收"></a>触发回收</h1><p>在普通GC下，对于小对象的内存分配，只有在ok_freelist和GC_hblkfreelist中都没有空闲内存块的情况下，才有可能触发GC；而<strong>在增量GC下，只要ok_freelist中没有空闲内存块，则有可能直接触发GC，不需要管GC_hblkfreelist，而对于大内存的分配，每次都有可能触发GC。</strong></p><p>具体来说，增量模式下判断是否触发GC的函数为GC_maybe_gc：</p><pre><code>STATIC void GC_maybe_gc(void){    if (GC_should_collect())     {        static int n_partial_gcs = 0;        if (!GC_incremental)         {            ...        }        else         {            if (GC_need_full_gc || n_partial_gcs &gt;= GC_full_freq)             {                GC_COND_LOG_PRINTF(                    &quot;***&gt;Full mark for collection #%lu after %lu allocd bytes\n&quot;,                    (unsigned long)GC_gc_no + 1, (unsigned long)GC_bytes_allocd);                GC_promote_black_lists();                (void)GC_reclaim_all((GC_stop_func)0, TRUE);                GC_notify_full_gc();                GC_clear_marks();                n_partial_gcs = 0;                GC_is_full_gc = TRUE;            }            else             {                n_partial_gcs++;            }        }        /* We try to mark with the world stopped.       */        /* If we run out of time, this turns into       */        /* incremental marking.                         */        if (GC_stopped_mark(GC_time_limit == GC_TIME_UNLIMITED ?            GC_never_stop_func : GC_timeout_stop_func))         {            GC_finish_collection();        }        else         {            if (!GC_is_full_gc)             {                /* Count this as the first attempt */                GC_n_attempts++;            }        }    }}</code></pre><p>其主要逻辑同样是普通GC下调用的GC_should_collect，但是参数值略有不同：普通GC下其中的一个判断条件是——如果“自从上次GC到现在分配的内存”超过一个阈值，则触发GC，而在增量GC下，该阈值变为了原先的一半</p><p>这意味着，触发增量GC的条件更加宽松了</p><h1 id="状态转换"><a href="#状态转换" class="headerlink" title="状态转换"></a>状态转换</h1><pre><code>STATIC GC_bool GC_stopped_mark(GC_stop_func stop_func){    ...    STOP_WORLD();    ...    GC_initiate_gc();    unsigned i;    for (i = 0;; i++)     {        if ((*stop_func)())         {            GC_COND_LOG_PRINTF(&quot;Abandoned stopped marking after&quot;                &quot; %u iterations\n&quot;, i);            GC_deficit = i;     /* Give the mutator a chance.   */            START_WORLD();            return(FALSE);        }        if (GC_mark_some(GC_approx_sp())) break;    }    ...}</code></pre><p>在确定了需要触发GC后，调用GC_stopped_mark会走以下流程：</p><ul><li><p>不会进行标记位清除：增量GC不会像普通GC一样在线程暂停前将hblk中所有的mark标志清空。</p></li><li><p>线程暂停：在增量GC下，单次GC的最长时间为3ms，如果耗时超过3ms，则会先停止回收，并让用户线程继续运行</p></li><li><p>收集dirty信息：调用GC_initiate_gc()来获取到自从上一次GC以来到现在的dirty标志并在本次GC中使用，以防在整个GC期间引用关系被修改，GC_dirty_pages再次发生变化。具体来说，GC_initiate_gc()会将GC_dirty_pages数组复制到一个相同规格的GC_grungy_pages数组中，并将GC_dirty_pages清空</p></li><li><p>进入GC_mark_some状态机，进行本次增量式GC的核心阶段</p></li></ul><h2 id="MS-PUSH-RESCUERS"><a href="#MS-PUSH-RESCUERS" class="headerlink" title="MS_PUSH_RESCUERS"></a>MS_PUSH_RESCUERS</h2><p>在收集完成dirty信息后，状态由MS_NONE变为MS_PUSH_RESCUERS：</p><p><strong>MS_PUSH_RESCUERS阶段的主要任务是”补救“，处理两次增量式GC之间用户线程发生的新变化，即处理dirty hblk和重新扫描root</strong></p><pre><code>STATIC GC_bool GC_mark_some_inner(ptr_t cold_gc_frame){    switch(GC_mark_state) {        case MS_NONE:            break;        case MS_PUSH_RESCUERS:            if ((word)GC_mark_stack_top                &gt;= (word)(GC_mark_stack_limit - INITIAL_MARK_STACK_SIZE/2))             {                /* Go ahead and mark, even though that might cause us to */                /* see more marked dirty objects later on.  Avoid this   */                /* in the future.                                        */                GC_mark_stack_too_small = TRUE;                MARK_FROM_MARK_STACK();                break;            }             else             {                scan_ptr = GC_push_next_marked_dirty(scan_ptr);                if (scan_ptr == 0)                 {#if !defined(GC_DISABLE_INCREMENTAL)                    GC_COND_LOG_PRINTF(&quot;Marked from %lu dirty pages\n&quot;,                                       (unsigned long)GC_n_rescuing_pages);#endif                    GC_push_roots(FALSE, cold_gc_frame); //扫描root                    GC_objects_are_marked = TRUE;                    if (GC_mark_state != MS_INVALID)                     {                        GC_mark_state = MS_ROOTS_PUSHED;                    }                }            }            break;        ...</code></pre><p>对于dirty hblk的处理逻辑在GC_push_next_marked_dirty中：</p><pre><code>/* mark only from dirty pages   */STATIC struct hblk* GC_push_next_marked_dirty(struct hblk* h){    hdr* hhdr = HDR(h);    for (;;)     {        if (EXPECT(IS_FORWARDING_ADDR_OR_NIL(hhdr)            || HBLK_IS_FREE(hhdr), FALSE))        {            h = GC_next_used_block(h);            if (h == 0) return(0);            hhdr = GC_find_header((ptr_t)h);        }        if (GC_block_was_dirty(h, hhdr))            break;        h += OBJ_SZ_TO_BLOCKS(hhdr-&gt;hb_sz);         hhdr = HDR(h);    }    GC_push_marked(h, hhdr);    return(h + OBJ_SZ_TO_BLOCKS(hhdr-&gt;hb_sz));}</code></pre><p>为了处理所有的dirty hblk，会从heap的起始地址开始遍历，对于大对象来说，一次循环会跳过其包含的多个hblk，对于小对象来说，一次循环会跳过当前的hblk。在整个heap遍历完成，所有的dirty hblk都被扫描完成后退出循环，并开始扫描root，扫描方式和普通GC一样</p><p>在遍历过程中，对于小对象会检查当前object所属的hblk是否是dirty，如果检测到有hblk为dirty，则通过GC_push_marked重新扫描当前hblk；对于大对象，则会检查当前object中是否有任意一个hblk为dirty，如果检测到有hblk为dirty，则通过GC_push_marked重新扫描对象下的所有hblk</p><p><strong>GC_push_marked中，会扫描hblk（大对象为object），其扫描、判断值是否为指针的逻辑和正常GC一致，对于认定为指针的值，会将其指向的对象的mark标记位置为1，并将该指针压入到mark stack中（注意，被压入到mark stack中的是”一个需要被继续扫描其内部指针的对象的地址“），等待下一阶段进行递归式扫描标记</strong></p><blockquote><p>注意区分“遍历”和“扫描”：遍历只是按照步长判断堆中的hblk是否是dirty的；而扫描指的是对dirty对象本身进行扫描，即判断其对应的内存中是否有疑似指针的值，二者的成本不同，后者成本高于前者</p></blockquote><p><strong>可能存在的问题：</strong></p><p><strong>为什么步长还是object，而不是hblk（4KB）？</strong></p><p>个人猜测：选择object为步长，可以保证每个object只会被扫描一次，如果按照hblk4KB大小来遍历，虽说不会出现明显问题，但是对于占据了多个hblk的大对象来说，会导致该对象被多次扫描并加入到mark stack中，而扫描标记是整个GC中最昂贵的部分，带来了额外的性能消耗</p><br><p><strong>明明有全局的GC_dirty_pages位图结构，记录了到底哪个hblk为dirty，为什么还是需要遍历整个堆？</strong></p><p>进一步的，完全可以根据bitmap中为dirty的序号，反推出是哪个hblk为脏，还能顺便通过全局__top_index定位到header，不需要遍历整个堆呀？确实是这样的，JVM的GC做法和这个思路类似，不会扫整个heap，而是根据set扫描对应的区域,具体原因尚不清楚</p><br><p><strong>对于小对象来说，实际上只有某个对象是dirty的，但是dirty标记的粒度为hblk，所以不得不把整个hblk全扫描一遍，这合理吗？</strong></p><p>这是一个工程上的权衡，其保证了在WriteBarrier时速度较快，牺牲了GC时的扫描速度。并且对于GC_dirty_pages来说，其没法确定object有多少个、大小是多少，这是运行时决定的，所以没法实现WriteBarrier时以object为粒度进行标记</p><br><p><strong>为什么还需要扫描root？</strong></p><pre><code>void newChange(){    GameObject obj = new GameObejct();    obj.field = otherObj;    //....    func(obj);  //出错！obj已经被回收}</code></pre><p>比如，在两次GC期间创建了新的对象obj，如果不重新扫描root，会导致该新对象没有被mark，进而导致该obj被错误地回收，后续调用出错</p><br><p><strong>看起来增量式GC的工作量更大了，每一次GC都要扫描dirty对象、扫描root、递归地扫描heap中被引用的对象，这不是效率比普通GC更低了吗？</strong></p><p><strong>增量式GC的总体工作量确实略高于普通GC，因为他需要额外处理每次GC间隔期间产生的dirty对象并且重新扫描root。但是，增量GC的核心优化在于将最昂贵的”从root出发递归扫描所有可达对象“拆分为了多次执行，从而将大规模的Mark操作摊平到了多个时间片中完成</strong></p><p>具体来说，每次GC都会进入MS_PUSH_RESCUERS阶段，对dirty hblk重新扫描，并对root重新扫描，用于修复在上次GC到此次GC发生的新变化，比如创建了新变量、变更了引用关系等，其会将相应的新对象的地址push到mark stack中并等待扫描。<strong>实际上，这一阶段的工作量通常很小，因为需要扫描的对象并不多，其是增量式GC相较于普通GC的额外开销。</strong></p><p>随后，会递归式地扫描标记mark stack，这才是真正耗时的工作，但是该工作会被均摊到多次增量式GC中完成，每次增量式GC的结果都保存在了mark stack中，达到上限时间后会重新启动用户线程，暂停GC，并等待下一轮增量式GC再继续扫描mark stack，从而避免了突然暂停用户线程较长时间</p><br><h2 id="full-GC"><a href="#full-GC" class="headerlink" title="full GC"></a>full GC</h2><p>可以看到，在GC_stopped_mark之前实际上还有一层full GC的判断逻辑</p><pre><code>STATIC void GC_maybe_gc(void){    if (GC_should_collect())     {        static int n_partial_gcs = 0;        if (!GC_incremental)         {            ...        }        else         {            if (GC_need_full_gc || n_partial_gcs &gt;= GC_full_freq)             {                GC_COND_LOG_PRINTF(                    &quot;***&gt;Full mark for collection #%lu after %lu allocd bytes\n&quot;,                    (unsigned long)GC_gc_no + 1, (unsigned long)GC_bytes_allocd);                GC_promote_black_lists();                (void)GC_reclaim_all((GC_stop_func)0, TRUE);                GC_notify_full_gc();                GC_clear_marks();                n_partial_gcs = 0;                GC_is_full_gc = TRUE;            }            else             {                n_partial_gcs++;            }        }        /* We try to mark with the world stopped.       */        /* If we run out of time, this turns into       */        /* incremental marking.                         */        if (GC_stopped_mark(GC_time_limit == GC_TIME_UNLIMITED ?            GC_never_stop_func : GC_timeout_stop_func))         {            GC_finish_collection();        }        else         {            if (!GC_is_full_gc)             {                /* Count this as the first attempt */                GC_n_attempts++;            }        }    }}</code></pre><p><strong>什么时候触发full GC？为什么？</strong></p><ul><li><p>周期性强制full GC，即n_partial_gcs &gt;&#x3D; GC_full_freq，意思是没进行N次增量式GC，就强制进行一次full GC，N为1。原因：在两次增量GC期间，如果用户线程对象创建或修改引用关系非常频繁，那么会导致mark stack中的增长速度快于每次GC扫描的处理速度，进而导致增量GC永远处理不完，所以需要full GC来进行一次全量标记</p></li><li><p>如果内存增长过多，即GC_need_full_gc（会在每次增量式GC后判断，从上一次回收以来，如果新投入使用的hblk大于当前objects大小除以2，则为true），那么也会触发full GC。同样的原因，如果有增量回收的内存满足不了内存分配的趋势，则同样会触发一次Full GC来进行全量标记，进而将内存回收</p></li></ul><p><strong>full GC会做什么？</strong></p><p>full GC会进行一次完整的普通GC。首先会对之前mark stack中的对象进行扫描，完成收尾工作，然后按照类似于普通GC的逻辑，将所有的用户线程暂停，将mark bit清空，扫描root并递归式地扫描标记所有被引用的对象，此时才允许启动用户线程，后续再通过后台线程进行回收工作</p><br><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>通过本篇对增量式GC的拆解，我们可以清晰地看到这种权衡的全貌：</p><p>Write Barrier​ 是增量并发的基石。它通过在引用变更时记录dirty状态，解决了“GC扫描时用户线程修改引用”这一经典难题，代价是每次赋值多了一层函数调用的开销。</p><p>Dirty Page 重扫描​ 是增量正确性的保障。它确保了两次GC间隔中新创建的对象或变更的引用不会被遗漏，即便这意味着需要遍历堆中的标记位。</p><p>Full GC 的兜底​ 揭示了工程的现实。无论增量机制多么精巧，当内存增长速度超过回收速度，或者碎片化严重时，一次彻底的全量暂停仍是不可避免的终极手段。</p><p>增量式 GC 并未改变 Boehm GC 的保守式本质，而是通过 Write Barrier 与状态机驱动的增量标记，将一次不可避免的“大停顿”平滑化为多次可控的“小停顿”。这是 Unity 在跨平台兼容性与实时性要求之间，做出的一项极具工程智慧的折中设计。</p>]]></content>
      
      
      <categories>
          
          <category> 内存分配与管理 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>【内存分配与管理】Unity内存分配</title>
      <link href="/posts/UnityMemory.html"/>
      <url>/posts/UnityMemory.html</url>
      
        <content type="html"><![CDATA[<p>本文作为Unity内存管理系列的开篇，将聚焦于Unity的内存分配原理，深入剖析Boehm GC在托管内存分配中的具体实现，包括小对象与大对象的分配策略、空闲链表与内存池的设计、以及这些机制背后的性能考量。后续文章将进一步讲解Unity GC的回收机制与优化实践。</p><br><h1 id="整体认识"><a href="#整体认识" class="headerlink" title="整体认识"></a>整体认识</h1><p>Unity具有两套内存——托管内存Managed Memory和原生内存Native Memory。</p><p>Managed Memory即C#层的堆内存，其中存储了C#代码分配的对象，即class实例对象、string、装箱等，由C#运行时（Mono&#x2F;iL2CPP）管理</p><p>Native Memory即Unity底层的C++层的堆内存，其中存储了Unity引擎内部的资源，比如纹理，模型，音频以及GameObject本体等，需要我们手动进行资源的卸载以及引擎内部的生命周期管理</p><pre><code>┌─────────────────────────────────────────────────────────────┐│                    Operating System                         ││                   (物理内存 RAM)                             │└───────────────────────┬─────────────────────────────────────┘                        │        ┌───────────────┴───────────────┐        ↓                               ↓┌──────────────────────┐    ┌──────────────────────┐│   Managed Memory     │    │   Native Memory      ││   (C# Heap)          │    │   (C++ Heap)         │├──────────────────────┤    ├──────────────────────┤│                      │    │                      ││  ┌────────────────┐ │    │  ┌────────────────┐ ││  │ C# Objects     │ │    │  │ Unity Engine   │ ││  │ - List&lt;T&gt;      │ │    │  │ - GameObject   │ ││  │ - Dictionary   │ │    │  │ - Transform    │ ││  │ - Custom Class │ │    │  │ - Renderer     │ ││  │ - String       │ │    │  └────────────────┘ ││  └────────────────┘ │    │                      ││                      │    │  ┌────────────────┐ ││  ┌────────────────┐ │    │  │ Assets         │ ││  │ MonoBehaviour  │◄┼────┼─►│ - Texture      │ ││  │ (C# Wrapper)   │ │    │  │ - Mesh         │ ││  └────────────────┘ │    │  │ - AudioClip    │ ││                      │    │  │ - Material     │ ││  GC 管理:            │    │  │ - AssetBundle  │ ││  - Incremental GC   │    │  └────────────────┘ ││  - Boehm GC         │    │                      ││  (Mono/IL2CPP)      │    │  手动管理:           ││                      │    │  - Destroy()        │└──────────────────────┘    │  - Unload()         │                            │  - DestroyImmediate()│                            └──────────────────────┘       ↑                               ↑       │                               │    C# 代码可见                     部分可见  (Profiler: Mono 内存)        (Profiler: Unity 内存)</code></pre><p>【托管内存和原生内存之间如何交互？】托管内存和原生内存之间并不是完全隔离的，二者需要经常交互。比如我们通过C#代码访问Unity资源或者通过Unity API调用时，既会在原生内存上创建一个Unity Object，代表场景中真正的GameObject对象或是Component（Transform、Rigidbody等），也会在托管内存上创建一个“包装器”对象（比如GameObject对象），其中包含了一个指向Unity Object的指针，作为我们访问Unity Object的”句柄“</p><pre><code>GameObject go = new GameObject();</code></pre><blockquote><p>这带来了一个重要的现象：假null问题。尽管Destroy了，但是托管内存上的C#对象还在，可是C++层对象已经被销毁了，如果只是通过C#的&#x3D;&#x3D;来判断对象是否为空，会得到误导性的结果。</p><p>为此，Unity重载了&#x3D;&#x3D;运算符，如果原生内存上的对象已经销毁了，那么就返回null，尽管C#层对象还没被回收</p><pre><code>Destroy(go);if (go == null) { ... } // true</code></pre></blockquote><br><h1 id="内存分配管理"><a href="#内存分配管理" class="headerlink" title="内存分配管理"></a>内存分配管理</h1><p>Unity中使用C#创建托管对象时，这一步最终会通过调用到底层的<strong>GC内存分配器来申请原始内存</strong>，而这一步是在C&#x2F;C++层完成的，随后<strong>运行时会在这块内村上构造托管对象</strong>，比如调用构造函数，写入对象头等。</p><p>其大体思路和STL allocator类似，对于2048字节以内小对象，则通过free-list+memory-pool的方式分配；对于2048字节以上的大对象，则直接从memory-pool中分配内存</p><p><img src="https://img.aixheee.top/8fc4bc29-d96d-4375-a63c-a5700493e773.png"></p><br><h2 id="小对象内存分配"><a href="#小对象内存分配" class="headerlink" title="小对象内存分配"></a>小对象内存分配</h2><p>关键数据结构：</p><ul><li><p>内存块 hblk(heap block)：即实际存储数据的内存块，其大小为4KB，即一个page的大小。</p><pre><code>struct hblk {    char hb_body[HBLKSIZE]; //HBLKSIZE=4096};</code></pre><p>一个hblk会被等大分割并插入到 ok_freelist 中，而内存块是直接从 ok_freelist中获得的，所以hblk中实际上会存放很多个”等大对象“。比如：</p><pre><code>hblk (4KB) ├── obj (32B) ├── obj (32B) ├── obj (32B) ...</code></pre></li><li><p>header信息 hblkhdr：hblkhdr用于存储hblk的信息，对于存储小对象的hblk来说，每个hblk都有唯一的hblkhdr；对于大对象来说，即一个大对象对应多个hblk的情况下，只有大对象的第一个hblk才有hblkhdr，但是借助_top_index实现地址-&gt;hblkhdr的映射，从而找到hblkhdr。hblkhdr<strong>其以某种hash算法存在于全局存放堆信息的数组-链表结构_top_index中，GC能够正常运行依赖于该header信息。</strong><br><strong>如果是小对象</strong>，hb_sz记录了对应的hblk中每个小对象的大小，该hblk中存储的小对象数量可以通过 4KB&#x2F;hb_sz得出。hb_marks标记出当前hblk中的对象的引用情况——0代表没有被引用，1代表被引用。比如，hblk为8KB，hb_sz为32B，那么对象数量为256，则hb_marks就需要256个bit，第i位对应第i个对象，标记其被引用情况。<br><strong>如果是大对象</strong>，则hb_sz记录了当前hblk所属的大对象的大小，且hb_marks[0]代表了该对象是否被引用。该对象占用的hblk数量可以通过hb_sz &#x2F; 4KB得出</p><pre><code>//头部信息struct hblkhdr {    struct hblk * hb_next; //指向下一个hblk    struct hblk * hb_prev; //指向上一个hblk    struct hblk * hb_block; //对应的hblk    unsigned char hb_obj_kind; //kink类型    unsigned char hb_flags; //标记位    word hb_sz;    word hb_descr;     size_t hb_n_marks;//标记位个数，用于GC    word hb_marks[MARK_BITS_SZ]; //标记为，用于GC}</code></pre><p><img src="https://img.aixheee.top/4fbc094e-6b65-4f37-878b-6ba7ea4fdf47.png"></p></li><li><p>GC_obj_kinds[3]：在分配内存时，将要分配的object分为三种内存类型——PTRFREE、NORMAL和UNCOLLECTABLE。PTRFREE指的是，内部不包含指针的类型，扫描时可以直接跳过；NORMAL是内部可能包含指针类型，需要扫描；UNCOLLECTABLE则是无法被回收的内存，在垃圾回收时会作为根，比如静态变量和GCHanlde都属于UNCOLLECTABLE对象</p><pre><code>struct obj_kind {    void **ok_freelist;    struct hblk **ok_reclaim_list;    ...} GC_obj_kinds[3];</code></pre></li><li><p><strong>空闲链表 ok_freeList</strong>：ok_freeList是一个二级指针链表，对应维护了128个存储空闲内存块的链表，每条链表记录的内存块大小相同，不同链表内的内存块大小不同，相互间隔16字节。每一条链表中的空闲内存块是通过hblk等分填充的，每个空闲内存块中都包含了下一个空闲块的地址（小块只有在空闲时才会在内部存储下一个空闲小块的地址, 返回给用户时就是纯数据块了）</p><p><img src="https://img.aixheee.top/5f3d7638-6555-4db8-9874-5d3cc382bb9f.png"></p></li><li><p>内存池空闲内存块链表 GC_hblkfreelist：类似于上面的 ok_freelist，但是在细节上有所不同。首先，在存储结构上，其是一个存储hblk地址的数组，<strong>hblk之间并不直接相连，而是通过hblkhdr间接地在逻辑上相连</strong>。遍历一条“链表”时，需要通过hblk的地址，查找到hblkhdr，再通过hblkhdr中的next指针找到当前“链表”的下一个hblk</p><pre><code>hblk* p = GC_hblkfreelist[i];while (p) {    hblkhdr* hdr = HDR(p);   // 通过地址查 header    p = hdr-&gt;hb_next;        // 拿到下一个 hblk}</code></pre><p>其次在大小上，每个内存块都以4KB为基本单位（而不是16B了），且从GC_hblkfreelist[32]开始，每个GC_hblkfreelist中存储的内存块大小不等，会有8种大小类型。例如GC_hblkfreelist[32]存储的内存块大小范围是128KB～156KB。</p><p><img src="https://img.aixheee.top/031a18e6-c3da-4ca0-8d23-c2ad974384f9.png"></p></li></ul><br><p>内存块分配获取流程：</p><p>根据目标内存块大小，首先对齐到16字节大小（但是如果刚好能被16整除，则再追加16字节）<strong>尝试从ok_freelist中获取到空闲的内存块</strong>——如果其中有空闲的内存块，则直接返回，流程结束；如果没有空闲的内存块，则调用尝试从底层的内存池的 GC_hblkfreelist 中获取内存块并填充ok_freelist（类似于allocator的refill()）</p><ul><li><p>如果GC_hblkfreelist中压根不存在可以被利用的内存块（为空或者太小）：则根据通过对应平台下分配内存的接口（比如malloc）来分配内存，或触发GC。有多余的内存后，将内存块加入到相应的GC_hblkfreelist中，并将其header信息初始化，加入到全局存放堆信息的数组中</p></li><li><p>如果GC_hblkfreelist中存在可被利用的内存块，则调用<strong>GC_allochblk()</strong>：根据所需字节数，<strong>对齐到4KB大小，以批量填充 ok_freelist</strong>，并根据该大小，计算出需要定位到哪一条GC_hblkfreelist中查找合适大小的内存块。<strong>如果经过查找，该GC_hblkfreelist中不存在合适的内存块，则从包含更大内存块的GC_hblkfreelist中寻找内存块</strong>具体的查找流程GC_allochblk_nth()：</p><ul><li><p>如果遇到了恰好符合要求的内存块大小，则直接return，填充ok_freeList。如图，假设从GC_hblkfreelist中获取到了一个4KB的内存块，且先前的目标是填充16字节的ok_freelist，则将4096字节按照16字节分割并进行填充</p><p><img src="https://img.aixheee.top/38de14bb-b45c-44a8-a6c1-07828100f4c3.png"></p></li><li><p>如果没有可以精准匹配的内存块，但是遇到了一个稍大的内存块，则将其<mark>分割</mark>并返回较为精确的一部分并填充ok_freeList，同时将剩余部分重新挂回到GC_hblkfreelist中，修改被分割的内存块的header信息。比如该图中，目标是要4KB大小的内存块，但是只有8KB的了，所以会将8KB大小的内存块分割</p><p><img src="https://img.aixheee.top/d4953009-8d7b-42a5-84f9-95f300a73612.png"></p></li></ul></li></ul><br><p>【设计原理思考】</p><p><strong>为什么对于小对象，需要通过ok_freelist来管理，而对于大对象则不需要，直接从GC_hblkfreelist取内存块？</strong></p><p>实际上，STL allocator、tcmalloc和jemalloc使用的都是这套策略</p><p>小对象的内存分配属于热路径，需要让多个对象共享一个hblk，在hblk内部做更细粒度的”对象级“复用，<strong>减少小对象带来的内存碎片问题</strong>（主要是内部），同时可以提高小对象的分配效率，<strong>做热路径优化</strong>（程序中大部分的内存分配需求都是小对象，而ok_freelist中获取为O(1)，其严格按照size class划分，但在GC_hblkfreelist中就不是O(1)了）</p><p>大对象的数量较少，一般都是可以直接对应一个或多个hblk，可以直接使用GC_hblkfreelist按照hblk做”内存块级别的“管理”，<strong>避免不必要的复杂度</strong>。对于大对象来说，主要需要解决的问题是外部碎片</p><p>进一步的，之所以一个hblk为4KB，是因为其与page的大小相同，可以提高内存分配效率（mmap&#x2F;brk都是按照page扩充的），减少TLB miss</p><br><p><strong>为什么将内存块和其头部信息分开存储，即分为了hblk和hblkhdr？</strong></p><p>将hblk和hblkhdr分离后，Boehm GC通过二级索引结构_top_index-&gt;bottom_index-&gt;header，可以通过地址分段进行索引，支持在O(1)内查找到header。其好处在于：</p><ul><li><p><strong>在Mark阶段快速判断是否为“指针”</strong>：Mark阶段可以利用地址和hblkhdr的映射关系，快速检查是否是一个指向对象的指针</p></li><li><p><strong>局部缓存性好</strong>：GC经常只需要header信息，比如Reclaim阶段并不关心具体数据，只关心哪些数据没有被标记，所以完全不需要访问对象本身。此时，如果header散布在各个hblk前，cache miss严重。而将header集中存储，比如前面提到的bottomeIndex一维双向侵入式链表，其效率更高</p></li><li><p><strong>避免了元数据对纯用户数据的干扰</strong></p></li></ul><br><p><strong>Boehm GC中用到了很多链表结构，不会因为cache miss较多而导致效率低下吗？</strong></p><p><strong>ok_freelist为二级链表，但实际上每一条链表中填充的内存块都是连续的，因为它们都是通过同一块hblk等量分割形成的。</strong> 并且，在实际分配过程中，并不会真的遍历ok_freelist，都是直接通过O(1)定位到指定大小的链表并从头部获取内存块。</p><p><strong>GC_hblkfreelist为指针数组</strong>，GC_hblkfreelist[i]代表了hblk的地址，尽管是数组，但是其效率一定低于ok_freelist，因为GC_hblkfreelist[i]的物理结构类似于：</p><p><img src="https://img.aixheee.top/79b015ff-63e9-4ab5-8119-fd9df638bb28.png"></p><p>hblk之间并不直接相连，而是通过hblkhdr间接地在“逻辑上”相连，遍历一条“链表”时，需要通过hblk的地址，查找到hblkhdr，再通过hblkhdr中的next指针找到当前“链表”的下一个hblk，cache miss严重</p><p>此外，辅助_top_index的也是一个一维的bottom_index<strong>双向侵入式链表</strong>，其存储了指向hblkhdr的指针数组</p><p><img src="https://img.aixheee.top/b1bbe012-3812-4513-af85-4772650893a6.png"></p><p>之所以这样做，一是有约束难以转化为使用数组，二是高频低频路径。</p><p>具体来说，每个hblk都是稀疏且不连续的，这一点是无法改变的，是由heap的特点决定的，强行数组化反而麻烦且浪费空间，<strong>链表更适合管理非连续内存、支持块合并，这些特性对于GC的内存管理比cache命中率更重要</strong></p><p><strong>此外，真正的高频访问路径在ok_freelist上</strong>，需要从中获取内存块以创建对象，而<strong>遍历这些链表大部分发生在无空闲内存块和GC触发等低频路径上，对性能影响有限</strong>。比如ok_freelist中没有可用的内存块时，则会到GC_hblkfreelist中查找，reclaim时需要根据所有的hblkhdr中的引用信息来判断回收哪个对象，都并不属于高频路径。</p><br><p>【实践启发】</p><ul><li><p>缓存数据的开辟。有时会使用字节数组byte[4096]用做缓存，实则反而导致内部碎片严重，需要多分配一个hblk，正确的方法是将长度设置为4096 - 32 - 1 &#x3D; 4063。-32是因为在IL2CPP下，数组需要32字节的额外内存来存储信息，而-1是因为从ok_freelist获取内存块时，如果是16的整数倍，则会再向上对齐一次。</p></li><li><p>GC时机的控制。在进度条加载时，申请一次强制的内存清理；在比赛过程中，将GC改为手动，以防止比赛过程中有明显的卡顿，这么做的前提是，内存优化较好</p></li></ul><h2 id="大对象内存分配"><a href="#大对象内存分配" class="headerlink" title="大对象内存分配"></a>大对象内存分配</h2><p>大对象内存分配时，是直接从memory-pool中获取内存块，即直接从GC_hblkfreelist中获取内存块，不再走ok_freelist，其逻辑和GC_allochblk()一模一样</p><br><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>本文详细拆解了Unity托管内存的分配机制，从整体架构出发，逐步深入到Boehm GC的核心数据结构与分配流程。我们可以看到，Unity的内存分配器在设计上与C++标准库的allocator乃至tcmalloc、jemalloc等高性能分配器有着相似的思路：通过分级管理、空闲链表、内存池等手段，在分配效率和内存利用率之间取得平衡。</p><p>理解这些底层原理，不仅能帮助我们写出更高效的Unity代码，更能为后续深入学习Unity GC的回收机制与优化策略打下坚实的基础。在后续文章中，我们将深入探讨GC的Mark-Sweep过程、增量GC的工作原理等</p>]]></content>
      
      
      <categories>
          
          <category> 内存分配与管理 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>【内存分配与管理】移动语义——参数传递</title>
      <link href="/posts/lvalue_rvalue_2.html"/>
      <url>/posts/lvalue_rvalue_2.html</url>
      
        <content type="html"><![CDATA[<p>在探讨了移动语义的机制之后，我们面临一个更实际的问题：如何在实际的函数设计中充分利用这些机制？​ 移动语义、右值引用、完美转发为我们提供了强大的工具，但这些工具的最佳使用场景是什么？不同的参数传递方式又有怎样的性能特性和设计考量？</p><h1 id="三种传参方案"><a href="#三种传参方案" class="headerlink" title="三种传参方案"></a>三种传参方案</h1><p>现代C++提供了多种参数传递策略：左值引用重载、右值引用重载、万能引用完美转发、按值传递移动。每种策略都有其适用场景和权衡点。理解这些选择背后的性能模型和设计哲学，是编写高效、健壮C++代码的关键。</p><p>对于函数传参的参数设计，常见的有以下几种形式：</p><pre><code>class Widget {          // 途径一：public:                 // 针对左值和右值重载    void addName(const std::string&amp; newName)    { names.push_back(newName); }    void addName(std::string&amp;&amp; newName)    { names.push_back(std::move(newName)); }    ...private:    std::vector&lt;std::string&gt; names;};class Widget {       // 途径二：public:              // 使用万能引用    template&lt;typename T&gt;    void addName(T&amp;&amp; newName)    { names.push_back(std::forward&lt;T&gt;(newName)); }    ...};class Widget {        // 途径三：public:               // 按值传递    void addName(std::string newName)    { names.push_back(std::move(newName)); }    ...};//考虑三种调用方式：Widget w;std::string name(&quot;Aixheee&quot;);w.addName(&quot;Aixheee&quot;); //传入左值 但类型不同 w.addName(name);      //传入左值w.addName(name + &quot;hello&quot;); //传入右值</code></pre><br><p>对于<strong>途径一(重载)</strong> 下的三种调用方式：</p><ul><li><p>w.addName(“Aixheee”)：会产生临时字符串对象，再调用右值引用版本，由于是右值引用，所以不会再次构造，最后在push_back()时触发移动构造。<strong>一次直接构造(临时对象创建)+一次析构(临时对象析构)+一次移动构造(push_back)</strong></p></li><li><p>w.addName(name)：不产生临时对象，调用const左值引用版本，push_back()时触发拷贝构造。<strong>一次拷贝构造(push_back)</strong></p></li><li><p>w.addName(name + “hello”)：不产生额外的临时对象(即传参过程中不产生临时对象，operator+产生临时对象带来的一次构造+一次析构不考虑)，调用右值引用版本，最后在push_back()时触发移动构造。<strong>一次移动构造(push_back)</strong></p></li></ul><br><p>对于<strong>途径二(万能引用)</strong> 下的三种调用方式：</p><ul><li><p>w.addName(“Aixheee”)：”Aixheee”的类型为const char[]数组类型，推导时退化为const char<em>指针类型。此时，由于是万能引用+完美转发，所以不会产生临时对象。但是在push_back时，由于是const char <em>右值类型，所以会产生**一次string构造</em></em>。</p></li><li><p>w.addName(name)：不产生临时对象，万能引用接收+完美转发，push_back()时触发拷贝构造。<strong>一次拷贝构造(push_back)</strong></p></li><li><p>w.addName(name + “hello”)：不产生额外的临时对象(即传参过程中不产生临时对象，operator+产生临时对象带来的一次构造+一次析构不考虑)，万能引用接收+完美转发，最后在push_back()时触发移动构造。<strong>一次移动构造(push_back)</strong></p></li></ul><br><p>对于<strong>途径三(值传递+std::move)</strong> 下的三种调用方式：</p><p>由于是值传递，所以一定涉及到一次构造。</p><ul><li><p>w.addName(“Aixheee”)：”Aixheee”的类型为const char[]数组类型，推导时退化为const char*指针类型。此时，会先直接构造一个string类型对象，push_back()时触发移动构造。<strong>一次直接构造(形参创建)+一次移动(push_back)+一次析构(形参析构)</strong></p></li><li><p>w.addName(name)：由于是左值，所以有一次拷贝构造用于创建形参，接着在push_back()时触发一次移动构造。<strong>一次拷贝构造(形参创建)+一次移动(push_back)+一次析构(形参析构)</strong></p></li><li><p>w.addName(name + “hello”)：不产生额外的临时对象(即传参过程中不产生临时对象，operator+产生临时对象带来的一次构造+一次析构不考虑)。由于是一个右值，所以有一次移动构造用于创建形参，接着在push_back()时又触发一次移动构造。<strong>一次移动构造(形参创建)+一次移动(push_back)+一次析构(形参析构)</strong></p></li></ul><br><h2 id="方案二"><a href="#方案二" class="headerlink" title="方案二"></a>方案二</h2><p>可以看出方案二的成本是最低的，但是并不推荐使用方案二：</p><p>【Effective Modern C++ 26条款】不要把将万能引用应用于函数重载，尤其是构造函数！由于万能引用是模板推导+值类型万能匹配，所以<strong>其匹配能力过强</strong>，在编译器重载决议时往往调用不到预期函数，从而产生意想不到的错误。<strong>该问题在重载构造函数的场景下尤为严重，其会劫持拷贝构造和移动构造函数</strong></p><p>具体来说：比如，同时提供了根据形参类型为int获取结果的函数以及通过万能引用获取结果函数。如果用户想调用参数为int版本的重载函数，但却传入了一个long，那么就会调用到万能引用版本的函数，而不是int版本的，因为万能引用版本更匹配</p><pre><code>template&lt;typename T&gt;void logAndAdd(T&amp;&amp; name){    auto now = std::chrono::system_clock::now();    log(now, &quot;logAndAdd&quot;);    names.emplace(std::forward&lt;T&gt;(name));}void logAndAdd(int idx)            // 新的重载函数{    auto now = std::chrono::system_clock::now();    log(now, &quot;logAndAdd&quot;);    names.emplace(nameFromIdx(idx));}</code></pre><p>在构造函数的场景下尤为严重：万能引用重载构造函数会劫持拷贝构造函数，因为拷贝构造函数一般会用常引用作为参数，而传入的待拷贝对象通常都是非const对象，这就导致万能引用重载版本的匹配更为精准，从而”劫持“调用</p><pre><code>class Person {public:    template&lt;typename T&gt;             // 完美转发构造函数    explicit Person(T&amp;&amp; n)        :name(std::forward&lt;T&gt;(n)) {}    explicit Person(int idx);     // 形参为 int 的构造函数    Person(const Person&amp; rhs);    // 复制构造函数（由编译器生成）    Person(Person&amp;&amp; rhs);         // 移动构造函数（由编译器生成）    ...};Person p(&quot;Aixheee&quot;);Person p2(p); //居然无法通过编译！！！</code></pre><p>如果存在继承关系，则会更为严重：</p><pre><code>class SpecialPerson : public Person {public:    SpecialPerson(const SpecialPerson&amp; rhs)  // 复制构造函数；        :Person(rhs)   //调用的是基类的完美转发构造函数！    { ... }            SpecialPerson(SpecialPerson&amp;&amp; rhs)   // 移动构造函数；        :Person(std::move(rhs)) //调用的是基类的完美转发构造函数！    { ... }      </code></pre><p>解决方案之一：</p><p><strong>std::enable_if对万能引用重载做约束，使其只在满足特定条件时才参与重载，从而避免”过度匹配“的问题。</strong> 其核心原理是利用 SFINAE，是不满足条件的模板在重载决议阶段被移除，不生成对应的模板函数。常见的做法比如排除自身以及派生类型、限制可转换类型等等，以及区分不同类型的参数</p><pre><code>//排除自身以及派生类型class Person {public:    template&lt;typename T,             typename = std::enable_if_t&lt;                 !std::is_base_v&lt;std::decay_t&lt;T&gt;, Person&gt;                 &amp;&amp;!std::is_integral&lt;std::remove_reference_t&lt;T&gt;&gt;             &gt;&gt; //decay_t用于去除const volatile修饰（cv饰词）    explicit Person(T&amp;&amp; name);};</code></pre><p>这种方案的优势是，保留了万能引用重载，其效率较高；缺点是，如果出现错误，报错冗长难以分辨，易用性不好</p><br><h2 id="方案一与方案三"><a href="#方案一与方案三" class="headerlink" title="方案一与方案三"></a>方案一与方案三</h2><p>对于方案一（<strong>左值重载和右值重载</strong>）：</p><p>其虽然匹配精准。其缺点在于：</p><p>写起来较为麻烦；在发生隐式类型转换的情况下，成本略高。比如调用setName(“aaaa”)，字符串常量是const char*类型，对于重载来说，需要先进行隐式类型转换，构造出临时字符串对象，匹配右值引用版本；而对于万能引用来说，可直接进行模板推导，没有中间的临时对象。少了一次构造、一次移动构造、一次析构</p><p><mark>对于方案三（按值传递+std::move()）：</mark></p><p><strong>其核心优点在于，通过引入一次额外的移动操作来统一处理了左值和右值，用些许性能换取了简洁性。</strong></p><p>具体来说，对于按值传递，其既可以通过拷贝构造接收左值，也可以通过移动构造接收右值。但是对比左值右值分别重载版本，其至少会多引入一次move，具体分析：不过这是在函数内部一定需要“消费（存储&#x2F;拷贝）”参数的前提下，否则引入的额外开销会更大</p><pre><code>void setName(std::string name);//传左值：s-&gt;name-&gt;std::move(name) 1copy+1move//传右值:临时对象-&gt;name-&gt;std::move(name) 2movevoid setName(const std::string&amp; name) {    this-&gt;name = name; // 1 copy}void setName(std::string&amp;&amp; name) {    this-&gt;name = std::move(name); // 1 move}</code></pre><p><strong>所以，在使用按值传递+std::move()时，需要考虑两方面：一是在当前场景下，是否真的可以发挥这种写法的优势？二是这种优势的代价会不会过大？</strong> 对于前者，我们应该考虑——是否真的需要同时支持左值和右值重载该支持类型的传递；对于后者，我们应该考虑——什么时候这种写法带来的代价相较于左值右值重载较小</p><ul><li><p><strong>仅仅对于支持拷贝复制的形参类型，才推荐使用按值传递。（从是否真的可以发挥优势来考虑）</strong> 因为对于可复制类型，按值传递可以统一copy和move；但是对于只移类型（无拷贝，只有移动）来说，其不存在copy路径，按值传递只会引入额外的一次move，直接使用右值引用作为参数即可。<strong>这既是效率上的提升，更是设计原则上的完善——按值传递版本的优势就在于，只需要写一个函数即可同时接收左值与右值，但对于“只移类别”来说，根本不需要提供对左值的重载版本，只提供接收右值的版本完全足够。</strong> 所以，不推荐使用按值传递，只提供右值版本即可具体来说，比如对于unique_ptr，如果按值传递，会先通过move构造形参，再通过move来传递参数，共两次</p><pre><code>void setPtr(std::unique_ptr&lt;std::string&gt; ptr) {    p = std::move(ptr);}</code></pre><p>如果按右值引用传递，则不会构造形参，只有一次move</p><pre><code>void setPtr(std::unique_ptr&lt;std::string&gt;&amp;&amp; ptr) {    p = std::move(ptr);}w.setPtr(std::make_unique&lt;std::string&gt;(&quot;Modern C++&quot;));</code></pre></li><li><p><strong>在函数一定会消费参数，比如存储、拷贝的场景下才适用于按值传递。（从代价的角度考虑）</strong> 如果参数可能不会被使用，比如带条件的插入或校验逻辑，则按值传递可能会引入不必要的构造、析构成本，此时应该优先使用const引用。</p><pre><code>void addName(std::string newName) {    //无论是否真的使用newName 都会付出构造+析构的代价    if ((newName.length() &gt;= minLen) &amp;&amp; (newName.length() &lt;= maxLen))        names.push_back(std::move(newName));}</code></pre><p>但要注意区分“赋值”和“拷贝构造”。对于基于赋值的参数复制，相比于左值右值重载，如果本来可以在“赋值阶段”做优化（比如复用内存），那么“传值”会让你<strong>提前失去这个机会</strong>，破坏对象内部资源复用优化的机会，其性能开销远大于一次移动操作。具体来说，传入左值一定会发生1copy+1move；但是对于引用，其存在优化点，比如下面的text在text.capacity &gt;&#x3D; newPwd.size() 的条件下，完全可以memcpy复用原有内存，而不是赋值分配新内存。在比较之下，此时按值传递+std::move()付出的成本较大实际上，此时的成本取决于对象当前的状态，性能变成”数据相关“——需要考虑数值的大小、是否有优化策略（比如std::string的SSO，对于小字符串来说，move ≈ copy）等等</p><pre><code>void changeTo(const std::string&amp; newPwd) {    text = newPwd;}</code></pre></li><li><p><strong>在移动构造成本较低时，才使用该方式（从代价的角度考虑）。</strong> 否则，这种额外移动的开销将抵消其带来的简洁性优势。因为按值传递通过引入了一次额外的移动操作来统一处理左值和右值。</p></li><li><p>如果在有切片风险的场景下，不能使用按值传递</p></li></ul><br><p><mark>总结：</mark></p><p><strong>按值传递的虽然带来了代码的简洁性、避免了完美转发的复杂性（误匹配、报错可读性差），属于一种折中方案，但是其性能取决于多种上下文因素，比如是否拷贝&#x2F;存储参数、是否重新分配内存、move代价等，我们在写代码时很难保证某些条件一直成立，有较大的不确定性。相比之下，左值右值重载的方式更为更稳定和可控。</strong></p><p><strong>因此，应该默认采取左值右值重载的方式，仅在完全保证按值传递不会带来额外的较大性能开销时，才将其作为简化接口的替代方案</strong></p><p><strong>而对于万能引用+完美转发，其应用场景不在函数重载和普通处理函数，但是在保持参数值类别并转发给其他函数的的场景下，其是我们唯一的选择。</strong> 具体来说，由于<strong>其匹配能力过强且类型推导复杂，容易导致”劫持“目标函数的调用，不应该用于普通的处理函数</strong>；<mark>其真正的应用场景在”转发“，即该函数本身不”消费“（存储&#x2F;拷贝）参数，只是负责将这些参数“转交”给目标函数进行处理。</mark>比如工厂函数make_shared，该函数本身不关心这批参数的一切信息，只是负责转交：</p><pre><code>template&lt;typename T, typename... Args&gt;std::unique_ptr&lt;T&gt; make(Args&amp;&amp;... args) {    return std::unique_ptr&lt;T&gt;(new T(std::forward&lt;Args&gt;(args)...));}</code></pre><p>function中的包装函数：</p><pre><code>template&lt;typename F, typename... Args&gt;auto call(F&amp;&amp; f, Args&amp;&amp;... args) {    return std::forward&lt;F&gt;(f)(std::forward&lt;Args&gt;(args)...);}</code></pre><p>emplace系列：</p><pre><code>template&lt;typename... Args&gt;void emplace_back(Args&amp;&amp;... args);</code></pre><p>至此，我们完成了C++内存管理系列中关于移动语义与参数传递策略的深入探讨。从malloc的底层内存分配，到new与构造函数的关系，再到RAII、智能指针、值语义与引用语义的辨析，最后落脚于移动语义及其在函数传参中的实际应用——这一路走来，我们始终围绕着一个核心命题：<strong>如何以最小且可控的开销，高效、安全管理内存与资源的生命周期。</strong></p><p>在接下来的文章中，我们将正式进入Unity的内存管理世界，从它的双轨制内存架构开始，深入剖析托管内存的分配与回收机制。</p>]]></content>
      
      
      <categories>
          
          <category> 内存分配与管理 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>【内存分配与管理】移动语义</title>
      <link href="/posts/lvalue_rvalue.html"/>
      <url>/posts/lvalue_rvalue.html</url>
      
        <content type="html"><![CDATA[<p>在探讨了RAII和智能指针之后，我们看到C++如何通过所有权模型来管理堆上资源的生命周期。unique_ptr的“独占所有权”和shared_ptr的“共享所有权”，将资源管理的责任从模糊的约定提升为编译器可检查的类型系统特性。</p><p>然而，所有权思想的价值远不止于智能指针。当我们审视值语义对象时，一个自然的优化思路浮现了：<strong>如果某些对象只是临时存在的“中间产物”，我们能否不进行昂贵的深拷贝，而是“移动”其内部资源的所有权？</strong></p><p>这就是C++11引入移动语义的核心动机。移动语义实际上是所有权思想在值语义领域的自然延伸：</p><ul><li><p>unique_ptr的移动是“堆资源所有权的转移”</p></li><li><p>普通对象的移动是“对象内部资源所有权的转移”</p></li></ul><p><strong>通过移动语义，C++实现了一个精妙的平衡：既保持了值语义的简洁性和确定性，又在需要时避免了不必要的拷贝开销。配合RVO&#x2F;NRVO等编译器优化，现代C++让值语义对象既安全又高效。</strong></p><p>本文将探讨移动语义的完整技术栈：从左值&#x2F;右值的本质区分，到右值引用如何支持移动语义，再到完美转发如何解决通用代码中的值类别保持问题。</p><br><h1 id="左值、纯右值与将亡值"><a href="#左值、纯右值与将亡值" class="headerlink" title="左值、纯右值与将亡值"></a>左值、纯右值与将亡值</h1><p>这些值类别是<strong>表达式属性</strong>，而<strong>不是对象的属性</strong>，因为对于同一个变量，其表达式不同，会有不同的值类别，比如x、std::move(x)</p><ul><li><strong>变量</strong>(variable)是有名字的对象</li><li><strong>对象</strong>是(object)存储某个类型的值的内存区域(memory)</li><li><strong>值</strong>(value)是按照类型进行解释的比特集合</li><li><strong>类型</strong>(type)定义一组可能的值以及一组（作用对象的）操作</li></ul><p><img src="https://img.aixheee.top/7615cb7d-2a31-408d-a884-bad6efacc4f0.png"></p><p>左值表达式是一个<strong>具有身份</strong>的表达式，<strong>表示一个可以持续存在的对象</strong>，可以取地址。从资源所有权的角度来看，其不会触发移动语义，其<strong>资源所有权</strong>依然被该对象持有且该对象长期存在（相对于右值），需要先通过<strong>std::move</strong>将左值转换为一个将亡值，并触发移动语义，才能将资源的所有权转移。（特殊的，字符串常量虽然是左值，但是可以被移动）</p><p>纯右值表达式是一个<strong>不具有身份</strong>表达式，<strong>并不对应一个具体的对象</strong>，<strong>专门用于计算或初始化对象</strong>。比如字面常量，直接编码在指令中，再比如函数的返回值，是临时存储在寄存器中的值，无法获取到其地址。从资源所有权的角度来看，其<strong>表示一个”尚未被任何具体对象拥有的资源”</strong>，其所有权会在初始化&#x2F;赋值时直接转移给目标对象</p><p>将亡值表达式<strong>具有身份</strong>，比如，std::move(s)就是一个将亡值，其对应的对象依然是原来的对象s。从资源所有权的角度来看，相比于左值，其即将消亡，正准备放弃资源所有权，所以其可以触发移动语义，将<strong>资源所有权</strong>转移给其他对象</p><blockquote><p>较为特殊的是，在C++下字符串常量和普通常量不同，字符串常量的类型是const char[]字符数组，其存储于只读数据段.rodata，可以被取地址，是一个左值，而不像其他常量是右值</p></blockquote><br><p><strong>将亡值和纯右值的区别在哪呢？</strong></p><p>二者最本质的区别在于，前者是对象，后者是值。将亡值相较于纯右值来说是有身份的，纯右值表示一个纯粹的值，不对应任何对象，用于初始化对象，其内嵌在指令中、存在于寄存器中等等；而将亡值表示一个已经存在的对象，只是该对象的资源可以被转移，通常在栈、堆上</p><br><h1 id="右值引用与移动语义"><a href="#右值引用与移动语义" class="headerlink" title="右值引用与移动语义"></a>右值引用与移动语义</h1><p><strong>为什么需要右值引用？</strong></p><p>返回值优化：函数的返回值是一个右值rvalue，如果通过变量接收该右值，相比于const 引用接收，会导致额外的一次拷贝构造+析构，即将匿名空间中的值拷贝到接收变量中，再将匿名空间析构。<strong>而const引用接收的原理是，让引用直接绑定这块匿名空间，并不再释放立即释放这块匿名空间，而是随着引用成为了栈上的变量。</strong> 但是，其虽然省去了调用，但是常引用是const修饰的，不可修改。<strong>于是，引入了右值引用。其在行为上和const引用几乎是一模一样的，区别只在于由于没有const修饰，所以可改。</strong> 但是，<mark>这并不是右值引用的主要功能</mark>，因为在编译器的RVO&#x2F;NRVO优化下，就算直接用变量接收函数返回值，同样不会触发额外的拷贝构造+析构，会在接收变量的内存区域上直接构造</p><pre><code>struct Test {  Test() {}  ~Test() {}};Test Demo1() {  Test t;  return t;}void Demo2() {  Test &amp;&amp;t = Demo1();}</code></pre><p><strong>支持移动语义：</strong></p><p>RVO&#x2F;NRVO主要针对的是返回值做优化，<mark>而对于传参、赋值等移动语义领域，需要移动语义减少对象拷贝的代价</mark>，这里才是<strong>右值引用的主要发挥场景</strong>。</p><p><strong>移动语义优化——由于右值对象一般是临时对象，所以如果新对象想要获取右值对象的资源，应该直接让新对象获取资源的所有权，而不是拷贝+删除，以降低传参拷贝时的代价，这也是为什么需要移动语义的原因。</strong></p><p>而<mark>为了能让函数接收右值，优先支持移动语义</mark>（而不是调用常引用参数版本），所以引入了<strong>右值引用</strong>。相比于左值引用，左值引用代表了该资源对象还需要被使用，而<strong>右值引用代表着我们可以对该对象的资源进行转移。</strong></p><p>比如<strong>移动构造函数</strong>​ 和 <strong>移动赋值运算符</strong>​：在移动赋值函数中，不会再对资源进行“深拷贝”，而是通过调整指针指向，达到“转移资源所有权”的目的。</p><pre><code>String::String(String &amp;&amp;str): buf_(str.buf_) { // 直接浅复制  str.buf_ = nullptr;}</code></pre><br><p><strong>在通过右值引用接收到右值后，如何转发、处理该右值？</strong></p><p>该问题的本质是，参数是作为一个具名的左值（具体为什么参数是左值，见下文解释），<strong>那么如何将表达式的值类型从左值转换为右值，从而传递给参数为右值引用的函数，以将资源移动？</strong> 为了解决该问题，所以引入了std::move()</p><p>【如何理解std::move()？】<strong>std::move()本质上是static_cast&lt; T&amp;&amp; &gt;，将变量<mark>表达式值类型</mark>从左值变为一个右值，把对象标记为“资源可以被移动”，将资源的所有权转移，其和右值引用参数相匹配，<mark>进而</mark>触发移动语义，真正的让资源被转移。</strong></p><p>这里有两个关键点：</p><p>一是<strong>std::move()不会改变对象本身的性质</strong>，其依旧在栈&#x2F;堆上存储着，只是在表达式层面将它“从左值转换为右值”；</p><pre><code>std::string s = &quot;hello&quot;;std::move(s);  // 表达式是右值s;             // 表达式仍然是左值</code></pre><p>二是<strong>std::move()本身并不会进行任何移动操作</strong>，其只是一个强制类型转换的模板函数，允许触发移动语义，让资源被移动，<strong>而真正的资源移动操作需要是在函数中进行的，比如移动构造&#x2F;移动赋值函数。</strong> 三者的关系为：</p><ul><li>std::move：发出“可以移动”的信号，将资源所有权转移</li><li>T&amp;&amp;：接收这个信号</li><li>移动构造函数（或其他函数中）：实际执行资源转移</li></ul><br><p>总结一下，有了移动语义后传参的行为：该行为取决于形参类型和实参，<strong>如果形参是按值传递的，则一定会触发构造，区别在于是拷贝还是移动——如果实参是左值，则为拷贝； 如果实参是右值，则为移动。</strong> 而如果形参是左值引用或右值引用传递的，则不会触发构造。</p><pre><code>void func(std::string s);std::string str = &quot;hello&quot;;func(str);              // 左值 → 拷贝构造func(std::move(str));   // 右值 → 移动构造func(&quot;hello&quot;); </code></pre><blockquote><p>关于func(“hello”)：“hello”是一个const char*类型的左值，如果考虑C++17的“强制拷贝消除”优化，prvalue不再先构造临时对象，而是直接初始化目标对象。</p><p>如果不考虑优化，则构造一个std::string临时对象，再调用拷贝构造，多产生了一次构造、一次移动、一次析构。</p></blockquote><br><p><mark>std::move底层原理：</mark></p><p><strong>本质上是一个强制类型转换，将对象的值类别转换为右值</strong></p><pre><code>template&lt;typename T&gt;typename std::remove_reference&lt;T&gt;::type&amp;&amp; move(T&amp;&amp; t) noexcept {    using ReturnType = typename std::remove_reference&lt;T&gt;::type&amp;&amp;;    return static_cast&lt;ReturnType&gt;(t);}</code></pre><ul><li><p>参数为万能引用，保证std::move()可以接收任意值类别</p></li><li><p>remove_reference：去掉模板推导实例化结果的引用类型。比如，如果传入一个左值，则模板推导为int&amp;，如果直接返回T&amp;&amp;，则依然返回的是一个左值。所以，需要去除引用类型</p></li><li><p>static_cast&lt; T&amp;&amp;&gt;(t)：将表达式的值类别从左值转换为右值。需要注意的是，t是一个左值表达式</p></li></ul><br><p><strong>为什么“右值引用变量”是左值？</strong></p><p><mark>类型和值类别是两个概念。</mark><strong>右值引用描述的是一个“类型”</strong>，就像int类型、double类型一样，<strong>而值类别描述的是表达式</strong>，比如Test&amp;&amp; t &#x3D; Demo()，t作为一个表达式(单个变量名也是表达式)是一个左值，其可以有具体的身份、可以取地址、可以被赋值，只不过其绑定的是一个右值。</p><p>比如，下面这个例子，虽然有万能引用，但是表达式t始终是一个左值，调用的结果是lvalue:123 lvalue:123</p><pre><code>void TestValue(int&amp; value){    std::cout &lt;&lt; &quot;lvalue: &quot; &lt;&lt; value &lt;&lt; &#39;\n&#39;;}void TestValue(int&amp;&amp; value){    std::cout &lt;&lt; &quot;rvalue: &quot; &lt;&lt; value &lt;&lt; &#39;\n&#39;;}template&lt;typename T&gt;void Print(T&amp;&amp; t){    TestValue(t);}int main(){    Print(123);    int num = 456;    Print(num);}</code></pre><p>从使用的角度来说为什么右值引用变量不能是右值：<strong>如果右值引用变量是一个右值，会导致对象被“隐式地”多次移动</strong>。所以，必须通过std::move()来“显示地”触发移动语义的的操作，由用户手动控制，而不能是隐式发生的</p><pre><code>假设右值引用变量是一个右值：void process(std::string&amp;&amp; s) {    save(s);    log(s); //错误 因为s的资源已经被转移走了}</code></pre><br><p><strong>什么应用场景下使用std::move()？【Effective Modern C++ 条款25】</strong></p><p><strong>让std::move和右值引用配合使用。如果当前函数的参数值类别是右值引用类型的，则代表实参不再需要该对象资源了，所以需要通过move将形参继续转换为右值传递下去，触发移动语义来转移其资源。</strong></p><p>移动构造&#x2F;移动赋值的实现：</p><pre><code>class A {public:    std::string s;    A(A&amp;&amp; other)        : s(std::move(other.s)) {}   // 必须 move    A&amp; operator=(A&amp;&amp; other) { //other绑定的对象是一个右值         s = std::move(other.s);      // 必须 move        return *this;    }};</code></pre><p>又或者，<strong>我们主动通过std::move()将表达式值类型，从左值转换为右值，传递给参数为右值引用的函数，以将资源移动。</strong> 比如容器插入的场景：</p><pre><code>std::vector&lt;std::string&gt; v;std::string s = &quot;hello&quot;;v.push_back(s);              // 拷贝v.push_back(std::move(s));   // 移动（推荐）v.push_back(&quot;hello&quot;);        //注意，这里不会触发移动，因为“hello”是一个左值，会先通过“hello”构造一个字符串的临时对象，再绑定到调用拷贝构造，绑定到const std::string&amp;v.push_back(std::string(&quot;hello&quot;));  // 创建临时 string，会调用移动构造函数（如果可行）</code></pre><br><p><mark>std::move使用注意事项：</mark></p><ul><li><p><strong>std::move不能完全保证资源被移动，是否真正可以发生移动还却决于”类型“是否允许被修改（即是否为const）。</strong> 如果想要一个资源对象被转移，那么就不要将其声明定义为常量</p><pre><code>class A{public:    A(const std::string text)        :value(std::move(text)) //实则依然调用的是拷贝构造 而不是移动构造    {}private:    std::string value;}</code></pre><p>这是因为std::move(text)的表达式值类别为<strong>const std::string&amp;&amp;</strong>，而不是std::string&amp;&amp;，无法和匹配上移动构造函数，因为移动本质上是“窃取”资源，而const修饰又不允许修改该对象，是相互矛盾的。所以，编译器会退而求其次地尝试拷贝构造，不会调用移动构造</p><pre><code>std::string(std::string&amp;&amp; other); //移动构造std::string(const std::string&amp;&amp; other); //拷贝构造</code></pre></li><li><p>不要在返回前move返回值。比如：</p><pre><code>std::string base_url = tag-&gt;GetBaseUrl();if (!base_url.empty()) {  UpdateQueryUrl(std::move(base_url) + &quot;&amp;q=&quot; + word_);}LOG(INFO) &lt;&lt; base_url;  // |base_url| may be moved-from</code></pre><p>导致外部接收到该变量后，不能继续使用，否则可能导致未定义行为。此外，最好不要对返回值move，比如：</p><pre><code>std::unique_ptr&lt;int&gt; foo() {  auto ret = std::make_unique&lt;int&gt;(1);  //...  return std::move(ret);  // -&gt; return ret;}</code></pre><p>C++会自动将离开作用域的非引用类型的返回值当作右值，并通过RVO&#x2F;NRVO进行优化，而std::move()会阻止编译器优化</p></li></ul><p><strong>RVO&#x2F;NRVO优化实现零拷贝：</strong></p><p><strong>何时触发？</strong><br>前者在返回匿名临时对象时触发，比如return {1,2}，C++17起，其被规定为<strong>强制优化行为</strong>；后者在<strong>返回具名的局部对象，且返回类型和函数签名种的返回类型一致时触发，比如 A a; return a;</strong>。</p><p>但是，在几种情况下可能无法触发NRVO，仍需移动语义兜底处理：返回表达式，比如a+b，std::move(p)等；返回函数参数（因为函数参数是调用方已经创建好的对象，没法在函数接受变量的内存空间上直接构造）；多个返回路径返回不同对象（没法在编译期判断究竟会构造哪个对象）；条件运算符返回不同对象；异常路径等等</p><p><strong>为什么RVO可以被强制，而NRVO不可以？</strong><br>RVO之所以可以强制，是因为在这些场景下返回的都是prvalue，C++17标准将其重新定义为“允许在返回值位置直接构造”，少了拷贝&#x2F;移动构造调用，<strong>不会改变“可观察行为”</strong>，其是一个匿名对象，没有身份和地址。<strong>而NRVO则不一样，其优化的是“真实存在的对象”，其有名字、有地址、有生命周期，这种优化实际上会改变“可观察行为”</strong>，比如地址的值、构造&#x2F;拷贝&#x2F;析构次数问题等，所以只能作为编译器优化，而不能作为语言强制规则</p><p><strong>如果NRVO失败了，会怎么做？</strong><br>NRVO失败时，如果是局部变量，会退而求其次地使用移动语义（两次move），如果没有move构造再使用拷贝拷贝</p><p><strong>RVO&#x2F;NRVO优化是怎么做到的？</strong><br>假设调用函数为B，被调用函数为A。编译器会为函数A添加一个传递参数，比如原先的函数原型是Object create()，实际上转换为void create(Object* __heidden_result)，调用方从result &#x3D; create()变为create(&amp;result)</p><p>B在调用A前，会在自己的函数栈帧中提前开辟内存区域，并将这块内存的起始地址作为参数传入到A中。A直接在这块预留的内存位置上构造obj，直接省去了两次拷贝和析构。比如T result &#x3D; a + b，则直接将a+b的结果放到result中。</p><p>效果即为最右侧一图：<br><img src="https://img.aixheee.top/d5926d7a-1a66-4f57-900b-43e68166c0c1.png"></p><br><h1 id="移动构造与移动赋值函数"><a href="#移动构造与移动赋值函数" class="headerlink" title="移动构造与移动赋值函数"></a>移动构造与移动赋值函数</h1><p><strong>编译器什么时候会生成默认的移动构造&#x2F;移动赋值函数？</strong></p><p>如果用户没有自定义拷贝构造&#x2F;拷贝赋值&#x2F;析构函数，则编译器才会生成默认的移动构造&#x2F;移动赋值函数。实际上，我们不需要记住这一点，只需要遵循<strong>Rule of Five</strong>即可——一旦要手动管理资源，手动定义了拷贝构造&#x2F;拷贝赋值&#x2F;析构函数&#x2F;移动构造&#x2F;移动赋值函数中的一个，那么应该也手动定义其余的成员函数</p><p><strong>为什么编译器只要在”无自定义资源“的情况下，才会生成默认的移动构造&#x2F;移动赋值？</strong></p><p>【可以和”为什么编译器不会生成默认的拷贝构造&#x2F;赋值重载“联系起来编译器生成的默认的拷贝构造&#x2F;赋值重载都是按对象一个个浅拷贝的】<strong>编译器生成的默认的移动构造&#x2F;移动赋值都是逐成员move的。但不会自动帮我们把源资源置空（other.ptr &#x3D; nullptr），存在双重释放的风险。</strong> 这是因为编译器根本理解不了我们的资源语义，不知道我们对于move后的对象是否由额外的状态约束，不知道是否允许空状态，所以不会帮我们把源资源置空</p><blockquote><p>具体来说，对于自定义类型成员，会看是否实现了移动构造，如果实现了就调用移动构造，没实现就调拷贝构造，如果自定义类不能拷贝和移动，则会报编译错误，比如包含const成员和引用成员；</p><p>对于内置类型成员直接会按字节拷贝，因为它们没有”资源“，只有”值“</p></blockquote><pre><code>T(T&amp;&amp; other)    : m1(std::move(other.m1)),      m2(std::move(other.m2)),      ...{}</code></pre><p><strong>只有在成员都是标准库类型，或者没有任何要手动管理的资源时</strong>，才能采用编译器默认生成：std::string std::vector都已经正确地实现了move，不会出错</p><pre><code>class A {public:    A(A&amp;&amp;) = default;    A&amp; operator=(A&amp;&amp;) = default;private:    std::string s;    std::vector&lt;int&gt; v;};</code></pre><br><p><strong>如何写一个正确的移动构造？</strong></p><p>转移资源所有权+将源对象置为”安全状态“+无异常</p><pre><code>class String {public:    String(String&amp;&amp; other) noexcept        : buf_(other.buf_)，nums_(other.nums_) // 1️⃣ 偷资源    {        other.buf_ = nullptr; // 2️⃣ 防止 double free    }private:    char* buf_;    vector&lt;int&gt; nums_;};</code></pre><p><strong>如何写一个正确的移动赋值运算符？</strong></p><pre><code>String&amp; opeartor(String&amp;&amp; other) noexcept{    if(this != other)  // 1️⃣ 防自移动    {        delete[] buf_;  //2️⃣ 释放旧资源        this.buf_ = other.buf_;  // 3️⃣ 接管资源        this.nums_ = std::move(other.nums_);        other.buf_ = nullptr;  // 4️⃣ 清空源对象 vecotr类型有相应的移动赋值来清理    }    return *this;}</code></pre><p><strong>为什么要是noexcept的？</strong></p><p>移动操作需要标价为noexcept，是因为<strong>标准库在需要提供”强异常安全保证“时（比如vector扩容），只有在确认移动操作不会抛异常的前提下，才会使用移动语义</strong>，否则会退化为使用拷贝。<strong>这是为了保证，在异常发生时能够正确地回滚状态，如果move到一半出现了异常，源对象的资源已经被”窃取“，无法回滚。相比之下，拷贝完全可回滚，源对象的资源完好无损</strong></p><p>那我们应该怎么保证noexcept呢？noexcept标记并不保证函数内部不抛异常，只是保证异常不会逃出当前函数；一旦异常逃出，程序会直接调用std::terminate终止执行。如果只是单纯的指针交换进行资源转移，不调用可能抛异常的函数，比如分配内存，则可以写noexept。具体异常相关内容，后续会出一篇文章再详细说说</p><br><h1 id="引用折叠与万能引用"><a href="#引用折叠与万能引用" class="headerlink" title="引用折叠与万能引用"></a>引用折叠与万能引用</h1><p><strong>为什么需要有引用折叠？</strong> C++不允许直接定义引用的引用，比如int&amp; &amp;&amp; r&#x3D; i，但是在模板推导的情况下，完全可能出现引用的引用，比如：</p><pre><code>template&lt;typename T&gt;void func(T&amp;&amp; parme)</code></pre><p>所以，C++制定了一个统一的规则——引用折叠，使得类型系统能够正常工作。规则——只要有一个引用是左值引用，那么结果就是左值引用。</p><pre><code>&amp;  +  &amp;  →  &amp;&amp;  + &amp;&amp;  →  &amp;&amp;&amp; +  &amp;  →  &amp;&amp;&amp; + &amp;&amp; →  &amp;&amp;</code></pre><p><strong>为什么是左值优先？</strong></p><p>而之所以是“左值优先”（左值和左值引用），是因为左值代表了“有身份，需要被持续使用的对象”，所以为了防止原对象的资源错误地被移动，推导链条中只要一个表达式是左值，那么就不能将其推导为右值，而是保守的处理为左值。<strong>尽管有可能右值被误判为左值，但其代价只是性能上的损耗，而误判为右值的代价就是语义完全错误，可能出现意想不到的错误</strong></p><p><strong>那为什么这样规定呢？</strong></p><p><strong>这是为了和模板推导规则相配合，从而实现万能引用。而万能引用的作用在于，让一个函数就能够同时接收左值和右值，与完美转发像匹配，为正确处理、转发左值和右值的打下基础。</strong> 但是要注意，万能引用没有保留“值类别信息”，因为其是一个具名变量，始终是左值，需要通过完美转发恢复“值类别信息”！（模板推导规则——对于左值，推导为“引用类型”，比如int&amp;；对于右值，推导为“非引用类型”，比如int。后续会再出一篇文章详细说说模板相关内容）</p><pre><code>template&lt;typename T&gt;void func(T&amp;&amp; parme)int num = 10;func(num); //T会被推到为int&amp; 最终模板函数参数实例化为为int&amp;func(10)   //T会被推到为int 最终模板函数参数实例化为为int&amp;&amp;</code></pre><br><p><strong>什么场景下才可以触发万能引用？【Effective Modenr C++ 24条款】</strong></p><p>万能引用 &#x3D; 模板推导 + 引用折叠，如果其中之一不符合，则无法触发</p><ul><li><p><strong>模板参数T必须在调用点处参与类型推导，其不能是类模板已经确定的参数或调用处显示指定。</strong> 因为万能引用依赖于模板推导实例T来和引用折叠配合，从而正确地接受参数。比如，下面的例子就不是万能引用，因为T在类实例化时就已经确定了，其是一个普通的右值引用：</p><pre><code>template&lt;typename T&gt;struct A {    void f(T&amp;&amp; x);  // ❌ 不是万能引用};</code></pre></li><li><p>不能对T施加额外的修饰和变换。比如const修饰、容器修饰</p><pre><code>const T&amp;&amp;        // ❌ 有 const 修饰T&amp;               // ❌ 左值引用std::vector&lt;T&gt;&amp;&amp; // ❌ T 被包裹T*&amp;&amp;             // ❌ 指针类型</code></pre></li></ul><p>另外，auto&amp;&amp;也是万能引用，因为auto的推导规则，本质上和模板推导类似</p><pre><code>int a = 10;auto&amp;&amp; x = a;   // x 是 int&amp;auto&amp;&amp; y = 10;  // y 是 int&amp;&amp;</code></pre><br><h1 id="完美转发"><a href="#完美转发" class="headerlink" title="完美转发"></a>完美转发</h1><p><strong>为什么需要完美转发？</strong></p><pre><code>template&lt;typename T&gt;void print(T &amp; t){    std::cout &lt;&lt; &quot;Lvalue ref&quot; &lt;&lt; std::endl;}template&lt;typename T&gt;void print(T &amp;&amp; t){    std::cout &lt;&lt; &quot;Rvalue ref&quot; &lt;&lt; std::endl;}template&lt;typename T&gt;void testForward(T &amp;&amp; v){     print(v); //v是个左值表达式了,永远调用左值版本的print    print(std::forward&lt;T&gt;(v)); //本文的重点    print(std::move(v)); //std::move(v)是个右值表达式，永远调用右值版本的print    std::cout &lt;&lt; &quot;======================&quot; &lt;&lt; std::endl;}int main(int argc, char * argv[]){    int x = 1;    testForward(x); //实参为左值    testForward(std::move(x)); //实参为右值}</code></pre><p>运行结果：</p><pre><code>Lvalue refLvalue ref //完美转发Rvalue ref======================Lvalue refRvalue ref //完美转发Rvalue ref======================</code></pre><p>【重点区分”值类别“和”数据类型“】<mark>万能引用只负责”能同时正确地接收左值和右值“，其保留了”类型信息“，但丢失了”值类别信息“。如果函数需要对这些参数进行”转发“——即该函数本身不”消费“（存储&#x2F;拷贝），只是负责将这些参数“转交”给目标函数进行处理，则会导致无法调用到以右值引用为形参的函数</mark>。这是因为，在多层函数调用的过程中，由于<strong>函数参数一定是具名的</strong>，其表达式始终是左值，这就会<strong>导致值类别信息在使用时丢失</strong>，被一视同仁地处理为一个左值（比如上面的testForward函数）</p><p>而<strong>完美转发通过static_cast&lt; T&amp;&amp; &gt;在编译期基于模板T，将表达式原有的值类别回复，保证参数”在函数内部不丢失值类型并正确传递“。</strong>【一定要基于模板】</p><br><p><mark>完美转发的实现原理：</mark></p><p><strong>通过模板参数T的信息以及static_cast&lt; T&amp;&amp; &gt;来进行值语义的恢复。</strong> 如果T推导为引用类型，比如传入num推导为int&amp;，那么由于引用折叠就会恢复为左值；如果T推导为非引用类型，比如传入10推导为int，那么就会恢复为右值。</p><p>需要注意的是，<strong>std::forward 本身不会“判断值类别”，它只是根据模板参数 T 来“执行转换”，而 T 是否正确，完全取决于是否发生了调用点的类型推导。这就意味着，完美转发的格式必须是：</strong></p><pre><code>template&lt;typename T&gt;void func(T&amp;&amp; param)</code></pre><p><strong>此外，我们传给forward的一定是一个左值表达式，而不是右值表达式。因为forward的设计初衷就是”根据已经退化为左值的参数“，输出”恢复后的表达式“并进行正确转发</strong></p><pre><code>// 1️⃣ 正常版本：接左值template&lt;class T&gt;T&amp;&amp; forward(std::remove_reference_t&lt;T&gt;&amp; arg){    return static_cast&lt;T&amp;&amp;&gt;(arg);}//remove_referencce是为了移除引用 让参数稳定接收一个左值// 2️⃣ 禁用版本：接右值template&lt;class T&gt;T&amp;&amp; forward(std::remove_reference_t&lt;T&gt;&amp;&amp; arg) = delete;</code></pre><p>实际上，标准库是绝对禁止传右值的，比如std::forward&lt; int &gt;(10)，其并不符合forward的设计语义，forward是为了恢复原始值的类别，其根本没有原始来源</p><br><p><strong>lambda、bind、function是否支持万能引用&#x2F;完美转发？</strong></p><p>bind只支持值拷贝，所以不可能支持完美转发。</p><p>在C++14及之后，模板lambda支持完美转发，相比于bind，bind内部在构造可调用对象时，就会把参数存储起来，此时直接变为了左值，失去了值类别信息。而lambda是在调用时接收参数，<strong>可以在调用时进行类型推导</strong>，没有丢失值类别信息，从而可以实现完美转发，而auto&amp;&amp;则是一个万能引用</p><pre><code>auto f = [](auto&amp;&amp; s) {     foo(std::forward&lt;decltype(s)&gt;(s)); }; f(std::string(&quot;hello&quot;)); // ✅ rvalue</code></pre><p>function不支持完美转发。比如lambda用function存储起来，则又会导致无法完美转发，核心是因为std::function作为一个模板类，<strong>std::forward的模板参数在std::function实例化时已经固定，无法根据调用时实参变化来推导，调整转发行为</strong>；而完美转发是依赖于模板参数在<strong>调用点</strong>的推导，使得std::forward<T> 能根据 T 的不同（值或引用）生成不同的转换，从而保留值类别。</p><p>具体来说，以下面代码为例子，在 std::function&lt; void(std::string)&gt; 中，<strong>模板参数 Args… 在实例化时已经固定为 std::string，因此 std::forward<Args>(args) 始终展开为 static_cast&lt;std::string&amp;&amp; &gt;(args)，不会随着调用时传入左值或右值而变化。</strong></p><p>而在真正的完美转发中，std::forward<T>(args) 中的 T 是在函数调用时根据实参推导得到的：</p><ul><li>如果传入左值，则 T &#x3D; std::string&amp;，forward 为左值</li><li>如果传入右值，则 T &#x3D; std::string，forward 为右值</li></ul><p>因此完美转发的关键在于 <strong>T 的推导发生在调用点，而不是在模板实例化时固定。这一点对于万能引用也是同样的</strong></p><pre><code>std::function&lt;void(std::string)&gt; f = [](std::string&amp;&amp; s) {    foo(std::move(s));};f(std::string(&quot;hello&quot;)); // ❗行为已经变化template&lt;typename R, typename... Args&gt;class MyFunction&lt;R(Args...)&gt;  //对函数类型的模板偏特化{    //.....    R operator()(Args... args)  // 通过callable中的func进行实际调用    {        if (ptr) {            return ptr-&gt;call(std::forward&lt;Args&gt;(args)...);        }    }    //.....}    </code></pre><br><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>通过对移动语义的深入探讨，我们看到了C++如何通过一套精巧的类型系统，在保持值语义本质的同时，大幅优化了对象传递的效率：</p><ul><li><p>右值引用为移动语义提供了语言基础，让“窃取”临时对象资源成为可能</p></li><li><p>移动构造&#x2F;赋值函数实现了资源所有权的低成本转移</p></li><li><p>完美转发解决了多层函数调用中的值类别保持问题</p></li><li><p>RVO&#x2F;NRVO在编译器层面进一步优化了返回值的创建</p></li></ul><p>这套机制共同构成了现代C++高效值传递的基础设施。<strong>然而，移动语义的价值最终要落实到具体的函数设计中——我们如何在实际编程中充分利用这些机制，设计出既高效又易用的接口？</strong> 碍于篇幅，这个问题会在下一篇文章中来回答</p>]]></content>
      
      
      <categories>
          
          <category> 内存分配与管理 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>【内存分配与管理】RAII与智能指针</title>
      <link href="/posts/RAII.html"/>
      <url>/posts/RAII.html</url>
      
        <content type="html"><![CDATA[<p>在上一篇文章中，我们探讨了C++为何选择值语义作为其核心设计哲学，以及这种选择如何深刻影响了C++的内存模型。我们看到了值语义带来的高效性，但也意识到并非所有对象都能简单地“被复制”——线程、文件、网络连接等资源天然具有唯一性，必须通过引用语义来管理。</p><p>这引出了一个更深层的问题：当C++程序员不得不使用引用语义（堆上对象）时，如何避免值语义所不具备的那些内存管理复杂性——内存泄漏、悬空指针、双重释放？</p><p>答案就在C++另一项核心思想中：RAII（Resource Acquisition Is Initialization）。</p><p>本文将探讨RAII如何将资源生命周期绑定到对象生命周期，以及智能指针如何通过明确的所有权模型，让堆上资源也能享受到类栈的自动管理。</p><h1 id="RAII思想"><a href="#RAII思想" class="headerlink" title="RAII思想"></a>RAII思想</h1><p><strong>为什么需要RAII？</strong></p><p>在没有RAII的情况下，有以下问题：</p><ul><li><p>资源所有权不清晰，很容易出现资源泄露或double free的问题，比如类中有裸指针成员</p></li><li><p>资源释放路径不唯一，函数退出的方式太多，比如return、分支内提前return、抛异常等等，在代码复杂的情况下，无法完全保证每条路径都正确地释放资源，可能没释放，也可能多次释放</p></li><li><p>存在异常安全问题。如果程序抛异常，后续代码没法执行到，导致资源未释放。虽然程序会栈展开，会调用对象的析构函数，但是析构函数内并没有将资源释放</p><pre><code>void f() {   int*p = new int{3};   int error = doSomething(p);   if (error) {       delete p; //释放内存，当出现错误的时候       return;    }   finalize(p);   delete p;}</code></pre></li></ul><p><strong>RAII如何解决该问题？</strong></p><p>RAII（Resources acquisition is initialization），意为资源获取即通过构造函数初始化，并通过析构函数释放资源。<strong>其核心思想是把“资源的生命周期”绑定到“对象的生命周期”，并借助析构函数的必然执行，来保证资源一定被释放。而其具体实践unique_ptr&#x2F;shared_ptr还起到了明确资源所有权的作用，解决资源生命周期由谁管理的问题。</strong></p><p>比如，将资源绑定到一个局部变量上，<strong>RAII将“资源对象的生命周期问题”转换为了”栈对象生命周期问题“</strong>。其之所以保证资源不泄露，核心是不再依赖于程序员手动控制，而是依赖于语言的两个机制——一是<mark>作用域</mark>结束一定调用对象的析构函数，编译器会在每个“可能离开作用域的点”插入析构逻辑，比如函数结尾、return等，即解决了“资源释放路径不唯一”的问题，统一由编译器完成；二是<mark>异常</mark>会触发栈展开，编译器会维护一张“清理表”，每构造一个对象就注册一个析构动作，并在离开作用域时倒序执行，调用析构函数，释放资源，即解决了“异常安全问题”</p><p>怎么体现的资源所有权归属？具体看下一节unique_ptr中的内容</p><p><strong>那RAII一定可以保证资源不泄露吗？</strong> </p><p>不一定。假如程序没有正常结束，比如std::terminate()、kill -9杀死进行，析构函数根本不会执行，就会导致RAII失效。或者，如果析构函数本身有问题，比如析构函数可能抛出异常、资源释放不完整等等</p><pre><code>void f() {    Wrapper w;    std::terminate();  // 或 std::abort()}</code></pre><p><strong>RAII的缺点有哪些？</strong></p><p>RAII只是基于对象生命周期对资源进行管理，只关心对象何时离开作用域，<strong>其不会分析资源的所有权关系，可能导致资源被提前释放或循环引用问题</strong>。相比之下，Java&#x2F;C#下的GC式管理则从“根”触出发，遍历整个对象图，找出“不可达”对象，其保证真正没人能访问的资源才被释放。但是，<strong>GC存在运行时开销，且资源释放具有不确定性，可能导致程序运行不稳定性</strong>，而RAII则不存在这些问题，其性能更优</p><h1 id="智能指针"><a href="#智能指针" class="headerlink" title="智能指针"></a>智能指针</h1><p>RAII是指导思想，而智能指针是一种具体的实现方式。</p><h2 id="unique-ptr"><a href="#unique-ptr" class="headerlink" title="unique_ptr"></a>unique_ptr</h2><p>unique_ptr会独占资源的所有权，即两个unique_ptr不能同时指向同一个资源对象。该<strong>唯一所有权通过”不可拷贝，只可通过移动转移资源所有权“来体现出来</strong>，其底层将拷贝构造函数和赋值运算符重载 &#x3D; delete删除</p><pre><code>std::unique_ptr&lt;A&gt; a1(new A());std::unique_ptr&lt;A&gt; a2 = a1 //编译报错 不允许复制std::unique_ptr&lt;A&gt; a3 = std::move(a1) //转移资源所有权 a1不再持有资源对象 </code></pre><p><mark>使用方式：</p><p></mark>通过-&gt;调用指针原有的方法，而<code>.</code> 则表示调用智能指针本身的方法：</p><ul><li><p>get()：获取原生指针，不推荐使用</p></li><li><p>bool()：判断是否拥有原生指针</p></li><li><p>release()：失去资源对象所有权，返回原生指针，但不销毁原生指针</p></li><li><p>reset()：释放资源、销毁指针。如果参数为一个新指针，则将继续管理该新指针</p></li></ul><p>样例：</p><pre><code>std::unique_ptr&lt;A&gt; a1(new A());A* origin = a1.get();if(a1){}std::unique_ptr a2(a1.release()); //转移资源所有权a2.reset(new A()); //释放资源 销毁指针 并重新管理新资源a2.reset(); //释放资源 销毁指针a2 = nullptr;</code></pre><p>站在和shared_ptr对比的视角来看，其优点：</p><ul><li>零额外开销，性能接近裸指针</li></ul><p>实际开发中，优先使用unique_ptr，只有在确实需要共享资源时再使用unique_ptr。</p><p>站在RAII目标的视角来看，其优点：</p><ul><li><p>明确了资源的所有权，生命周期易管理，大大降低资源泄露的可能性，这是智能指针最具价值的一点</p></li><li><p>把“资源的生命周期”绑定到“对象的生命周期”，在对象析构时自动释放资源，避免了忘记delete这种错误。此外，搭配上异常的栈展开机制，更好地保证异常安全</p></li></ul><p><strong>对于第一点：unique_ptr最大价值在于对资源所有权的掌控，大大降低了内存泄露的风险，并增强了代码的可维护性和可读性。</strong></p><p>比如，在使用裸指针的代码中，你看到一个函数签名 voidProcess(MyObj* obj)时，你完全没法判断这个函数会完全接管obj并负责删除释放它，还是只是读取&#x2F;修改资源，很容易导致资源泄露或是double free。<strong>而使用unique_ptr做参数——void process(std::unique_ptr&lt; MyClass&gt; obj)可以明确地表示，当前函数要接管当前资源的所有权</strong>，而调用者则必须使用std::move将资源所有权转让，不需要再操心该资源的生命周期，而是完全由函数内部控制。所以，unique_ptr将模糊的注释&#x2F;约定提升为了由编译器强制检查的一部分，大大提升了代码的可维护性，减少了资源泄露的可能性（代码语义不明确，同样是导致容易出现资源泄露的源头）。</p><p>再比如，一个类持有一个裸指针，你怎么防止出现空悬指针？怎么知道在释放其指向的资源时，没有其他指针依然在引用该资源？而<strong>使用unique_ptr可以非常明确地知道，该资源只属于该对象</strong>，不会被其他对象共享，从而保证资源的正确释放</p><pre><code>class TextureManager{   std::unique_ptr&lt;Texture&gt; textures;};</code></pre><p>那是不是说，裸指针没用了呢？</p><p><strong>智能指针并不是裸指针的完美替代品，应该只在需要转移资源所有权时才使用智能指针。</strong> 具体来说，<mark>通过unique_ptr表达资源所有权的转移（onwership），通过引用&#x2F;裸指针表达借用（borrowing）</mark>。比如，工厂函数创建对象时，显然应该转移资源的所有权：</p><pre><code>std::unique_ptr&lt;Widget&gt; createWidget() {    return std::make_unique&lt;Widget&gt;();}</code></pre><p>而对于一些只读处理，或只是修改对象的状态，则应该用裸指针。这一点，已经在上一篇文章中提到过了。</p><h2 id="shared-ptr"><a href="#shared-ptr" class="headerlink" title="shared_ptr"></a>shared_ptr</h2><p>shared_ptr强调共享资源所有权，多个shared_ptr可以拥有同一个原生指针。其内部通过引用计数的方式来管理，在引用计数为0时才会释放资源。</p><p><mark>常用方法：</mark></p><ul><li><p>get()：获取原生指针，不推荐使用</p></li><li><p>bool()：判断是否拥有原生指针</p></li><li><p>reset()：释放资源、销毁指针。如果参数为一个新指针，则将继续管理该新指针</p></li><li><p>unique()：如果引用计数为1，则返回true，否则返回false</p></li><li><p>use_count()：返回引用计数的大小</p></li></ul><p><mark>应用场景：</mark></p><p>一份资源需要被多个对象管理时，比如多个任务需要同一份资源，并且在任务结束时，由于无法确定哪个任务先结束，所以无法确定哪个任务会最后释放该资源，为了避免提前释放或遗漏释放，此时可以通过shared_ptr对该资源进行管理。这个任务可能是线程、回调或是协程</p><p><mark>缺点：</mark></p><ul><li><p>开销更大。一方面是需要额外的<strong>内存开销</strong>，用于存储指向控制块的指针，控制块中会存储引用计数、weak_count、删除器等；另一方面是<strong>性能开销</strong>更大，在多线程环境下，对引用计数的加减操作都是原子的，比裸指针、unique_ptr慢得多，此外，在析构时涉及到一次虚函数的调用（类型擦除）</p><p><img src="https://img.aixheee.top/4204f464-5665-4ebd-af86-64212a8c9fe5.png"></p></li><li><p>相比于unique_ptr，其资源所有权不明确。这是从设计层面出发的，由于多个shared_ptr都共享资源的所有权，所以资源的生命周期不好管理，容易隐式地延长对象的生命周期（在make_share初始化的情况下更明显），不好调试。</p></li></ul><p><mark>底层原理：</mark>简化的结构：shared_ptr本身拥有一个指向资源对象的指针和一个指向控制块的指针，控制块中有强引用计数、弱引用计数、删除器以及指向资源对象的指针。</p><pre><code>class shared_ptr {    T* ptr;    ControlBlock* ctrl;};struct ControlBlock {    std::atomic&lt;long&gt; shared_count; // 强引用计数    std::atomic&lt;long&gt; weak_count;   // 弱引用计数    void (*deleter)(void*);         // 删除器    void* ptr;                      // 指向对象    // 也可能有 allocator、类型擦除等};</code></pre><p>其中，<strong>对于引用计数的修改通过std::atomic来保证是原子的，可以保证线程安全，避免多线程下拷贝&#x2F;析构时的的计数错误。</strong> 比如线程A和线程B同时读取到引用计数准备+1，最终只加上了一次，进而导致资源被提前释放</p><p><strong>之所以还要有弱引用计数，是因为需要通过弱引用计数来管理“控制块本身”的生命周期，防止weak_ptr访问控制块时访问到已释放的内容，出现未定义行为。</strong> 具体来说，当shared_ptr全部释放，强引用计数归零，资源对象被释放后，weak_ptr是不知道的，需要通过lock()或expired()访问控制块，检查shared_count是否大于0。<strong>总结一下，强引用计数控制资源对象的生命周期，弱引用计数控制控制块的生命周期</strong></p><p><strong>shared_ptr是否是线程安全的？</strong></p><p>shared_ptr只有引用计数是线程安全的，允许在多线程的情况下同时拷贝&#x2F;析构shared_ptr，但是操作shared_ptr指向的对象，比如两个线程同时修改资源对象中的值；此外，修改shared_ptr本身并不是线程安全的，比如多线程同时修改shared_ptr资源指针的指向。</p><pre><code>//对资源对象进行写操作std::shared_ptr&lt;int&gt; p = std::make_shared&lt;int&gt;(0);// 线程1(*p)++;// 线程2(*p)++;//对shared_ptr本身std::shared_ptr&lt;T&gt; p;// 线程1p = std::make_shared&lt;T&gt;();// 线程2p.reset();</code></pre><p>之所以不保证线程安全，是出于性能考虑的，shared_ptr本意是要解决资源管理问题，而不是并发访问问题，线程安全需要由用户自己控制，比如加锁或者使用原子类型</p><h2 id="weak-ptr"><a href="#weak-ptr" class="headerlink" title="weak_ptr"></a>weak_ptr</h2><p>weak_ptr不拥有资源的所有权，指向shared_ptr所管理的资源对象，但不影响资源对象的生命周期，只会增加弱引用计数。<strong>主要应用于：不拥有资源所有权，又需要安全访问资源对象的场景。</strong></p><p>比如在<strong>观察者模式</strong>中，存在“事件源”和”观察者“的关系，如果观察者被销毁，但事件源扔持有观察者的强引用，就会导致悬空指针，此时就需要通过weak_ptr安全地检查观察者是否存在；</p><p>在<strong>缓存系统</strong>中，可以使用weak_ptr实现“弱引用”缓存，当缓存项被其他强引用持有时，其不会被立即清除，提高重复获取效率，但如果没有其他强引用，则缓存失效，可及时释放资源</p><p>在<strong>异步执行任务</strong>时，在执行的一系列Task时，我们还需要观察这些TAsk的执行状态，比如放在一个容器中观察，但是该容器不应该拥有Task本身，<strong>Task的生命周期不应该被一个观察者所改变</strong>，所以容器中应该使用weak_ptr来安全地访问Task对象</p><p>常用方法：</p><ul><li><p>创建：必须依赖于shared_ptr</p></li><li><p>expired()：判断所指向的原生指针是否被释放，如果被释放则返回true，否则返回false</p></li><li><p>use_count()：返回原生指针的引用计数</p></li><li><p>lock()：由于weak_ptr没有资源所有权，所以没有重载*和-&gt;，只能通过lock()获取到当前资源对应的shared_ptr。如果weak_ptr管理的资源没被释放，则返回一个shared_ptr，通过该shared_ptr进行资源管理，同时引用计数+1；如果被释放了，则返回nullptr</p></li><li><p>reset()：将本身指向置空</p></li></ul><p>样例：</p><pre><code>std::shared_ptr&lt;A&gt; a1(new A());std::weak_ptr&lt;A&gt; weak_a1 = a1;//不增加引用计数if(weak_a1.expired()){    //如果为true，weak_a1对应的原生指针已经被释放了}long a1_use_count = weak_a1.use_count();//引用计数数量if(std::shared_ptr&lt;A&gt; shared_a = weak_a1.lock()){    //此时可以通过shared_a进行原生指针的方法调用}weak_a1.reset();//将weak_a1置空</code></pre><p><strong>为什么weak_ptr要有lock()操作？</strong> 因为weak_ptr不拥有资源对象，不能保证资源对象的存货。如果先用expired()方法判断再访问，再多线程环境下会产生竞态条件。具体来说，比如线程A通过expired()判断资源依然有效，但是线程B反手就把资源释放了，此时再切换回线程A，再继续访问已释放的对象，即产生未定义行为。<strong>核心问题是，检查和使用不是原子操作。</strong> 而lock()则提供了一个原子操作：在对象仍然存在时安全地将 weak_ptr 转换为 shared_ptr，不存在时shared_ptr为空，从而保证访问安全。</p><pre><code>if (!wp.expired()) {    //错误的做法！！！    wp.lock()-&gt;DoSomething();}auto sp = wp.lock();    //正确的做法！if (sp) {    sp-&gt;DoSomething();}</code></pre><p><strong>为什么可以解决循环引用的问题？</strong></p><p>weak_ptr并不参与资源所有权的管理，不会增加强引用计数，从而不会形成强引用的闭环。weak_ptr将整个循环结构中的一条强引用改为了弱引用，打破了shared_ptr之间互相拥有的关系</p><p>具体来说：【weak_ptr和shared_ptr的交互工作原理】shared_ptr与 weak_ptr 通过<strong>共享同一个控制块协同工作</strong>：控制块中维护<strong>强引用计数和弱引用计数</strong>。shared_ptr 负责对象的所有权管理，每次拷贝增加强引用计数，析构减少强引用计数，当强引用计数归零时销毁资源对象，<strong>但保留控制块，以防weak_ptr访问空对象</strong>；weak_ptr 仅作为观察者，拷贝时只增加弱引用计数，<strong>不影响对象生命周期</strong>。当需要访问对象时，weak_ptr 通过 lock()原子地检查对象是否仍存在，并在存在时提升为 shared_ptr（增加强引用计数），从而实现安全访问；当强弱引用计数都归零时，控制块才被释放。</p><p><mark>weak_ptr的应用场景：</mark></p><p><strong>对于类似“shared_ptr但有可能空悬的指针”使用weak_ptr。</strong></p><p>比如实现缓存：假设有一个Widget类，我们需要从磁盘上加载该对象，这个过程涉及到IO，比较耗时。为了避免每次使用到该资源都进行IO，所以希望Widget对象可以缓存起来，<strong>如果Widget对象还存在于内存中在被使用着，则不需要重新加载；在Widget对象不再被使用时，缓存自动失效，不阻止对象被销毁，避免内存泄露。</strong></p><pre><code>std::shared_ptr&lt;Widget&gt; fastLoadWidget(int id) {    static std::unordered_map&lt;int, std::weak_ptr&lt;Widget&gt;&gt; cache;    auto objPtr = cache[id].lock();     if (!objPtr) {        objPtr = loadWidgetFromFile(id);        cache[id] = objPtr; // use std::shared_ptr to construct std::weak_ptr    }    return objPtr;}</code></pre><p>之所以unordered_map不直接存储shared_ptr，是因为这会导致缓存中的对象永远不会被销毁并释放，除非手动检查引用计数并清理。而使用weak_ptr，可以在资源对象不被shared_ptr引用时，自动被释放。</p><p>此外，<strong>使用weak_ptr还可以避免裸指针的悬空引用问题，在通过lock()尝试获取缓存的资源时，如果资源已经被释放了，则会返回nullptr</strong></p><p>观察者列表：</p><p>比如观察者模式中，被观察者持有指向观察者的指针，以在发生变化时可以通知到观察者。<strong>被观察者虽然不关心观察者的生命周期，但是如果观察者析构后，需要保证不会再去访问它。</strong> 这种场景下，合理的设计为，被观察者持有一个容器来存储指向观察者的weak_ptr，并在通知观察者前，先就检查是否悬空</p><p>循环引用：</p><p>比如双向链表的场景下，通过shared_ptr来管理节点资源，会因为循环引用而导致资源泄漏。比如，右侧节点资源的释放，需要等待左侧节点资源释放，让next指针置空，而左侧节点资源的释放，需要等待右侧节点资源释放，让prev指针置空。</p><pre><code>struct ListNode {     int _data;     std::shared_ptr&lt;Listnode&gt; _next;     std::shared_ptr&lt;ListNode&gt; _prev; };</code></pre><p><img src="https://img.aixheee.top/28d36f2f-1f76-46a8-ba70-9bf13eca0867.png"></p><p>通过weak_ptr解决：weak_ptr引用资源时，不会增加引用了同一个资源的shared_ptr的引用计数，_next和_prev不参与资源释放管理逻辑，打破循环引用</p><pre><code>struct ListNode{    int _data;    std::shared_ptr&lt;Listnode&gt; _next;    std::shared_ptr&lt;ListNode&gt; _prev;  };</code></pre><h1 id="智能指针的使用方式"><a href="#智能指针的使用方式" class="headerlink" title="智能指针的使用方式"></a>智能指针的使用方式</h1><p><mark>make_shared</mark>：</p><p>总的来说，绝大多数情况下使用make_shared来构造智能指针对象，其通过可变参数模板+完美转发将一个任意实参集合转发给动态分配内存的对象的构造函数，将对象和控制块合并为一次内存分配，从而<strong>减少分配开销，增强局部缓存性，并增强异常安全</strong>，但是<strong>代价是对象和控制块的生命周期绑定在了一起，可能导致内存延迟释放，不推荐对大对象使用make_shared构造</strong></p><p>具体来说，make_shared的优点在于：</p><ul><li><p>一是因为其将“对象+控制块”合并为一次内存分配，而通过new的方式则是两次内存分配，new&#x2F;malloc是昂贵的操作，可能导致<strong>系统调用、锁竞争</strong>（多线程），且一次内存分配不易产生<strong>内存碎片</strong>。</p></li><li><p>二是因为“对象+控制块”二者存储在一块连续的内存中，在访问对象时，大概率会顺便将控制块一起加载到cache line中，缓存局部性更好。</p><pre><code>make_shared:[控制块 + 对象] 连续普通写法:[控制块]   [对象] （分散）</code></pre></li><li><p>三是因为make_shared是<strong>异常安全</strong>的。比如下面的场景，在C++17之前，<strong>函数参数的计算顺序是不固定</strong>的，有可能是new int(10)-&gt;bar()-&gt;构造shared_ptr。如果bar()抛异常，会导致shared_ptr没有构造成功，进而内存泄露，因为原生指针没有被管理起来。C++17之后不存在该问题，虽然顺序不固定，但是禁止交错，每个参数表达式的完整求值，必须在另一个开始求值之前完成</p><pre><code>//错误做法foo(std::shared_ptr&lt;int&gt;(new int(10)), bar());</code></pre><p>而mask_shared要么完全失败，要么完全成功，没有中间状态，其是一次性分配内存，并且如果任何一步出现异常，都会自动释放申请的所有资源</p><pre><code>foo(make_shared&lt;int&gt;(10), bar());</code></pre><p>但<strong>如果要自定义删除器，正确的方法是将new语句单独提出来，保证new构造对象和通过智能指针托管资源这两件事一起做完，不会被异常打断</strong></p><pre><code>//正确做法：保证异常安全shared_ptr&lt;int&gt; sp(new int(10), myDel);foo(sp, bar());</code></pre></li></ul><p>make_shared的缺点在于：</p><ul><li><p><strong>不提供自定义删除器。</strong> 这是因为资源对象“内嵌”在控制块中，而不是像非make_shared构造时为两块独立的内存块，<strong>所以没法只对一整块内存中的一小部分释放，释放时必须整体释放这块内存。</strong> 进而不能将释放策略交给用户，用户并不清楚如何完整地释放内存块。（allocate_shared同理）</p></li><li><p><strong>由于控制块和对象是绑定的，所以只要控制块还活着，对象本地占用的内存就不会释放</strong>（注意区分是对象本地占用的内存，还是对象管理的资源，比如vector本地占用的内存并不大，但是其管理的资源可能很大，其管理的资源会在其析构函数中被释放）。比如<strong>在waek_ptr延长内存引用的情况下，可能导致一整块内存无法释放</strong>，所以对于本地内存占用较大的对象，更推荐使用new的方式初始化智能指针：</p><pre><code>// 如果需要 weak_ptr 长期存在但想及时释放大对象auto sp = std::shared_ptr&lt;BigObject&gt;(new BigObject());std::weak_ptr&lt;BigObject&gt; wp = sp;sp.reset();  // BigObject析构，控制块还在，但对象内存立即释放// 内存占用：只有小的控制块，没有BigObject的大内存</code></pre></li><li><p><strong>make_shared&#x2F;allocate_shared会绕过类自定义的operator new&#x2F;delete</strong>，因为其会直接通过全局通用版本的operator new&#x2F;delete分配一块同时包含控制块和资源对象的内存，而不是通过类自身的内存分配&#x2F;释放逻辑</p></li><li><p><strong>支持{}列表初始化不方便</strong>：无法直接支持{}初始化列表来进行初始化，make_shared中使用的是()初始化。如果非要使用{}初始化，调用initializer_list构造函数，则可以先创建一个initializer_list对象再传给make_shared：</p><pre><code>//创建initializer_list对象auto initList = {10, 20};//调用到vector中initializer_list参数类型的构造函数auto sp = make_shared&lt;vector&lt;int&gt;&gt;(initList);</code></pre></li></ul><p><mark>自定义删除器</mark>：</p><p>我们可以指定自定义的删除器在释放资源时执行一些特殊的操作，比如资源释放时<strong>打印日志</strong>；<strong>管理除了内存以外的资源</strong>，比如FILE*、mutex等；或与<strong>自定义分配器（allocator）</strong> 相互配合使用。<strong>删除器本质上是一个可以调用的对象，比如函数指针、lambda表达式、仿函数等。</strong> 默认是通过delete&#x2F;delete[]，后者是通过模板特化来实现的。而删除器又分为<strong>有状态删除器</strong>和<strong>无状态删除器</strong>。无状态删除器内部不保存任何数据成员，其大小为0（空类优化），不需要额外的存储开销，比如没有捕获任何变量的lambda表达式：</p><pre><code>auto deleter = [](int* p) {    delete p;};struct Deleter {    void operator()(int* p) const {        delete p;    }};</code></pre><p>有状态删除器内部会持有额外的数据，每个删除器的实例可能不同，这会增加智能指针的大小：</p><pre><code>int x = 42;auto deleter = [x](int* p) {    std::cout &lt;&lt; x;    delete p;};struct Deleter {    int id;    Deleter(int i) : id(i) {}    void operator()(int* p) const {        std::cout &lt;&lt; &quot;delete by &quot; &lt;&lt; id &lt;&lt; &quot;\n&quot;;        delete p;    }};</code></pre><p><strong>那删除器的类型对智能指针有什么影响吗？</strong></p><p>unique_ptr是通过<strong>模板推导</strong>的方式实例化对应的删除器，<strong>删除器属于unique_ptr类型的一部分</strong>，会在类中直接保存删除器本身。</p><pre><code>std::unique_ptr&lt;T, Deleter&gt; p(new T, deleter);template&lt;class T, class Deleter&gt;class unique_ptr {    T* ptr;    Deleter deleter;};</code></pre><p>之所以这样设计：</p><ul><li><p>零额外开销（理论）：如果删除器是无状态的，那么编译器可以用空基类优化，unique_ptr相比于裸指针没有任何额外内存成本。不过，由于unique_ptr是非平凡类型，函数传参的过程中没法放到寄存器中，需要通过栈，所以会有些许额外的开销</p></li><li><p>不需要共享：unique_ptr独占资源所有权，不需要控制块，所以不需要跨对象共享删除器，所以可以放心地把删除器放在对象中</p></li></ul><p>但缺点是，<strong>删除器如果有状态，会直接影响到unique_ptr本身的大小</strong></p><p>对于shared_ptr来说，删除器存储在控制块中，shared_ptr通过指针引用控制块，删除器并不属于shared_ptr类型的一部分</p><pre><code>std::shared_ptr&lt;T&gt; p(new T, deleter);class shared_ptr {    T* ptr;    ControlBlock* ctrl;};struct ControlBlock {    int shared_count;    int weak_count;    void (*deleter)(T*); // 实际更复杂（类型擦除）};</code></pre><p>之所以这样设计，是因为：</p><ul><li><p>shared_ptr需要进行类型擦除，让”带不同deleter构造出来的shared_ptr“在类型系统看来是同一种类型，以<strong>支持多个shared_ptr对同一资源进行管理</strong>如果没有类型擦除：</p><pre><code>auto p1 = shared_ptr&lt;int, D1&gt;(...); auto p2 = p1; // OK shared_ptr&lt;int, D2&gt; p3 = p1; // ❌ 无法转换</code></pre><p>有类型擦除：尽管删除器类型不同，但是允许重新赋值，共享管理一块资源</p><pre><code>auto p1 = std::shared_ptr&lt;int&gt;(new int, D1{}); std::shared_ptr&lt;int&gt; p2 = p1; // ✅ OK</code></pre></li><li><p>多个shared_ptr必须共享同一个删除器，如果不放在堆上的控制块中，让每个shared_ptr都拥有自己的专属的删除器，就无法保证读个shared_ptr在对象销毁时行为是一致的</p></li></ul><p>其缺点是，有虚函数调用+额外的控制块</p><p><mark>operator bool类型转换：</mark></p><p>shared_ptr和unique_ptr都支持了operator bool的类型转换，如果智能指针对象没有管理资源，则返回false，否则返回true。</p><pre><code>if(ptr) //合法判断</code></pre><p><mark>智能指针unique_ptr作为参数传递：</mark></p><p><strong>按值传递callee(unique_ptr&lt; Widget&gt; smart_w)：</strong> 需要通过std::move()转移资源所有权。这是最常见的使用方式，代表着要转移资源的所有权</p><p><strong>按引用传递callee(unique_ptr&lt; Widget&gt; &amp;smart_w)：</strong> 基本不太可能出现。因为按引用传递有两方面优势，一方面是避免拷贝，另一方面是希望函数内修改对象，并在函数返回后，调用者可以看到这个改动的效果。但是，对于unique_ptr而言，不存在拷贝一说，只有移动赋值，且代价较小；而在函数内部，修改unique_ptr中的裸指针的这个行为几乎不存在（如果修改的是资源对象本身，则更不需要传引用了）。所以，参数引用类型并不常见，其完全可以通过一个裸指针来传递</p><pre><code>void caller(){  unique_ptr&lt;Widget&gt; smart_w = std::make_unique&lt;Widget&gt;();  callee1(smart_w.get());  callee2(smart_w.get());  // 可以继续用smart_w，也可以随便抛出异常}   // guarantee: 当caller退出后，smart_w会销毁Widget资源，不管发生任何异常void callee1(Widget *w){  // use w，同时，可以随便抛出异常}void callee2(Widget *w){  // use w，同时，可以随便抛出异常}callee(const unique_ptr&lt;Widget&gt; &amp;smart_w)callee(const unique_ptr&lt;Widget&gt; &amp;smart_w)</code></pre><p><strong>按常引用传递callee(const unique_ptr&lt; Widget&gt; &amp;smart_w)：</strong> 同样的，意义不大，完全可以用裸指针替代，比如const Widget*或const Widget&amp;</p><p><strong>按右值引用传递callee(unique_ptr&lt; Widget&gt; &amp;&amp;smart_w)：</strong> 对于unique_ptr来说，几乎和按值传递等效</p><h1 id="智能指针常见错误"><a href="#智能指针常见错误" class="headerlink" title="智能指针常见错误"></a>智能指针常见错误</h1><p><mark>将一个原生指针托管给多个智能指针管理。</mark> 无论是unique_ptr还是shared_ptr都不应该这样做，对于shared_ptr而言，会对同一个资源对象创建多个控制块，而多个控制块则意味着多重的引用计数，而多重的引用计数则意味着多重析构</p><pre><code>void incorrect_smart_pointer(){    A *a= new A();    std::unique_ptr&lt;A&gt; unique_ptr_a1(a);    std::unique_ptr&lt;A&gt; unique_ptr_a2(a);// 此处将导致对象的二次释放    std::shared_ptr&lt;A&gt; shared_ptr_a1(a);    std::shared_ptr&lt;A&gt; shared_ptr_a2(a);// 此处将导致对象的二次释放}</code></pre><p>为了避免此类问题，我们应该尽可能避免用裸指针构造智能指针，正确的方式是使用make_shared&#x2F;make_unqiue，如果要求自定义删除器的话，则通过：</p><pre><code>std::shared_ptr&lt;A&gt; shared_ptr_a1(new A(), MyDel);</code></pre><p>接着，如果需要共享资源，则通过拷贝构造的方式作为新shared_ptr的创建蓝图</p><p><mark>将this指针直接托管给shared_ptr（本质上还是问题一），会导致二次释放。</mark> 因为新的shared_ptr在创建时，会创建新的控制块，而新的控制块则意味着新的引用计数，如果有旧的shared_ptr同样也在管理当前对象，但是引用计数却没有共享。这将导致其中一个shared_ptr计数为0后提前释放资源对象，而另一个shared_ptr后续再操作该对象时出现异常</p><pre><code>class E{    std::shared_ptr&lt;E&gt; use_this()    {        //错误方式，用this指针重新构造shared_ptr，将导致二次释放当前对象        return std::shared_ptr&lt;E&gt; this_shared_ptr1(this);    }};std::shared_ptr&lt;E&gt; e = std::make_shared&lt;E&gt;();</code></pre><p>正确的方式是，如果想在类内部获取一个管理当前对象的shared_ptr指针，应该让该类继承enable_shared_from_this，并使用C++提供的shared_from_this()来获取到shared_ptr，<strong>该shared_ptr并不持有新的控制块，而是和旧的shared_ptr共享同一个控制块</strong>，从而避免多次释放的错误。但如果旧的shared_ptr不存在，即当前资源对象并没有与之关联的控制块，则shared_form_this()会抛出异常</p><pre><code>class A : public std::enable_shared_from_this&lt;A&gt; {public:    std::shared_ptr&lt;A&gt; get_shared_ptr() {        return shared_from_this();    }};</code></pre><p>其原理是，会在 enable_shared_from_this 类中维护一个weak_ptr，shared_from_this会将该weak_ptr作为参数传入shared_ptr的构造函数，返回一个shared_ptr对象</p><p><mark>将栈上对象托管给智能指针，会导致二次销毁。</mark>栈上对象在出了作用域后本来就会被自动销毁，用不到智能指针</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>通过对RAII和智能指针的深入分析，我们看到C++如何通过所有权模型来驯服引用语义的复杂性：</p><ul><li><p>unique_ptr明确了独占所有权——“这个资源只属于我，我负责它的生死”</p></li><li><p>shared_ptr明确了共享所有权——“我们共同拥有这个资源，最后一个离开的负责关灯”</p></li><li><p>weak_ptr明确了观察者角色——“我只是看看，不负责管理”</p></li></ul><p>这种所有权模型不仅解决了资源泄漏问题，更在代码层面显式表达了设计意图，将原本靠注释和约定的资源管理规则，提升为编译器可以检查和强制执行的类型系统特性。</p><p>然而，智能指针只是解决了“堆上资源”的管理问题。对于值语义对象本身，C++还有另一个重要的优化方向：<strong>避免不必要的拷贝。</strong> 这就引出了我们下一篇文章的核心话题：<strong>移动语义与拷贝优化</strong>。</p>]]></content>
      
      
      <categories>
          
          <category> 内存分配与管理 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>【内存分配与管理】从值语义出发，理解C++内存管理设计逻辑</title>
      <link href="/posts/zhiyuyi.html"/>
      <url>/posts/zhiyuyi.html</url>
      
        <content type="html"><![CDATA[<p>在具体展开C++内存管理之前，本篇文章将从值语义这一C++设计基石出发，理解C++内存管理的根因缘由。你将明白为什么C++选择这条看似复杂但极致高效的道路，以及这种选择如何深刻影响着从对象生命周期到智能指针设计的每一个角落。</p><h1 id="值语义与C"><a href="#值语义与C" class="headerlink" title="值语义与C++"></a>值语义与C++</h1><p><strong>值语义</strong>是指对象的复制会创建一个独立的副本，每个副本都有自己的数据，修改一个副本不会影响其他副本。简单来说，值语义对象就像是数学中的数值，彼此独立，互不干扰。相对的，<strong>引用语义</strong>指的是对象不直接存储数据，而是持有对数据的引用（指针、引用等），多个对象可以共享相同的数据，修改其中一个会影响其他所有共享该数据的对象。</p><p>C++的设计初衷是让用户定义的类型（class）可以像内置类型一样工作，具有同等的地位，让class本质上是值语义</p><blockquote><p>Bjarne Stroustrup在《C++语言的设计与演化》中多次强调这一点：</p><p>“在C++中，用户定义类型应与内置类型具有相同的支持、相同的语法使用方式和相同的效率。”</p><p>“让用户定义的类型与内置类型一样工作良好的目标，是C++抽象机制设计的中心思想。”</p><p>“C++的类机制允许程序员定义与内置类型在使用方式、行为复杂性和效率方面都难以区分的类型……<strong>这是我设计C++的主要目标</strong>：提供一种工具，使得出色的抽象能够以内置类型一样的代价被使用。”</p><p>                                        ——《What is “Object-Oriented Programming”?》</p></blockquote><p><strong>为什么C++要这样设计？</strong></p><p>C++的诞生背景直接决定了这一点，其是在C语言的基础上进行开发的，而C语言天然就是值语义。当然，这里不是说是特地为了兼容C所以才要保留值语义，而是因为C++和C都在追求高性能，值语义恰恰可以满足这一点，且不止满足这一点——值语义的对象可像内置类型一样，直接分配在栈上，而分配在栈上带来了很多好处：</p><ul><li><p>栈的内存的分配&#x2F;释放都是基于移动栈指针完成的，其效率较高</p></li><li><p>栈在内存上是连续的，不需要指针跳转，可以减少间接访问，提高缓存命中率；</p></li><li><p>栈基于局部作用域来管理对象的生命周期，这让内存泄露可能性降低，并可明确地管理对象的生命周期（而不是像GC一样）</p></li></ul><p>我们似乎体会不到”class本质上就是值语义“这一点，但其实际上无处不在，C++设计的背后也为实现这一点做出了大量努力，甚至可以说大量的C++独有的“特性”都和这一点有关（这里的特性指的是，在C#&#x2F;Java这些语言下你从来不需要考虑的内容）：</p><ul><li><p>C++一直在追求的零开销抽象：class作为值语义对象，其不应该有额外的开销；而C#class对象是引用语义通常需要额外的GC信息，具有额外的开销</p><pre><code>class A{    int x;};sizeof(A) == sizeof(int)</code></pre></li><li><p>class对象作为成员时会直接嵌入</p></li><li><p>class的成员的初始化：class成员默认不会初始化，比如值语义的int从来都是未初始化的；C# class对象则是默认初始化的</p></li><li><p>拷贝构造和拷贝赋值：class对象作为值语义对象，必须支持拷贝，所以C++必须定义”class如何复制“，为我们生成默认按照值语义来进行拷贝的成员函数，也就是常说的”浅拷贝“。但是有些class并不符合值语义的要求，比如File、thraed、socket、mutex等资源（这一点下面再具体展开），如果class中存在这些成员，那么就不能按照值语义来拷贝，而是需要我们自定义规则，即Rule of 5，甚至有的时候都不应该支持拷贝，即 &#x3D;delete。而C#中的class是引用语义，永远都是在复制引用（地址），而不是对象本身复制，所以完全不需要考虑这些东西，但是代价就是一切默认共享</p></li><li><p>传参时需要考虑pass-by-value还是pass-by-const-reference：因为值语义对象相互独立，所以拷贝后需要成为一个独立的对象，比如函数传参int时，永远都是拷贝。而如果class对象是一个大对象，那么可能需要通过pass-by-const-reference来避免复制；而C#的class对象是引用语义，天然避免该问题</p></li><li><p>函数返回值的RVO&#x2F;NRVO优化：同样的，因为值语义对象相互独立，所以对象拷贝后需要成为一个独立的对象，比如函数返回int时，永远都是拷贝。而如果class对象是一个大对象，那么需要相应的优化来尽可能避免这次不必要的拷贝；而C#的class对象是引用语义，天然避免该问题</p></li><li><p>继承会出现切片问题，并且多态必须使用引用&#x2F;指针：在C++中Base b &#x3D; d时，这是真的在创建一个新的Base对象，而不止是一个“访问句柄”，但Base对象的内容里只能存放Base部分，所以Derived部分会被裁掉，即切片问题。而如果Deirved部分被裁掉了，那么自然无法发挥多态的作用，所以C++多态必须用引用&#x2F;指针，Base&amp; b &#x3D; d; 没有创建新对象，只是引用了旧对象，Deirved部分仍然存在，多态才成立而C#中Base b &#x3D; new Derived()则是在让Base引用的指向Derived对象而已，对象本体仍然完整，不会发生切割，也就不需要有引用&#x2F;指针才能使用多态的要求</p><pre><code>class Base{    int a;};class Derived : public Base{    int b;};Derived d;Base b = d;</code></pre></li></ul><h1 id="值语义下的问题"><a href="#值语义下的问题" class="headerlink" title="值语义下的问题"></a>值语义下的问题</h1><p>将class作为值语义固然高效，但实际上会有问题。</p><p>回顾一下值语义的定义：值语义对象的复制会创建一个独立的副本，每个副本都有自己的数据，修改一个副本不会影响其他副本。由于需要独立的副本，那么必然要发生拷贝，<strong>那什么class对象的拷贝才是是合理的？拷贝的开销会不会过大呢？</strong> 这两点直接导致C++复杂度增大。</p><h2 id="问题一：值语义的合理性"><a href="#问题一：值语义的合理性" class="headerlink" title="问题一：值语义的合理性"></a>问题一：值语义的合理性</h2><p>对于第一个问题：并不是所有class都能够按照值语义来解释。比如thread线程，难道拷贝一个thread可以让操作系统增加一个一模一样的线程吗？再比如，一个socket链接，难道拷贝一个socket可以让操作系统再增加一条网络连接吗？类似的例子还有很多，比如file、mutex以及内存资源等。</p><p>于是，C++需要引用语义来描述这些具有唯一实体的class——其不允许复制，多个地方需要通过指针&#x2F;引用作为“句柄”来<strong>共享访问</strong>。而这就意味着这些引用语义的对象需要分配在堆上。而堆上对象特点是，生命周期不受约束、内存布局不受约束，即在时间和空间上都是自由的，其自由背后的代价是，内存分配涉及到系统调用，<strong>分配和释放成本较高</strong>；由于生命周期不受控制，所以可能发生<strong>内存泄露</strong>的问题，并存在内存碎片的问题。</p><p>那为什么引用语义的对象需要分配在堆上呢？这两个缺点C++又是怎么尽可能避免的呢？</p><p>先来回答前者：<strong>这里就引出了一个经典的问题：为什么值语义对象默认在栈上，引用语义对象默认在堆上？</strong> 这是因为，引用语义对象的需要被多处共享，对象需要支持跨作用域存在。比如下面这个例子，tex如果分配在栈上，那么随着作用域的结束，tex的生命周期结束，无法被其他对象共享；而如果tex分配在堆上，则不会再受到作用域的限制。</p><pre><code>Texture* Create(){    Texture tex;    return &amp;tex;}</code></pre><p><strong>总的来说，之所以引用语义对象分配在堆上，是因为栈无法支持这种跨作用生命周期的需求，所以往往借助堆分配来获得独立生命周期。而对于值语义对象来说，其并不需要被共享，也就没有这层限制，其既可以分配在栈上，也可以分配在堆上，而栈的效率更高且生命周期管理更简单，所以默认分配在栈上。值语义引用语义和栈堆分配之间没有必然关系，其实际上是由生命周期管理、效率等各方面需求决定的</strong></p><p>对于后者，缺点之一为———堆内存分配&#x2F;释放成本较高的问题，则需要通过allocator来对堆内存的分配进行优化，比如STL allocator、ptmalloc等，一是在时间上提高分配&#x2F;释放时的效率，减少锁竞争，并尽可能地提高缓存局部性，二是在空间上提高利用率，减少内存碎片问题。ptmalloc的设计已在前几篇文章中展开，对于STL allocator会在后续文章中讲解</p><p>缺点之二为——堆上资源（不止是内存，还有File、thread、socket、mutex等资源）复杂的生命周期问题。其是C++必须投入大量设计精力解决的部分，也是平时我们写代码时直接接触到的</p><p>引用语义相比于值语义：值语义不存在共享对象的概念，资源对象只属于该class自己，对象被分配在栈上，作用域结束，对象生命周期结束，资源对象的生命周期也结束，资源被回收，万事大吉。这十分便捷，就像你从来不会关心一个int变量的生命周期一样，而这实际上就是RAII——将资源的生命周期和对象的生命周期绑定</p><pre><code>void Func(){    std::string s = &quot;abc&quot;;}</code></pre><p><strong>而引用语义下不一样，由于对象被共享，所以此时对象的生命周期不再简单地跟着作用域走，而是取决于最后一个使用它的class。</strong> 为此，我们需要额外考虑许多问题：对于一个裸指针&#x2F;引用，其指向的资源对象什么时候释放？被谁释放？是否是共享的？而由于这些问题的存在，所以才出现了资源泄露（我以为其他class负责释放资源，所以我不释放，结果没有任何一个class释放资源）、悬空指针（我明明还在访问该资源，其他class却给释放了）、double free（我以为其他class都不会释放该资源，所以我来释放，结果其他class同样也释放了）、循环引用等等问题。</p><pre><code>TcpConnection* conn = Create();ModuleA(conn);ModuleB(conn);ModuleC(conn);</code></pre><p><strong>而解决这些问题的根源，便是明确规定一个资源到底由谁来管理，即确定资源的所有权。</strong> 而最简单的资源所有权模型，便是unique_ptr，其明确了该资源对象只由该智能指针对象来管理，将资源包装进值对象中。比如下面的例子，一离开作用域则自动delete，RAII便又回来了。这可以说是上C++最重要的思想之一，比如vector、string、lock_guard等等都遵循该规则</p><pre><code>{    std::unique_ptr&lt;Texture&gt; tex = Load();}</code></pre><p>再比如，让类成员unique_ptr持有资源，明确该资源只属于该类对象，不需要被其他对象共享，从而保证资源的正确释放</p><pre><code>class TextureManager{   std::unique_ptr&lt;Texture&gt; textures;};</code></pre><p>既然提到了unique_ptr，那么不得不再说说shared_ptr：程序中确实存在资源对象需要被共享的情况，比如纹理资源、场景资源等，所以需要shared_ptr，其通过引用计数的方式<strong>尽可能</strong>地保证资源的正确释放。</p><p>但其缺点在于，资源的释放时机不再确定，需要额外的内存空间用于存储控制块，并且存在循环引用，在动态环的场景下束手无策，只能通过类似于GC的思路来解决。这也是为什么推荐优先使用unique_ptr。</p><p><strong>此外，通过以上方式来明确资源所有权的另一个好处在于提高代码的可读性——你完全可以通过局部代码来推导出谁负责管理资源，而这实际上间接地防止了资源泄漏问题。</strong></p><p>考虑这样一个函数，在多人协作的场景下，如果你不去看该函数的具体实现，你完全没法判断这个函数会完全接管obj并负责删除释放它，还是只是读取&#x2F;修改资源，容易导致资源泄露或是double free。这个问题的根源同样是资源所有权——MyObj*无法体现出所有权关系</p><pre><code>void Process(MyObj* obj)</code></pre><p>而在有了智能指针后，完全可以通过其来显示地传递所有权信息——unique_ptr&#x2F;shared_ptr代表我（站在函数的角度）拥有资源的所有权，负责其生命周期，即“拥有者”；</p><pre><code>void Process(std::unique_ptr&lt;MyObj&gt; obj)</code></pre><p>T*&#x2F;T&amp;&#x2F;const T&amp;代表我只是临时访问、修改该对象，但是不负责管理其生命周期，即“借用者”，需要外界来管理其生命周期；weak_ptr同样也是代表访问、修改该对象，且不影响其生命周期，但是其和指针&#x2F;引用的区别在于，weak_ptr可以感知到资源对象是否被释放了（lock()），而指针&#x2F;引用无法感知，其默认资源对象始终有效，完全依赖于外部来保证在调用期间该资源对象不会被释放</p><p>以上是引用语义的问题之一——资源所有权不确定导致的资源生命周期的复杂性。<strong>引用语义的另一个问题，更准确说是特点，是引用语义的对象需要分配在堆上，而分配在堆上就导致了效率的降低。这里的效率，即是缓存局部性导致的访问效率降低，也是分配&#x2F;释放对象时的效率降低。</strong></p><h2 id="问题二：值语义的拷贝代价"><a href="#问题二：值语义的拷贝代价" class="headerlink" title="问题二：值语义的拷贝代价"></a>问题二：值语义的拷贝代价</h2><p><strong>现在，让我们来看看之前提到的第二个问题——值语义所带来的拷贝开销问题。</strong></p><p>为了解决该问题，编译器首先出现了RVO&#x2F;NRVO优化，并在C++17之后RVO被规定为强制优化。RVO&#x2F;NRVO即直接将返回值对象在目标位置上构造，而不需要中间临时对象的参与，减少不必要的拷贝。这使得我们既能够返回值保留值语义，又能有效降低拷贝开销。</p><pre><code>MyClass Func(){    MyClass obj;    return obj;}</code></pre><p>而对于传参时的拷贝代价，则通过引入了移动语义来解决。move的本质是将资源的所有权直接转移，避免了class对象被复制。这个过程中，move依然属于值语义，因为该class对象中的资源对象依然只有一个所有者，只不过所有权被转移了</p><p>而为了能够让move移动语义成立，从而引入了右值引用、万能引用、完美转发等一系列机制，这里不再详细说明它们的底层机制以及彼此间的逻辑关系，后续文章再具体展开</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>总结一下，C++追求class作为值语义对象，其有很多优点，但是现实情况下并不是所有对象都能简单地复制、按照值语义的方式来理解，所以需要引用语义。但是引用语义带来了生命周期的复杂性，资源对象的释放不再简单由作用域决定，而是由引用关系决定，从而出现了资源泄漏等各种问题。而C++便通过资源所有权来解决该问题，产生了unique_ptr&#x2F;shared_ptr等机制，重新通过RAII的思路来管理资源对象。而对于堆分配的缺点，则采用了allocator策略在事件和空间上尽可能地优化。现代C++所有的努力，本质上都是在保证值语义，这也是C++的核心目标之一，其带来了优秀的缓存局部性、确定性的资源管理和较低的运行时开销，但是也引入了复杂的生命周期管理问题以及拷贝问题</p><p>那C#呢？C#的目标是提高开发效率，尽可能降低内存管理的复杂度，所以选择了默认引用语义并通过GC自动追踪引用，大大降低了开发难度，不需要再考虑引用语义下生命周期的问题，但其局部缓存性较差，且GC带来了不确定的析构、运行时较大的额外开销。并且实际上，GC只解决了内存资源的管理，而对于其他资源，C#依然采取了类似于RAII的解决思路，比如using IDisposable等</p><pre><code>using(var file = ...){}</code></pre><p>理解这套哲学，就理解了为什么C++没有选择Java&#x2F;C#那样的“简单”引用语义+GC模型。C++的设计者相信，程序员应该有权在“自动化的便利”和“手动的控制”之间找到最佳平衡点。这种选择让C++变得更复杂，但也让它能够在性能敏感的领域大放异彩。在后续的系列文章中，我们将具体展开C++的内存管理方式。</p>]]></content>
      
      
      <categories>
          
          <category> 内存分配与管理 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>【内存分配与管理】理解new与placement new</title>
      <link href="/posts/new.html"/>
      <url>/posts/new.html</url>
      
        <content type="html"><![CDATA[<p>前三篇文章中，我们已经了解了malloc的底层实现以及与我们平时接触最为密切的的堆栈内存分配，本篇文章将从malloc的底层机制到new的面向对象封装，了解new的设计理念、底层实现以及正确用法</p><h1 id="new表达式"><a href="#new表达式" class="headerlink" title="new表达式"></a>new表达式</h1><p>我们先来从最表层来对比一下malloc与new：<br><strong>1.概念上的区别</strong></p><p>        malloc&#x2F;free是C&#x2F;C++标准库函数，而new&#x2F;delete是C++关键字，或者说是new表达式。因此malloc仅仅只分配内存，而不会进行初始化类成员的工作，new不止分配内存，而且还调用类的构造函数。</p><p><strong>2.返回类型的不同</strong></p><p>        new内存分配成功时候，返回的是对象类型的指针，不需要进行类型转换，从这个角度来说比较安全malloc内存分配成功则返回 void类型（泛型指针），必须通过强制类型转换将 void* 转换成需要的类型。</p><p><strong>3.分配失败后的返回值不同</strong></p><p>        malloc失败，会返回空指针。new失败，默认是抛出异常，要捕获异常bad_alloc</p><p><strong>4.是否需要指定内存大小</strong></p><p>        new操作符申请内存的时候不需要指定内存块的大小，编译器会自动根据类型信息来计算malloc需要显示的指出内存的大小。</p><p>这只是浅层表象，我们该如何理解new呢？</p><h2 id="理解new"><a href="#理解new" class="headerlink" title="理解new"></a>理解new</h2><p><strong>new不仅仅是”申请内存“，本质上是一个将”内存分配“和”对象构造“绑定在一起的语法糖。</strong></p><p>从<strong>底层</strong>来看，其首先调用全局的operator new分配原始内存（通常基于malloc实现），然后通过在这块内存上调用构造函数，对象的生命周期开始。相对的，delete则先调用析构函数结束对象的生命周期，再调用opeartor delete释放内存。</p><p><strong>这背后反应了C++对对象的一个核心理解：对象由”内存存储“和”生命周期“两部分组成，二者在默认情况下是绑定的，目的是提供简单对象语义。</strong></p><p>但是，C++也同样允许我们将二者拆开单独控制，将“内存的生命周期”和”对象的生命周期“解耦，实现更为精细的控制。</p><p>比如，在智能指针make_shared的实现中，对象和控制块是统一分配的，因此内存无法单独释放，但是对象的生命周期需要在强引用归零时结束。此时，就需要通过”字符数组+placement new“将“内存的生命周期”和”对象的生命周期“解耦，并通过显式析构结束对象生命周期。这既保证了及时结束对象生命周期，又不会在释放控制块时导致对资源对象的双重析构</p><p>所以可以说，<strong>new&#x2F;delete 的设计本质是：在提供简单对象语义的同时，也允许程序员在需要时精细控制内存和对象生命周期</strong>，这是 C++ 能实现智能指针、内存池等高级机制的基础。</p><p><strong>现在，我们再来对比一下malloc和new，理解为什么有了malloc&#x2F;free为什么还要new&#x2F;delete？</strong></p><p><strong>从C++面向对象的特点出发：</strong> C语言是面向过程的，其关注的是“数据+操作”，并不严格区分“内存”和“对象”，所以只需要通过malloc&#x2F;free来管理内存的存储期即可。而C++是面向对象的，内存仅仅是对象的一部分，对象还包括构造、析构等生命周期的管理，<strong>需要区分“内存生命周期”和“对象的生命周期”</strong>。<strong>而new&#x2F;delete将内存分配和对象的生命周期绑定起来，做面向对象的封装，从而支持RAII思想与异常安全，符合C++语义要求</strong></p><p>具体来说，RAII要求资源创建即绑定到对象的生命周期中，在构造时申请资源，析构时释放资源，以防止资源的泄露。如果没有new&#x2F;delete，那么用户需要手动malloc+construct+destruct+free，这一流程既繁琐又容易出错，破坏了面向对象的抽象和封装的核心原则，内存管理负担过重</p><p><strong>从异常安全的角度来说：</strong> new&#x2F;delete通过try-catch+回滚析构，保证了在对象构造时能正确调用operator delete来释放申请资源，防止内存泄露。如果没有new&#x2F;delete，那么用户始终都需要手动try-catch来保证对象构造时的异常安全，这使得资源管理逻辑与业务逻辑深度耦合，代码复杂度急剧上升，且难以在所有可能的分支上保证正确的资源释放。</p><h2 id="new底层原理"><a href="#new底层原理" class="headerlink" title="new底层原理"></a>new底层原理</h2><p><strong>new表达式在编译器展开后，首先调用operator new分配原始内存（通常基于malloc实现），然后调用构造函数完成对象的初始化。如果构造函数抛出异常，则会被try-catch捕获，并调用对应参数的operator delete，将operator new申请的内存释放，防止内存泄露</strong></p><pre><code>T* p = new T(args);//类似于下面逻辑：void* mem = operator new(sizeof(T));  // ① 分配try {    p = static_cast&lt;T*&gt;(mem);    p-&gt;T::T(1, 2);                   // ② 构造} catch (...) {    operator delete(mem);            // ③ 构造失败回滚    throw;};</code></pre><p><strong>operator new是C++提供的可重载内存分配函数，用于为new分配原始内存，其默认调用的就是全局的::operator new</strong></p><p>其逻辑是，尝试通过malloc进行内存分配，<mark>如果分配失败</mark>，则会循环调用new-handler处理函数——如果用户没有手动设置，则会直接抛出std::bad_alloc异常；如果设置了，则会在调用后再次尝试malloc，为一个死循环</p><pre><code>// libstdc++-v3/libsupc++/new_op.ccvoid* operator new(std::size_t size) _GLIBCXX_THROW (std::bad_alloc) {    if (size == 0)        size = 1;    void* p;    while ((p = malloc(size)) == 0) {  // 核心：调用 malloc        // 如果 malloc 失败，尝试调用 new_handler        std::new_handler nh = std::get_new_handler();        if (nh)            nh();  // 用户可能释放一些内存        else            throw std::bad_alloc();    }    return p;}</code></pre><h2 id="operator-new重载"><a href="#operator-new重载" class="headerlink" title="operator new重载"></a>operator new重载</h2><p><strong>为什么需要重载？</strong></p><ul><li><p><strong>用于检测使用上的错误。</strong> 比如，对于越界访问new分配的内存的情况，我们可以在内存空间的头部和尾部的额外的空间，填充特定的标志位置，并在operator delete时便可以检查出标志内容是否被修改</p><pre><code>void* operator new(size_t size) {    void* p = std::malloc(size + extra);    // 写 guard bytes    return p;}</code></pre></li><li><p><strong>提高效率。</strong> operator new和operator delete针对的是“通用分配”，其需要对各种内存分配需求做适配，所以表现更为均衡。所以，如果程序大部分情况下，对内存的分配要求都是相同的，则可以制定相应的分配策略</p></li><li><p><strong>统计使用信息</strong></p></li></ul><p><strong>如何写一个正确的operator new？</strong></p><ul><li><p><strong>正确处理size &#x3D;&#x3D; 0的情况</strong>。operator new(0)必须返回一个“可区分”的空指针，常见做法是将size设置为1，并分配一块大小为1的内存块</p></li><li><p><strong>支持new-handler</strong>。在分配失败时，循环调用std::get_new_handler()获取到设定的new_headler函数，让handler决定如何处理，比如释放内存&#x2F;终止程序&#x2F;抛异常等，然后重新尝试分配</p><pre><code>void* operator new(std::size_t size) {    if (size == 0) size = 1;    while (true) {        if (void* p = std::malloc(size)) {            return p;        }        std::new_handler handler = std::get_new_handler();        if (!handler) {            throw std::bad_alloc();        }        handler();  // 可能释放内存或终止    }}</code></pre></li><li><p><strong>处理继承下的operator new。</strong> 类专属的静态operator new默认会被派生类继承使用，但是其往往只是适用于当前类，因此必须在分配内存前先检查size大小是否和当前类类型大小相同，如果不同，则交给默认的全局operator new处理。</p><pre><code>void* Base::operator new(std::size_t size) {    if (size != sizeof(Base))        return ::operator new(size);  // 交给全局 new    // 否则：走 Base 专属逻辑}</code></pre></li></ul><p><strong>相对的，只要我们自定义了operator new，就必须提供对应的operator delete，否则在构造失败的路径下会导致未定义行为，如下面代码所示：</strong></p><pre><code>class Widget {public:    static void* operator new(std::size_t size) {        // 自定义分配    }    static void operator delete(void* p) noexcept {        // 对应释放    }    //placement new 版本    static void* operator new(std::size_t size, MemoryPool&amp; pool) {        return pool.allocate(size);    }    static void operator delete(void* p, MemoryPool&amp; pool) {        pool.deallocate(p);    }};</code></pre><p>这是因为，我们在new创建对象时，<strong>如果构造函数抛出异常，会选择调用对应参数版本的operator delete来回收内存</strong>，但是如果找不到相应版本的operator delete，则会回退到全局的operator delete。这就会出问题：new使用的是自定义的策略，而delete使用的却是标准的释放，<strong>分配和释放机制并不匹配</strong>，可能导致内存泄露、破坏</p><pre><code>void* raw = Widget::operator new(sizeof(Widget)); // 成功try {    new (raw) Widget();                           // 构造失败 ❌} catch (...) {    Widget::operator delete(raw);                 // ❗回收内存    throw;}</code></pre><p><strong>那如何写一个正确的operator delete呢？</strong></p><p>其需要注意的类似，一是要正确处理删除空指针，直接return即可；二是同样要检查size大小，防止大小错误的删除</p><pre><code>class Base {        //一如以往，但此刻重点在 operator deletepublic:    static void* operator new(std::size_t size) throw(std::bad_alloc);    static void operator delete(void* rawMemory, std::size_t size) throw();    ...};void Base::operator delete(void* rawMemory, std::size_t size) throw(){    if (rawMemory == 0) return;           //检查 null 指针。    if (size != sizeof(Base)) {           //如果大小错误，令标准版        ::operator delete(rawMemory);     //operator delete 处理此一申请。        return;    }    //现在，归还 rawMemory 所指的内存；    return;}</code></pre><h1 id="placement-new表达式"><a href="#placement-new表达式" class="headerlink" title="placement new表达式"></a>placement new表达式</h1><p>【new、placement new、operator new(size_t, void*)重载之间的关系是什么？】</p><p><strong>new表达式、placement new表达式更像是一种“语法糖”，而operator new重载是底层实现。new和placement new在语法上都是new表达式，不是相互独立的两种语法，编译器生成的代码结构类似，区别在于二者调用到的operator new重载版本不同，进而影响到在构造异常时调用到的opeartor delete版本及其回滚行为。</strong></p><blockquote><p>C++标准：如果内存分配成功但对象构造失败（构造函数抛出异常），且存在与 placement new 参数匹配的 placement delete 函数，那么<strong>编译器应该调用这个 placement delete</strong>​ 来释放内存。</p></blockquote><p><strong>再本质一些，对于普通的new具有资源所有权，由C++保证自动回滚；而placement new需要用户自主决定所有权和回滚行为</strong></p><pre><code>// 二者共同的伪代码void* mem = operator new(...); //调用到的operator new 版本不同try {    construct(mem); //都会构造} catch (...) {    operator delete(...); //构造失败时调用对应的operator delete回滚    throw;}</code></pre><p>具体来说，二者的区别在于：</p><ul><li><p><strong>调用的operator new重载版本不同。</strong> new表达式调用的版本是 operator new(std::size_t size)，其回调用malloc进行内存分配，具体原理见“new底层原理”；<em><em>而默认placement new表达式调用的重载版本是 operator new(std::size_t, void</em> __p)，其只是单纯地返回指针，不会分配内存，不会调用new-handler，不会抛异常。</em>* （当然，也可以调用你自定义的placement operator new版本，以上讨论都是针对C++默认提供的默认版本的placement new）</p><pre><code>// placement new底层调用到的operator new重载，实现简化为：void* operator new(std::size_t, void* ptr) noexcept {    return ptr;  // 只是原样返回指针，不做任何处理！}</code></pre><p>二者实际上都是C++在全局作用于下提供的operator new重载版本</p><pre><code>void* operator new(std::size_t) throw(std::bad_alloc); //normal new.void* operator new(std::size_t, void*) throw();   //placement new.void* operator new(std::size_t,                   //nothrow new.                   const std::nothrow_t&amp;) throw(); </code></pre></li><li><p><strong>异常处理机制不同。</strong> new表达式会调用构造函数，在构造函数抛出异常时，会通过try-catch捕获并通过operator delete释放new表达式通过operator new(std::size_t size)申请的内存，自动完成回滚，防止内存泄露</p><p>而<strong>placement new表达式在面对构造函数抛出异常时，不会做任何事，因为这块内存根本不是它分配的，其不拥有该资源的所有权，所以C++提供相匹配的operator delete重载函数中没有做任何事，需要由用户完成资源的释放。</strong> 换句话说，如果placement new的构造出现了异常，需要用户手动兜底</p></li></ul><p><mark>我们该如何使用？</mark></p><p><strong>基本语法</strong>：new (address) Type (initializer-list) address必须是一个指针，initializer-list是类型的初始化列表</p><pre><code>void* memory = operator new(sizeof(MyClass)); // 1.分配原始内存 MyClass* obj = new (memory) MyClass(/* 构造函数参数 */);  // 2.在指定内存构造对象</code></pre><p><strong>使用注意点</strong>：</p><p><mark>placement new构造出来的对象，其析构与内存释放必须由用户自己负责，而不能直接delete，以保证内存来源和释放相匹配</mark></p><p>从内存管理的角度来说，因为placement new，T* obj &#x3D; new(buffer) T()所初始化的内存，其可能来自于栈、堆、文件和匿名映射区或是内存池中，即可能对应了不同的operator new来分配内存，而delete无法泛化处理，需要我们手动调用析构，让分配内存的operator new和释放内存的operator delete相匹配</p><p>从语义上来说，由于不是通过普通new表达式创建的对象，而是通过operator new(size_t)首先分配内存，再通过placement new表达式调用operator new(size_t, void*)+构造函数来初始化，则<strong>代表了需要将“内存管理”和“对象生命周期管理”分离</strong>，所以不能直接delete，因为delete的底层实现类似析构+调用对应版本的operator delete，又将“内存管理”和“对象生命周期管理”耦合在了一起，属于未定义行为。</p><pre><code>if (obj != nullptr){    obj-&gt;~MyClass();      // 析构    operator delete(obj); // 释放内存}</code></pre><p>尽管有些场景下混搭使用不会出现问题，比如以下场景，3和4可以合为delete(buffer)。但这种行为的性质就像malloc&#x2F;delete或new&#x2F;free混搭一样，也许不会内存泄露，可依旧属于未定义行为</p><pre><code>int main() {    // 1. 在堆上分配原始内存（未构造）    void* buffer = operator new(sizeof(MyClass));    // 2. placement new 构造对象    MyClass* obj = new (buffer) MyClass(42);    // 3. 手动调用析构函数    obj-&gt;~MyClass();    // 4. 手动释放内存    operator delete(buffer);}</code></pre><p>比如以下场景：对于MyClass类，其分配内存优先从内存池中分配内存，归还内存也要归还到内存池中</p><pre><code>class MemoryPool{public:    static inline char pool[1024];    static inline bool used = false;    static void* Allocate(size_t size)    {        //从内存池中分配    }    static void Free(void* ptr)    {        //归还到内存池    }};class MyClass{public:    int x;    MyClass(int v)        : x(v){}    // 普通new走对象池    static void* operator new(size_t size)    {        return MemoryPool::Allocate(size);    }    static void operator delete(void* ptr)    {        MemoryPool::Free(ptr);    }};</code></pre><p>如果我要求某次对象的构造不从对象池分配，而是从全局的堆内存中分配，即调用全局的operator new来分配内存，并通过placement new对其初始化，如果直接使用delete就会导致将全局的堆内存归还到内存池中，出现错误。因为此时delete调用的operator delete是MyClass类中的operator delete，而没有调用到和全局operator new相匹配的operator delete。</p><pre><code>int main(){    // 调用全局的operator new，而不是类中的，会外部分配原始内存    void* buffer = ::operator new(sizeof(MyClass));    // placement new    MyClass* obj = new(buffer) MyClass(42);    // 错误！    delete obj;}</code></pre><p>正确的做法是析构+全局::operator delete，必须让operator new和operator delete配对使用，而不是使用默认的delete</p><p><strong>如果正确地自定义实现placement new表达式？</strong></p><p>实际上，其和“写一个正确的operator new”的行为是一致的，因为本质上都是在写operator new的重载，只不过说的placement new重载，一般都不会在operator new中分配内存，也不会在operator delete中释放内存</p><ul><li><p><strong>placement new和placement delete必须成对设计。</strong> 换句话说，如果提供了operator new(A, B, C)版本的重载，那么也必须提供对应的operator delete(A, B, C)版本的重载，以保证在构造函数抛异常时可以被正确回滚，防止资源泄漏。</p></li><li><p><strong>在类内重载operator new时，需要避免遮盖了正常版本的operator new。</strong> 正确的方法是，创建一个基类，包含所有C++默认提供的operator new，并让需要自定义operator new的类继承该基类</p><pre><code>class StandardNewDeleteForms {public:    // normal new/delete    static void* operator new(std::size_t size) throw(std::bad_alloc)    { return ::operator new(size); }    static void operator delete(void* pMemory) throw()    { ::operator delete(pMemory); }    // placement new/delete    static void* operator new(std::size_t size, void* ptr) throw()    { return ::operator new(size, ptr); }    static void operator delete(void* pMemory, void* ptr) throw()    { return ::operator delete(pMemory, ptr); }    // nothrow new/delete    static void* operator new(std::size_t size, const std::nothrow_t&amp; nt) throw()    { return ::operator new(size, nt); }    static void operator delete(void *pMemory, const std::nothrow_t&amp;) throw()    { ::operator delete(pMemory); }};class Widget: public StandardNewDeleteForms {             //继承标准形式public:    using StandardNewDeleteForms::operator new;           //让这些形式可见    using StandardNewDeleteForms::operator delete;    static void* operator new(std::size_t size,            //添加一个自定的                              std::ostream&amp; logStream)     //placement new    throw(std::bad_alloc);    static void operator delete(void* pMemory,             //添加一个对应的                                std::ostream&amp; logStream)    //placement delete    throw();    ...};</code></pre></li></ul><p><strong>placement new的应用场景？</strong></p><p>一般应用在自定义的内存池——避免频繁地调用malloc&#x2F;free，重复利用同一块内存区域；STL allocator容器的底层实现，consructor函数的本质就是placement new，支持延迟构造，比如vector支持reserve来提前分配内存而不初始化</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>从malloc的底层机制到new的面向对象封装，我们看到了C++在内存管理哲学上的重大演进。new&#x2F;delete不仅是简单的语法糖，更是C++面向对象核心思想的体现——将内存分配与对象生命周期管理绑定，为RAII和异常安全提供基础支持。<br>对于大多数场景，直接使用new&#x2F;delete（或智能指针）是最安全的选择；但在需要极致性能或特殊内存管理的场合，理解如何正确重载operator new、使用placement new等技术<br>在后续的文章中，我们将继续探索内存管理的更多话题——智能指针、移动语义等机制如何优化对象生命周期管理</p>]]></content>
      
      
      <categories>
          
          <category> 内存分配与管理 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>【内存分配与管理】关于堆、栈的思考</title>
      <link href="/posts/stack-and-heap.html"/>
      <url>/posts/stack-and-heap.html</url>
      
        <content type="html"><![CDATA[<p>在上一篇文章中，我们深入探索了malloc的底层实现，看到了通用内存分配器如何在碎片与效率之间寻找平衡。然而，malloc仅仅管理着内存世界的一半版图——堆。理解内存分配的全貌，我们还需要认识另一个同样重要但哲学截然不同的领域：栈。</p><p>本文我们将跳出具体的分配器实现，从更高维度思考：为什么程序需要同时存在堆和栈这两种内存模型？它们的本质区别是什么？又是什么深层的设计哲学决定了它们各自的特性和限制？</p><h1 id="总结概括"><a href="#总结概括" class="headerlink" title="总结概括"></a>总结概括</h1><p><strong>【基本管理规则】</strong> 栈是内存连续的，栈上的内存由系统自动管理，其空间较小，栈上对象的生命周期严格受到作用域的限制。<strong>【优点】</strong> 优点在于栈内存的分配和释放基于移动栈指针实现，速度较快不需要担心内存泄露；并且由于永远遵循先进后出的规则回收，所以内存永远是连续的，不会产生内存碎片。<strong>【缺点】</strong> 但是，为了保证栈的高效，栈布局必须在编译期确定，因此栈对象的灵活性较差，不允许动态分配。 <strong>【示例】</strong> 比如函数调用的返回地址、函数参数、局部变量等都会存储在栈上，在函数调用时移动栈指针开辟函数栈帧，函数结束时通过移动栈指针来释放内存。</p><p><strong>【基本管理规则】</strong> 堆是无序内存，需要由用户手动管理或是通过GC来管理，堆空间更大且生命周期更灵活。<strong>【优点从堆本身定义说起】</strong> 优点从其名字可以看出来，其之所以叫做堆（heap）这个名字，是因为heap在英文中有“堆积、杂乱（一堆）”的意思，按照这个角度来看，更准确的翻译是”堆杂物的内存“，和数据结构的堆（优先级队列）没啥关系，其更可能是形容的是“用户的管理方式”——程序员在获取到heap内存后，相比于栈那样必须连续存储、分配释放顺序固定，heap允许你”杂乱无章“地将自己的东西存储在这块内存上——<strong>生命周期不受约束、内存布局不受约束，即在时间和空间上都是自由的，适合存储动态对象</strong>。<strong>【缺点】</strong> 这种自由是有代价的，其内存分配涉及到系统调用，分配和释放成本较高；此外由于生命周期不受控制，所以可能发生内存泄露的问题，并且还带了内存碎片</p><p><strong>从时间和空间的角度来看，栈 &#x3D; 时间空间均受限，栈上分配的内存大小固定、生命周期固定；堆 &#x3D; 空间时间均自由，堆上的内存大小由运行时自由决定，生命周期由程序员手动管理</strong></p><h1 id="一些问题"><a href="#一些问题" class="headerlink" title="一些问题"></a>一些问题</h1><p><strong>为什么选择了“栈”这个结构？</strong></p><p>栈并不是单一原因产生的，而是<strong>函数调用机制</strong>和<strong>结构化编程</strong>需求共同演化的结果。数调用天然具有后进先出的特性，而结构化编程中的作用域嵌套和局部变量生命周期也符合LIFO模式。ALGOL（一种早期的编程语言）将这两种需求相统一，提出了“栈”这种管理内存的概念</p><p><mark>从结构化编程来说</mark>，<strong>为了解决代码难以维护的问题，而引入了作用域的概念</strong>。通过划分出作用域，不让全局变量满天飞，而是让变量局部化——进入每个作用域时，为其开辟一块内存，退出作用域时，再自动将该内存释放掉，<strong>让变量的生命周期可控</strong>。并且，这些作用域是可以相互嵌套的，所以选择了栈。</p><p><mark>从函数调用机制的角度来说</mark>，函数同样调用天然符合嵌套调用结构，而嵌套结构的返回地址和局部信息符合后进先出的特点，所以选择了栈。程序执行的过程实际上就是在调用一个个的函数，而函数调用的本质是保存返回点+跳转，函数返回的本质是跳回到返回点，这和栈没有必然的关系，完全可以只用寄存器来保存返回的地址。<strong>真正的问题在于函数嵌套调用的场景，必须保证函数返回的地址以及局部信息不会被覆盖</strong>，需要将旧地址和局部信息放到一块内存区域，同时不受到后调用函数的影响，保证一个函数向栈中存的数据和拿出来的数据前后是一致的，<strong>这就要求后存的数据必须先拿，先存的数据必须后拿，即FILO，这才是为什么需要使用栈</strong>。而call指令只是利用了这个结构，将push+jmp相融合</p><p><strong>再向深一层说，为什么函数的调用结构天然符合嵌套的特点？</strong></p><p>这是由冯诺依曼体系结构决定的，冯诺依曼机器一次只能执行一个控制流(PC指针唯一)，且是同步执行流(顺序执行+阻塞等待)。比如A-&gt;B-&gt;C-&gt;D，当当前正在执行A的逻辑时，如果顺序执行到B，那么必须跳转到B的逻辑；当当前正在执行D的逻辑时，C B A函数都必须暂停，D返回时必须先回到D自身的调用点，回到调用点后继续执行C的逻辑。同理，在执行C的逻辑时，B A函数都已经暂停，C返回时必须先回到C自身的调用点，回到调用点后继续执行B的逻辑…..所以，函数的调用天然符合嵌套的结构</p><p>从拓扑结构的视角来看，A-&gt;B-&gt;C的函数调用过程，被调用者必须在调用者继续执行之前完成，也就是说，C没返回前，B也不会继续；B返回前，A也不会继续，其拓扑结构是一棵树，执行顺序是DFS，而DFS的实现结构就是栈</p><p><strong>为什么需要堆？</strong></p><p>换个说法，为什么需要有一块内存区域，通过”堆“的方式进行管理？</p><p><strong>栈是为了满足作用域嵌套、避免全局变量乱飞而提出的，其可以让局部变量自动创建和销毁，使其生命周期可控。</strong> 而这个保证是通过栈指针RSP的移动、函数栈帧的创建和销毁提供的（用函数调用流程解释怎么提供的保证）。具体体现为汇编指令中的RSP -&#x3D; size开辟栈帧，栈帧回退时的RSP +&#x3D; size。而为了保证这些操作足够高效，所以这些汇编指令中的偏移值都需要在编译期完全确定。而要求这些偏移值在编译期确定下来，就导致无法在运行时分配内存，因为会直接影响栈帧布局。</p><p><strong>逻辑链条：生命周期可控-&gt;通过栈帧机制保证-&gt;为了高效，编译指令中的偏移值必须在编译时确定-&gt;内存开辟的大小必须在编译时确定</strong></p><pre><code>void f(int n) {    int a[n]; // C++ 标准不允许（VLA）}</code></pre><p>那如果要开辟多大内存根本没法在编译期确定呢，不就导致无法在编译器确定偏移值了吗？比如网络请求响应、用户输入了多少数据、文件有多少数据，会根据运行时信息决定的需要的内存块的大小，该怎么办？<strong>所以，我们需要一块内存区域，允许将内存的分配推移到运行时，但这也意味着，没法再依赖于LIFO对内存进行生命周期管理，需要由程序员手动管理。我们将这块内存区域称作为堆。</strong></p><p>从时间和空间的角度来看，栈 &#x3D; 时间空间均受限，栈上分配的内存大小固定、生命周期固定；堆 &#x3D; 空间时间均自由，堆上的内存大小由运行时自由决定，生命周期由程序员手动管理</p><p>之所以叫做heap这个名字，更可能是形容的是“用户的管理方式”——程序员在获取到heap区域的内存后，相比于栈那样必须连续存储、分配释放顺序固定，heap允许你”杂乱无章“地将自己的东西存储在这块内存上——生命周期不受约束、内存布局不受约束。按照这个角度来看，更准确的翻译是”堆杂物的内存“，和数据结构的堆（优先级队列）没啥关系</p><p><img src="https://img.aixheee.top/80a470d6-1030-4a72-81b4-5df2a14bb8b5.png"></p><p><strong>为什么栈由高地址到低地址增长，堆由低地址到高地址增长？</strong></p><p>从堆栈设计的角度：</p><p>需要综合考虑二者，二者都是动态增长的，相向动态增长是最好的选择，可以尽可能地降低二者的冲突，充分利用内存。如果二者都是向着同一个方向增长，比如都向着高地址增长，出现的问题：如何确定栈的起始位置&#x2F;栈底位置？</p><p>这个位置决定了栈的大小和堆的大小的堆内存的划分分配。但是二者的大小并不是绝对的，在有些情况下，可能堆用的空间更大，有时就是栈用的空间更大。所以，会出现这种情况——一个程序因为栈溢出而崩溃，但是程序却有大量的堆空间没有利用。</p><p>如果要支持动态调整栈底的位置，则涉及到移动整个栈的数据，太过麻烦。</p><p>从硬件的角度：现代CPU指令集大部分都定义好了栈顶指针push pop的指令，默认规定了栈是从高地址向着低地址增长的</p><p><strong>栈是否可以动态增长？</strong></p><p>当栈的空间被耗尽时，会触发缺页异常，通过异常陷入内核态后，异常会被内核的expand_stack() 函数处理，进而调用 acct_stack_growth() 来检查是否还有合适的地方用于栈的增长。如果当前栈的大小小于8M（通常情况下），那么栈会被扩容，否则会发生栈溢出(stack overflow)，进程收到段错误(segmentation fault)信号。</p><p>动态栈增长是唯一一种访问未映射内存区域依然合法的情形，其他情况下都会直接触发页错误，进而导致段错误</p><p><strong>为什么栈内存分配速度快，堆内存分配速度慢？</strong></p><p>栈：</p><p><mark>分配机制的角度</mark>：栈空间的分配极为简单，内核通过rbp指向栈底，rsp指向栈顶，<strong>栈分配内存的本质就是移动rsp栈顶指针预留空间，而释放就是rsp栈顶指针回退，O(1)时间复杂度完成。</strong></p><p><mark>系统调用的角度</mark>：这个过程中几乎不涉及系统调用，程序启动时，内核给进程分配一块固定大小的栈空间，后续完全是在用户态移动rsp指针(除非触发栈增长或是缺页异常)</p><p><mark>硬件层面的角度</mark>：栈空间是连续的，缓存命中率高；栈对应的物理页是少量连续的，TLB局部性好</p><p>堆：</p><p><mark>分配机制的角度</mark>：堆内存是任意顺序分配的，所以malloc首先需要找到合适的空闲块，如果块过大还需要split，涉及到一系列数据结构的增删查改操作</p><p><mark>系统调用的角度</mark>：当堆空间不够用时，如果申请的是小于128K的小块内存——使用do_brk()系统调用来调账堆中brk指针的指针来增加或者回收堆内存；如果申请的是大于128K的大块内存——则会调用mmap在虚拟地址空间中的文件映射与匿名映射区中，创建出一块VMA内存区域(这里是匿名映射)。涉及到了用户态和内核态的切换、页表的修改、TLB flush，成本较高</p><p>维护堆空间：内核需要处理内存碎片，合并相邻空闲块等</p><p><mark>多线程同步问题</mark>：对于栈来说，每个线程独立栈，不需要加锁；对于堆来说，堆是全局共享的，需要加锁</p><p><mark>硬件层面</mark>：堆并不是连续的，是通过双向链表管理起来的，缓存命中率不高；此外，堆可能对于大量的离散页，TLB miss更频繁</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>通过对堆栈的深入探讨，我们看到：栈的本质是“编译期可预测的自动管理”，堆的本质是“运行时期动态的手动管理”。这种根本性的差异，决定了它们在性能、安全性、灵活性上的不同权衡。这直接引出了我们下一篇文章的核心话题：C++的值语义与引用语义。</p>]]></content>
      
      
      <categories>
          
          <category> 内存分配与管理 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>【内存分配与管理】malloc设计哲学</title>
      <link href="/posts/mallocDesign.html"/>
      <url>/posts/mallocDesign.html</url>
      
        <content type="html"><![CDATA[<p><strong>上篇文章中主要讲解了malloc底层的原理机制，本篇文章则侧重于讲解为什么malloc要这样设计</strong></p><h1 id="allocator要解决的核心矛盾在哪？"><a href="#allocator要解决的核心矛盾在哪？" class="headerlink" title="allocator要解决的核心矛盾在哪？"></a>allocator要解决的核心矛盾在哪？</h1><p><mark>allocator需要在有限的时间开销下，尽可能高效地利用虚拟地址空间，其必须在 分配&#x2F;释放的时间复杂度 与 堆空间的碎片率(空间利用率) 之间做权衡</mark></p><p><strong>追求速度意味着：</strong></p><p>为了快，allocator通常会使用固定内存大小块、此时可以做到O(1)分配，减少了遍历、合并和切割操作，更倾向于粗粒度管理内存块，比如fastbin和smallbin。但这样做的结果就是，由于不能切割，用户可能拿到“偏大的块”，导致内部碎片；同时由于不能频繁合并，在短时间内会导致外部碎片。<strong>速度增加—&gt;碎片增多—-&gt;空间利用率下降</strong></p><p><strong>追求空间利用率意味着：</strong></p><p>为了提高空间利用率，allocator必须做精细切割、频繁合并，通过遍历做best-fit并维护更复杂的数据结构(红黑树&#x2F;ordered bins)。这样做的结果就是，malloc&#x2F;free成本增大，多线程下锁竞争更严重。<strong>空间利用率升高—&gt;时间开销增大</strong></p><p><mark>所以，ptmalloc的分层设计，本质上就是把目标拆分到不同层级，用不同的策略尽可能优化。但是，不存在完美通用的allocator，对于allocator的不同实现，本质上就是在对这组平衡做出不同的权衡</mark></p><h1 id="内存池各层设计思路："><a href="#内存池各层设计思路：" class="headerlink" title="内存池各层设计思路："></a>内存池各层设计思路：</h1><p>如何解决矛盾？</p><p><strong>小块追求常数时间，大块追求空间利用率。因为前者是高频查找路径，需要提高查找效率；后者对空间利用率影响大，需要提高空间利用率。通过分层和延迟策略在性能和空间碎片间取得平衡</strong></p><p><strong>fastbin&#x2F;smallbin：热路径优化，对于小内存需要高频查找，设计思想是用空间换时间</strong>，采用exact-fit的思想，“只匹配完全相同大小的块”，虽然不合并会产生内存碎片，但是换来了free和malloc都是O(1)。如果采用best-fit，则势必要进行O(n)查找，并插入排序</p><p><strong>unsorted bin：free后chunk的“缓冲区+热缓存层”，用于延迟分类并提高复用概率。</strong> 程序中的访问的局部性原理——刚释放的内存，大概率会被马上申请，如果把这块内存又放到smallbin&#x2F;largebin中，下次malloc又要去查找，是浪费时间。所以，<strong>采用了经典的系统优化策略——写操作快、读时再整理，将unsorted bin作为一个缓冲区。</strong> 即，在free时不做复杂的分类，在malloc时再做分类。free时，所有合并完成后的内存块都会直接放到unsorted bin中，以利用访问的局部性原理；malloc时检查目标块是否是unsorted bin中的chunk，如果不是，则再把该chunk分类放回到smallbin&#x2F;largebin中，可以理解为访问的局部性原理此次失效</p><p><strong>largebin：属于低频大块路径，设计思想是时间换空间，允许牺牲一部分时间，以O(n)查找来换取更优的空间。</strong> 其选择了best-fit策略，即“选择最接近请求大小的空闲块”，目的是“最小化切割后的剩余空间，保留大块空间”：如果要180B内存，则应该找200B进行切割，而不是1000B。因为1000B切割后，剩余的空间的820B空间不大不小，也许很难被利用，这也是 <strong>外部碎片的常见来源——大块被切割为中等大小，导致难以适配未来请求</strong>；而切割200B剩余的20B内存较小，可以较易被选取.</p><p><strong>为什么不是first-fit？</strong> first-fit就会导致更严重内存碎片问题，例如连续多次的300B请求，会把1000B大块分割，最终剩下100B，难以分配</p><h1 id="brk-与mmap-决策思路"><a href="#brk-与mmap-决策思路" class="headerlink" title="brk()与mmap()决策思路"></a>brk()与mmap()决策思路</h1><p><strong>注意，常常说调用malloc就是直接通过brk()&#x2F;mmap()申请内存，实际上，这并不完全准确。虽然内存分配确实通过二者来实现，但是只有申请的内存较大，无法在bins中找到合适的缓存内存块时，才会调用这两个系统调用来分配内存。</strong></p><h2 id="1-brk-：堆区的线性扩展工具"><a href="#1-brk-：堆区的线性扩展工具" class="headerlink" title="1.brk()：堆区的线性扩展工具"></a>1.brk()：堆区的线性扩展工具</h2><p>核心机制：通过移动进程堆顶指针来动态扩展内存空间。</p><p>特点：</p><p>1.虚拟内存和物理内存的延迟绑定：brk仅仅修改虚拟内存边界，并不会理解分配物理内存。只有在首次访问虚拟内存，才会触发缺页中断，真正分配物理内存并建立映射关系</p><p>2.堆区内存的顺序释放：由于Linux VMA机制以及其分配内存的方式，所以释放内存时必须先释放高地址内存，再释放低地址内存。</p><h2 id="2-mmap-：独立映射"><a href="#2-mmap-：独立映射" class="headerlink" title="2.mmap()：独立映射"></a>2.mmap()：独立映射</h2><p>核心机制：建立虚拟内存区域和物理内存区域的映射关系</p><p>特点：</p><p>1.支持非连续内存分配与大块内存。</p><p>2.零拷贝特性提升效率。</p><p>3.独立释放避免内存碎片化。mmap通过munmap可以独立释放内存，减少内存的碎片化问题</p><h2 id="3-对比与选择"><a href="#3-对比与选择" class="headerlink" title="3.对比与选择"></a>3.对比与选择</h2><p>在内存区域上：brk分配的内存空间位于堆上，而mmap分配的内存空间位于文件映射区上</p><p>内存地址连续性：brk分配的内存区域属于堆的一部分，地址连续；mmap分配的内存是独立的，其通过vm_area_struct独立管理起来</p><p><strong>由这两个特点，我们可以推断出两种分配方式的内存碎片化程度：</strong></p><p>brk()动态调整brk指针，在堆区上进行地址的线性扩展，所有分配的内存块紧密相连的、连续的。这就决定了，在内存释放时，只有堆尾部的连续空闲区域，才能通过brk向回收，会退给操作系统，而虚拟地址碎片不可以从中间回收，其势必会导致内存碎片化程度较高</p><p>mmap()在文件映射区创建独立的内存映射区域，所有mmap的内存区域不一定是连续的，是多VMA模型。这就决定了在内存释放时，可以独立释放掉一整个mmap区域，内存碎片化程度较低，更利于大块释放</p><p><strong>此外，需要考虑两种系统调用本身的开销：</strong></p><p>brk()只需要动态调整指针值，操作简单便捷，系统调用开销较低，大约为100ns；mmap创建内存映射时，需要创建vm_area_struct并立即建立映射关系，开销较大，大于为500ns</p><h2 id="4-malloc中的策略选择"><a href="#4-malloc中的策略选择" class="headerlink" title="4.malloc中的策略选择"></a>4.malloc中的策略选择</h2><p><strong>在所需内存较小时(小于128KB)时：</strong> malloc选择通过brk()来分配内存。原因在于，小块内存的分配非常频繁，对于这种热路径，我们需要用空间换时间，选择<strong>系统调用成本更低</strong>的brk()。<strong>此外，这些小块的生命周期短，容易复用，不需要立即归还给操作系统，通过bins管理起来即可</strong>，似乎发挥不出mmap可立即归还物理内存的优势，反而增加了分配成本(这也是为什么只有小块内存通过bins管理——其容易复用)。brk()的另一个优势在于，其内存空间都是连续的，<strong>缓存命中率更高</strong></p><p><strong>在所需内存较大时(大于等于128KB)：</strong> malloc选择通过mmap()创建独立的内存映射来分配内存。原因在于，大块内存的分配频率较低，但是<strong>如果划分分配不当，就会导致较为严重的内存碎片。</strong> 此外，这些大块相比于小块，<strong>其占用空间大、生命周期长，不容易复用，所以不需要通过bins管理起来，free时需要尽早归还内存，避免大块长期占用heap中部，阻止heap top shrink</strong>。所以，采取mmap来分配内存(为什么只有小块内存通过bins管理)。尽管效率相对较低，但是瑕不掩瑜，可以理解为用时间换空间</p><h1 id="allocator、VMA和buddy-sysem三者的协作"><a href="#allocator、VMA和buddy-sysem三者的协作" class="headerlink" title="allocator、VMA和buddy sysem三者的协作"></a>allocator、VMA和buddy sysem三者的协作</h1><p>allocator只管理chunk，VMA管理虚拟内存区域，二者通过brk()&#x2F;mmap()&#x2F;munmap()系统调用协作关联，chunk并不与物理内存页有直接协作关系；VMA在缺页中断时，驱动物理页的分配，建立二者的协作关联</p><p><strong>malloc时 allocator与VMA的关联：</strong></p><p>如果直接命中bins中的chunk——VMA没有任何变化。如果是brk()扩展heap——则会扩大heap vm_area_struct的end指针与mm_struct中brk指针，并不创建新的vm_area_strct</p><p>如果是mmap()映射——则创建新的vm_area_struct用于描述这块映射区域，并插入到红黑树以及双向链表中管理起来</p><p><strong>free时 allocator与VMA的关联：</strong></p><p>如果free的是brk块且不触发trim——则只在allocator层有操作，VMA没有任何变化，因为VMA描述的是heap整体</p><p>如果free的是brk块且触发trim(heap shrink)——则会减小heap vm_area_struct的end指针与mm_struct中brk指针，并不销毁vm_area_strct</p><p>如果free mmap块——则删除对应的vm_area_struct，释放页表、释放物理页</p><p>VMA与buddy sysem的关联：缺页中断、页表机制，此处不展开</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>通过本文的分析，我们看到了malloc在简单接口背后复杂的实现机制与设计哲学。在接下来的系列文章中，我们将以malloc为起点，逐步构建完整的C++内存管理知识体系。下一篇文章，我们将探讨堆与栈的差异，以及这种差异如何影响我们的编程模型和性能优化策略。</p>]]></content>
      
      
      <categories>
          
          <category> 内存分配与管理 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>【内存分配与管理】malloc原理剖析</title>
      <link href="/posts/malloc.html"/>
      <url>/posts/malloc.html</url>
      
        <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>当我们谈论程序性能时，总绕不开“内存”这个话题。内存管理如同程序的血脉系统，决定着程序的效率、稳定性和资源利用率。我们对内存的理解不能只停留在malloc&#x2F;free、new&#x2F;delete的简单调用层面，更要对其背后的机制以及设计缘由有所理解。</p><p>这是“内存分配与管理”系列的第一篇文章。我们将从最基础、也最重要的malloc开始，逐层揭开内存管理的神秘面纱。malloc不仅仅是C语言中的一个库函数，更是理解现代操作系统内存分配思想的窗口——它的设计哲学、实现策略、以及效率与通用性的权衡，构成了整个内存生态的基础。</p><p>在具体说malloc之前，我们先分清楚几个常见的概念：allocator、malloc和ptmalloc：malloc理解为分配内存的接口，allocator是一个负责内存分配和回收的具体策略实现。在Linux + glibc默认环境下，通过ptmalloc策略来实现(ptmalloc是一个名称)；Google实现了tcmalloc策略，微软实现了mimalloc….</p><h1 id="arena"><a href="#arena" class="headerlink" title="arena"></a>arena</h1><p>ptmalloc针对堆管理，最顶层的管理结构为malloc_state，即arena</p><pre><code>arena（分配器实例）  └── heap（一段连续内存区域）        └── chunk（用户分配单位）struct malloc_state{  // 本arena的锁  mutex_t mutex;  /* Flags (formerly in max_fast).  */  int flags;  /* Fastbins */  mfastbinptr fastbinsY[NFASTBINS];  //当前arena中最高地址的、尚未分配的连续内存块  mchunkptr top;  /* The remainder from the most recent split of a small request */  mchunkptr last_remainder; // small/large/unsorted bins  mchunkptr bins[NBINS * 2 - 2];  // Bitmap of bins   unsigned int binmap[BINMAPSIZE];  // 链表连接所有arena  struct malloc_state *next;  /* Linked list for free arenas.  */  struct malloc_state *next_free;  // 本arena从系统分配的总内存  INTERNAL_SIZE_T system_mem;  INTERNAL_SIZE_T max_system_mem;};</code></pre><p><strong>【arena是什么？】</strong></p><p><strong>每个线程都会绑定一个arena，其代表着一整套完整的malloc管理体系，独立着管理多个heap（非主arena的heap来自于多个mmap区域），这些heap通过链表相连，共用一套bins，而arena通过top指针指向当前正在被使用的heap</strong></p><pre><code>arena  ├─ fastbins  ├─ smallbins  ├─ largebins  ├─ unsorted bin  ├─ top chunk  └─ mutex</code></pre><p><strong>【arena的类别有哪些？】</strong></p><ul><li><p>main arena：即对应到进程的初始堆，可通过brk&#x2F;sbrk系统调用扩展堆大小，即来自进程对应的“堆”，每个<strong>进程</strong>有且只有一个main arena。</p></li><li><p>thread arena：即每个线程独有的arena（不超过数量限制的前提下），需要通过mmap单独映射内存以扩展heap，多个内存块是离散的</p></li></ul><p><strong>为什么不用brk给所有arena扩展？</strong></p><p>brk只能线性增长，但是多线程情况下，需要多块独立的内存，可以分别独立扩展、独立释放。如果线程被回收了，则线程申请的内存同样需要被回收，而对于进程地址空间下的heap来说，其无法做到这一点，必须从top开始释放内存，而通过mmap映射的内存可以通过unmmap单独释放</p><p><strong>多线程程序中可能有多个heap VMA是什么意思？不是所有线程共享同一个heap吗？</strong></p><p>准确来说，<strong>多线程程序中可能存在“多个由mmap 创建的 heap-like VMA”</strong> 指的是，多线程中只能使用mmap作为动态内存的分配来源，这些mmap VMA位于虚拟地址空间中的<mark>文件和匿名映射区</mark>，其不属于进程的heap，但是在线程眼中被当作heap使用。此时，就会存在多个匿名mmap VMA，它们分别属于不同的arena，<mark>但是仍然属于同一个进程地址空间中</mark>。</p><p>一个多线程程序的内存分配情况可能如下所示：</p><pre><code>[ text ][ data ][ brk heap ]      ← main_arena[ mmap arena 1 ]  ← thread_arena1[ mmap arena 2 ]  ← thread_arena2[ mmap arena 3 ][ stack thread1 ][ stack thread2 ]线程1 (绑定主arena)             线程2 (绑定arena1)             线程3 (绑定arena2)      |                               |                               |      v                               v                               v  main_arena                    malloc_state1                   malloc_state2  +-------------+               +-------------+               +-------------+  | mutex       |               | mutex       |               | mutex       |  | fastbinsY[] |               | fastbinsY[] |               | fastbinsY[] |  | top ------------&gt; [heap]    | top ------------&gt; heap1    | top ------------&gt; heap2  | bins[]      |    +------+   | bins[]      |    +------+  | bins[]      |    +------+  | ...         |    | chunk|   | ...         |    | chunk|  | ...         |    | chunk|  | system_mem  |    | chunk|   | system_mem  |    | chunk|  | system_mem  |    | chunk|  +-------------+    | ...  |   +-------------+    | ...  |  +-------------+    | ...  |                     +------+                      +------+                     +------+                       brk堆                          mmap区域                    mmap区域</code></pre><p><strong>我们知道了arena是什么，以及如何获取内存，那么arena是怎么和线程交互的？</strong></p><ul><li><p>如果线程第一次分配内存：</p><ul><li><p>对于第一个分配内存的线程：使用main arena</p></li><li><p>对于其他线程：会遍历已有的arena，尝试找到一个未被其他线程使用的arena绑定（即锁是空闲状态），若所有arena都被占用，则会创建新的arena。arena默认数量上限为8*CPU核数，每个线程都会尝试绑定一个独有的arena，如果arena数量达到上限，则多个线程会共享同一个arena</p></li></ul></li><li><p>如果线程后续需要分配内存：则优先复用先前使用过的arena，如果其已被其他线程占用，则去尝试使用其他的arena，而不是干等着。<strong>这是为了提高内存利用效率，防止明明一个进程分配到了大量内存资源，但线程依然无法获取</strong></p></li></ul><p>【为什么要有arena？其优点和缺点是什么？】</p><p><strong>arena的主要设计目的是为了减少多线程下分配内存的锁竞争。</strong> 如果所有线程的malloc&#x2F;free都需要依赖于同一个堆结构，那么每次分配&#x2F;释放内存需要抢同一把锁，性能会非常低下。</p><p>其缺点在于，在多线程下每个线程都有自己的arena，A线程中的空闲块无法被B线程使用，可能导致明明分配给了该进程较多内存，但是依然不够用的情况，这就会导致<strong>内存膨胀</strong>，整体的内存利用率下降。</p><h1 id="内存池内存管理机制"><a href="#内存池内存管理机制" class="headerlink" title="内存池内存管理机制"></a>内存池内存管理机制</h1><h2 id="为什么堆要用内存池管理内存？而栈不需要？"><a href="#为什么堆要用内存池管理内存？而栈不需要？" class="headerlink" title="为什么堆要用内存池管理内存？而栈不需要？"></a>为什么堆要用内存池管理内存？而栈不需要？</h2><p><strong>从操作系统资源粒度来看</strong>：操作系统只提供了“页级别”的粗粒度管理，内核负责建立VMA、建立页表映射关系、分配物理页框，整个过程都是以页为基本单位的。但是用户程序却需要几十几十字节的内存，对于这种远小于4KB页大小的请求，如果没有allocator作为中间层负责页内细粒度的分配，那么会导致内部碎片严重、内存利用率极低。</p><p><strong>从生命周期控制来看</strong>：堆内存的释放允许任意顺序，生命周期不可预测。如果没有allocator，无法记录哪些内存块是空闲&#x2F;已用的。相比于栈，栈内存的分配是严格按照LIFO——后分配、先释放的，生命周期相互嵌套，出作用域后直接释放，不存在该问题</p><p><strong>从碎片控制的角度来看</strong>：由于堆内存的分配在空间上不是连续的，这就会可能出现外部碎片问题，如果没有allocator，就无法合并、切割，否则很快就无法分配大块内存区域</p><p><strong>从系统调用申请内存的角度来看</strong>：brk()&#x2F;sbrk()&#x2F;mmap()属于系统调用，如果每次申请内存都要调用这三个系统调用的其中之一，触发用户态到内核态的切换，是非常影响性能的。相比之下，栈的内存分配只需要通过移动栈指针即可</p><h2 id="内存池具体管理方法"><a href="#内存池具体管理方法" class="headerlink" title="内存池具体管理方法"></a>内存池具体管理方法</h2><h3 id="如何描述堆虚拟内存"><a href="#如何描述堆虚拟内存" class="headerlink" title="如何描述堆虚拟内存"></a>如何描述堆虚拟内存</h3><h4 id="隐式链表技术"><a href="#隐式链表技术" class="headerlink" title="隐式链表技术"></a>隐式链表技术</h4><p><strong>malloc将整个堆内存空间分为连续的、大小不一的chunk，chunk即为堆内存的最小管理单位。</strong></p><p>为了区分不同的内存块的信息，比如已分配、未分配……会将这些信息直接嵌入到chunk内部</p><p>堆内存要求chunk的大小必须是8的整数倍，因此chunk size的后三位是无效的，为了充分利用内存，就将这三位作为chunk的标志位。</p><p>而padding部分则是用于内存对齐，让整个chunk为8的整数倍</p><p>allocated chunk：</p><p><img src="https://img.aixheee.top/de2b88c5-934d-4d50-b180-15606606372c.png"></p><p>free chunk：</p><p><img src="https://img.aixheee.top/c7ceb5b7-2324-422a-a3d3-fa8209fde08d.png"></p><p>此时，整个堆内存组织为一个连续的，已分配或未分配的chunk序列</p><p><img src="https://img.aixheee.top/54cc37b7-bd5e-4f5a-b461-72f0c74e76e4.png"></p><p>这种结构叫做隐式链表，我们只要拿到堆的起始地址，就可以根据每个chunk中存储的size信息，遍历所有chunk，以找到合适的chunk进行分配</p><p>缺点：遍历效率低，难以进行不同chunk之间的合并，久而久之，就会导致堆内存被分割为许多内存碎片。为什么不能合并？假设下面这样的场景，从上到下有3个内存块ABC，假设释放B内存：如果想和C合并，只需要让B内存的起始地址加上记录的chunk size即可得到C内存的起始地址，根据C的标志位即可判断是否可以合并；但是想和A合并较为困难，因为我们没有办法找到上一个内存块(高地址)，无法判断，最坏情况下必须遍历整个堆才可以将AB合并</p><p><img src="https://img.aixheee.top/6ca4b83b-bfe1-460f-89f2-7c73e2b02919.png"></p><h4 id="带边界标记的合并技术"><a href="#带边界标记的合并技术" class="headerlink" title="带边界标记的合并技术"></a>带边界标记的合并技术</h4><p>Kunth在每个chunk的最后添加一个和header的副本——footer，此时，只要拿到了一个chunk的起始地址，将该地址+4就可以获得到高地址的chunk的信息了</p><p>问题：只有free chunk才需要footer，allocated chunk并不需要，此时footer对于allocated chunk是无用的</p><p>优化：用第1位，表示前一个chunk是allocated还是free。如果为1，则代表前一个chunk为allocated，当前chunk地址的前4个字节为前一个allocated chunk的payload或padding，此时，allocated chunk就算不保留footer也不会导致相邻的free chunk访问自身出错；如果为0，则代表前一个chunk为free，则将二者合并</p><p>free chunk：</p><p><img src="https://img.aixheee.top/b3a1f6c5-0d38-4699-89bd-2ff7303834d6.png"></p><h4 id="支持多线程"><a href="#支持多线程" class="headerlink" title="支持多线程"></a>支持多线程</h4><p>随着技术的发展，堆内存管理器需要添加对堆多线程的支持，可是，此时chunk的标志位已经不够用了，我们还需要两个标志位——一个用于判断当前chunk是否属于主线程，另一个判断当前chunk是由mmap得来的还是由brk得到的。</p><p>思考先前chunk的缺点：是否有必要在一个chunk中，同时保存前一个chunk的已分配&#x2F;未分配标志位和当前chunk的已分配&#x2F;未分配标志位？没必要，<strong>只保存前一个chunk的已分配&#x2F;未分配标志位就够了，对于当前chunk是否已经分配，完全可以查下一个chunk的Header得到</strong>。此时满足要求。</p><p>第0位P(PREV_INUSE) 则代表前一个chunk是否已经被分配，如果为1则代表已分配，是一个allocated chunk，为0则代表未分配，是一个free chunk。</p><p>第1位M(IS_MAPPED)，表示当前chunk对应的是哪个区域的虚拟内存，1表示文件和匿名映射区，0表示heap区</p><p>第2位N(NON_MAIN_ARENA)，表示chunk是否是thread arena</p><p>此时的free chunk：</p><p><img src="https://img.aixheee.top/7077ffab-568d-4fb5-9ff4-d2ca421ee8cd.png"></p><p>allocated chunk：</p><p><img src="https://img.aixheee.top/468fb77b-e430-447c-8636-a789d05ca7a5.png"></p><p>同理，发现对于不同的标志位，只需要存储一份即可。所以，将原先的footer中的标志位部分统一为prev_size，并挪动到chunk头部</p><p><img src="https://img.aixheee.top/a36e521b-955b-402f-80a3-061b58a692f9.png"></p><h3 id="如何组织堆虚拟内存"><a href="#如何组织堆虚拟内存" class="headerlink" title="如何组织堆虚拟内存"></a>如何组织堆虚拟内存</h3><p>如果我们通过隐式链表的方式管理chunk，则总会涉及到chunk的遍历，效率极低，<strong>由此引入了半侵入式链表来提高堆内存的分配和释放效率。</strong></p><p><strong>bin中各个free chunk是如何连接在一起的？</strong> free chunk的frea area中会有两个指针，分别指向当前chunk所属链表中的前一个chunk和后一个chunk</p><p>链表中的节点即为free chunk，链表称之为<strong>bin</strong>；为了将这些bin管理起来，通过数组<strong>fast bins和bins</strong>来将所有bin的头结点存储起来，二者同属于malloc_state</p><pre><code>struct malloc_state{  ……  /* Fastbins */  mfastbinptr fastbinsY[NFASTBINS];  ……  /* Normal bins packed as described above */  mchunkptr bins[NBINS * 2 - 2];  // #define NBINS    128  ……};</code></pre><p><img src="https://img.aixheee.top/d7b8c7f9-aaee-4fd6-ba81-9a41a2199500.png"></p><p><img src="https://img.aixheee.top/bdf0150a-a8d7-4494-be35-aa07dda8bdf9.png"></p><h4 id="fast-bins"><a href="#fast-bins" class="headerlink" title="fast bins"></a>fast bins</h4><p>（规定下文中，chunk size代表malloc_chunk的实际大小，chunk unused size为实际可用大小，前者比后者大16字节，用于存储prev_size，size以及两个指针）</p><p>作用：存放小尺寸且快速分配的空闲块</p><p><strong>设计目的：快速地分配和释放小内存</strong></p><p>程序中会经常申请和释放较小的内存空间。如果一释放就被分配器合并为一个较大的chunk，那么一旦又有小块内存的请求，那么分配器又需要把大的空闲内存做切分，无疑是比较低效的。所以，引入了较小块内存fast bin。</p><p><strong>特点：</strong> 链表特点：每个fast bin都是单链表且总数为10，10个fast bin中所包含的fast chunk size是按照递增16字节排序的，比如第一个fast bin中所有的fast chunk size为16字节，第二个fast bin中所有的fast chunk size为32字节</p><blockquote><p>为什么是单链表+LIFO？通过单链表就可以实现删除和添加操作。删除(malloc分配)即进行头删，添加(free释放)操作即头插，不需要对中间的节点进行操作，为O(1)操作。之所以要LIFO，是因为根据temporal locality原理，刚释放的chunk很可能马上再用，LIFO可以提高效率</p></blockquote><p>节点特点：fast bin中的fast chunk大小较小，chunk unused不大于64字节，且所有的fast chunk不会自动合并，有<strong>延迟合并的特点</strong>。</p><p>合并只会发生在malloc请求large chunk、malloc请求smallbin但是smallbin为空、free 非 fastbin chunk或显示地调用malloc_consolidate()，此时会触发malloc_consolidate()，将所有的fastbin chunk取出并与相邻块合并，放入到unsorted bin中</p><p>此外，会将fast free chunk的prev标志位置为1，保证不会对fast free chunk进行合并操作，这是其设计目的决定的。</p><h4 id="unsorted-bin"><a href="#unsorted-bin" class="headerlink" title="unsorted bin"></a>unsorted bin</h4><p>作用：当一个chunk被释放时，它首先被放入unsorted bin，而不是直接放入其他bins。其是free后chunk的“缓冲区+热缓存层”，用于延迟分类并提高复用概率</p><p>设计目的：1.<strong>降低free成本</strong>。如果每次free内存，都立刻判断是smallbin还是largebin，larginbin还要维护有序结构，并加入到其中，效率较低。所以，会首先合并相邻chunk并先放进unsorted bin中</p><p>2.<strong>提高命中率</strong>。程序中有个重要现象——刚释放的内存，大概率会被马上申请，可以理解为访问的局部性原理。所以如果把这块内存又放到smallbin&#x2F;largebin中，相比于将这些热数据放在一个unsorted bin中，其查找时间会更多</p><p>特点：只有一个unsorted bin，其为双向链表且不排序，FIFO——头插(释放内存)，遍历时从尾部开始查，并优先尾删(malloc分配)</p><blockquote><p>为什么是双向链表 + FIFO？因为遍历时可能删除中间节点，而单链表删除中间节点的成本高，为O(n)，在知道要删除的节点的情况下，需要通过遍历获取前驱节点。FIFO的效果是，如果freeABC，则顺序为C–&gt;B–&gt;A，但是malloc检查是从尾部A开始检查的，目的是让最早被释放的内存优先被检查，防止某些块长期滞留</p></blockquote><h4 id="small-bin"><a href="#small-bin" class="headerlink" title="small bin"></a>small bin</h4><p>作用：用于管理比 fast bins 稍大一些的小尺寸空闲块，支持合并</p><p>链表本身特点：共62个small bin，为双向链表，FIFO——内存释放操作就将新的chunk头插，分配操作就尾删</p><p>节点特点：一个small bin中的所有free small chunk大小相等，第一个small bin的size为16字节，后续递增8</p><h4 id="large-bin"><a href="#large-bin" class="headerlink" title="large bin"></a>large bin</h4><p>作用：用于管理超过 small bin 范围的大尺寸空闲块</p><p>链表本身特点：其大体逻辑和small bin类似，区别在于单一large bin中每个chunk的大小不是相同的，从链表头到链表尾部呈现递增趋势，采用范围管理策略，而不是small bin的固定大小管理策略</p><p><strong>关于bins的初始化时机：</strong></p><p>注意，bins不是“预分配内存”的缓存池，而是已经分配过、又被free的chunk回收站。在第一次malloc前，bins完全是空的，直至第一块内存区域被free，如果大小较小则放入fastbins，否则合并并放入到unsorted bin中，再分类放入到smallbin&#x2F;largebin中</p><h2 id="malloc流程梳理"><a href="#malloc流程梳理" class="headerlink" title="malloc流程梳理"></a>malloc流程梳理</h2><p>1.void* p &#x3D; malloc(size)。内部首先内存对齐(8&#x2F;16字节)，并加上chunk header大小，得到normalized size</p><p>2.tcache中查找。tcache为线程本地缓存，按照size class分桶，LIFO，O(1)查找且无锁。但是其容量较小，且只会缓存小对象，在高并发下依然无法替代arena，可能导致频繁的锁竞争</p><pre><code>thread  └── tcache（每线程私有）        ├── size class 1 → 链表        ├── size class 2 → 链表        └── ...</code></pre><p>3.选择arena并获取到其对应的锁，开始从bins中查找何时的chunk，此后的查找都是在持锁的状态下完成的</p><p>4.fastbin中查找。如果nb属于fastbin范围内(80字节左右)，则做<mark>精准匹配</mark>，如果fastbin[index]非空，则返回，否则进入下一次</p><p>5.smallbin。如果nb属于smallbin范围内(小于512字节)，则将nb转换为smallbins索引，做<mark>精准匹配</mark>，并从对应的smallbin中取出一个chunk并返回</p><pre><code>index = size_to_smallbin(nb)if smallbin[index] 非空    取一个 chunk    标记 inuse    返回</code></pre><p>6.unsorted bin延迟分类层。做<mark>最接近匹配</mark>，遍历unsorted bin，在第一次满足nb&gt;&#x3D;chunk.size时，将该chunk从unsorted bin中移除，并将其做切割，将富裕部分根据其大小<strong>插入到smallbin&#x2F;largebin中</strong>，并返回结果。注意，<strong>对于不符合大于等于条件的chunk都会被转移到smallbin或largebin中</strong></p><p>如果遍历完unsorted bin依然没有找到合适块，即nb&lt;所有chunk.size，此时unsorted也会被清空，因为已经被分类</p><pre><code>for (chunk in unsorted):    if chunk.size &gt;= nb:        从 unsorted 摘除        if chunk.size - nb &gt;= MIN_CHUNK_SIZE:            切割            remainder 分类插入 smallbin/largebin        返回前半部分    else:        将该 chunk 分类到 smallbin 或 largebin</code></pre><p>7.largebin。做<mark>最接近匹配</mark>，目标是找到&gt;&#x3D;nb的最小块，会先定位到index，得到largebins[index]，再在其中找到目标快，如果有剩余则做切割，将剩余部分插入到smallbin或largebin中，并返回</p><p>8.top chunk。如果top.size()&gt;&#x3D;nb，则从top中做切割，将剩余部分插入到smallbin或largebin中，更新top指针，并返回</p><p>9.向系统申请内存以扩展堆。如果nb&lt;128KB，则调用brk()扩展heap，更新top chunk并再次尝试分配；如果nb&gt;&#x3D;128KB，则调用mmap()创建匿名映射，返回mmap chunk</p><p>10.返回地址。此时，用户得到的是chunk+header_size，但是还没有物理页</p><p>可能存在的问题：</p><p><strong>既然unsorted bin作为热缓存层，为什么是malloc查找顺序是fastbin-&gt;smallbin，而不是先查unsorted bin？</strong></p><p>因为smallbin是精确匹配层，如果请求在smallbin的范围内，直接通过O(1)时间复杂度即可确定是否存在可用的内存块：</p><pre><code>smallbin[index] 非空 → 直接返回</code></pre><p>相比之下，unsorted bin里chunk大小杂乱，查找unsorted bin的代价是O(n)，且查找到后可能需要进行分割。</p><p>malloc分配内存的设计原则是，先查找“确定性精确命中”的结构，再查找“需要遍历判断”的较低效结构</p><p><strong>是否所有的内存块在分配时，都需要切割？</strong></p><p>不是。如果想要让smallbin命中，必须要请求大小和small chunk大小完全相同，即必须做完全匹配而不是最恰当匹配，由此来避免低效的查找。这是空间利用率与速度的折中。</p><p>如果unsorted bin或者large bin命中，则需要进行切割</p><h2 id="free流程梳理"><a href="#free流程梳理" class="headerlink" title="free流程梳理"></a>free流程梳理</h2><p>1.定位chunk。free(ptr)将根据ptr定位到chunk的Header地址，获取PREV_INUSE、IS_MAPPED和NON_MAIN_ARENA信息</p><p>2.检查是否是mmap分配的chunk。通过查看IS_MAPPED标志位，可以判断是否是mmap分配的——如果是，则通过munmap(chunk,size)<strong>将虚拟地址区间移除、VMA移除、页表项删除并立刻释放物理页，归还给操作系统</strong>，不涉及任何合并以及bin；如果不是，则进行_int_free()函数来合并释放</p><p>3.检查是否属于fastbin大小范围。如果size &lt;&#x3D; 64B&#x2F;128B，则将其头插到fastbin的单链表中，<strong>不做合并</strong>(延迟合并，因为小块频繁分配释放，很可能马上又malloc，合并的成本太高了)，不清除PREV_INUSE</p><p>4.说明属于smallbin或是largebin的大小范围，<strong>进行合并</strong>。合并目标是，尽可能地让当前chunk和前一个chunk、后一个chunk合并。<strong>对于前一个chunk</strong>——通过Header中的PREV_INUSE标志位来判断前一个chunk是否为free，如果是free则通过chunk - prev_size来获取到前一个chunk的起始地址并合并；<strong>对于后一个chunk</strong>——通过chunk + size获取到下一个chunk的Header，检查其是否被使用，如果未被使用，则进行合并。合并的流程——从对应的bin中unlink(nextChunk)，并让当前chunk-&gt;size +&#x3D; nextChunk-&gt;size</p><p>5.合并完成，将合并后的块放入到unsorted bin中。<strong>此时，虚拟地址依然存在、VMA存在、物理页也依然存在，并没有归还给操作系统，allocator认为可重用</strong></p><p>6.特殊情况：合并到了top chunk。如果释放的块后面是top chunk，即chunk + size &#x3D;&#x3D; top，则直接扩展top，不进入unsorted bin。<strong>此时，如果top_size &gt; trim_threshold(一般是128KB)，就会调用systrim，进而调用brk(new_end)，让heap缩小，缩减mm_struct映射的堆区域与对应的vm_area_struct映射的虚拟地址范围，清除对应的页表项，并真正释放物理页，将物理内存归还给操作系统</strong></p><p><strong>重新理解free：</strong></p><p><strong>从进程的视角来看：</strong> free是“进程管理内存资源的行为”，等同于将这块内存的使用权还给了allocator，但是这块内存是否会交还给操作系统，取决于分配器策略，并不是free的必然结果</p><p><strong>从数据结构的视角来看：</strong> allocator维护bins以及chunk状态，free本质上是对这些数据结构的操作，会根据ptr找到chunk并标记为free、尝试进行合并操作并插入到bin中，本质上是对堆管理数据结构的操作</p><p><strong>从虚拟内存的视角来看：</strong> 对于小块内存(brk())，free不等同于删除虚拟页，VMA、页表依然存在且有效、PTE访问权限没有变化，此时并不是硬件层面禁止访问，只是属于未定义行为。比如，下面的代码通常不会立刻报错</p><pre><code>free(p);*p = 1;</code></pre><p>对于大块内存，才会删除vm_area_struct、将物理页归还给操作系统，此时访问是硬件层面的错误</p><p><strong>从内核视角来看：</strong> 绝大多数free并不会立刻进入内核，因为内存块已经交给allocator管理。只有在munmap、brk收缩和进程退出时才会进入内核态，调整映射，真正把物理内存收回</p><p><strong>从硬件的视角来看：</strong> CPU完全不知道，CPU只做虚拟地址到物理地址的映射，free不会修改CR3、不会刷新TLB、不会改变PTE也不会触发page fault，除非munmap</p><p><mark>究竟什么情况下才会真正将物理内存归还给操作系统？</mark></p><p>一是mmap映射的区域直接通过munmap()归还，二是brk()调整收缩堆大小，内核才会调整虚拟映射、归还物理内存，此时的物理内存才可被其他进程使用</p><p><strong>注意，释放内存必须从高地址，即heap的尾部开始释放。</strong> 例如下图中，只有最右边free区域可以被归还，中间的free块永远无法被归还</p><pre><code>| allocated | free | allocated | free(top) |</code></pre><p><strong>不能归还中间部分的虚拟地址，这是由VMA机制决定的。</strong> mm_struct通过heap_start和brk指针分别指向堆底和堆顶，vm_area_struct同样也会用start和end指针指向开始和结束地址。二者都说明了，这块虚拟内存区域是连续的。如果强行删除，会导致VMA被拆成两段，二者的语义被破坏，glibc的连续heap模型崩溃。所以，brk机制天然不支持中间部分裁剪</p><h2 id="内存碎片"><a href="#内存碎片" class="headerlink" title="内存碎片"></a>内存碎片</h2><p><mark>内存碎片是在什么情况下产生的？</mark></p><p><strong>第一层：allocator层碎片</strong></p><p>外部碎片(空间总量够 但不连续)——malloc产生大小不同的chunk，切割时会产生剩余碎片；fastbin延迟合并，以性能换空间</p><p>内部碎片——chunk对齐导致</p><p><strong>第二层：虚拟内存层</strong></p><p>外部碎片：brk heap中的不可裁剪空洞，由于heap只能是单一连续的VMA 只可以尾部shrink，中间free无法通过brk归还</p><p><strong>第三层：物理内存层</strong></p><p>外部碎片：缺页中断触发物理页的分配，但是一张张4KB的页被打散分配给了不同的进程使用，如果此时某个进程需要2MB的连续页，可能难以分配，需要swap out</p><p><mark>内存碎片如何处理？何时处理？由谁处理？</mark></p><p><strong>对于虚拟地址碎片：</strong></p><p>heap空洞：中间的free chunk的虚拟地址仍然要保留，在一定时间内不能归还。其<strong>由ptmalloc来管理，这部分碎片的处理方式，需要等待未来被malloc复用——要不就是best-fit减少碎片，要不就是与相邻区间合并</strong></p><p><strong>总结一下，对于虚拟地址碎片，ptmalloc都有哪些处理手段——free时对smallchunk&#x2F;largechunk立即合并、fastbin的延迟合并、unsortedbin的优先复用以避免频繁分割、trim的尾部处理；malloc时的best-fit策略，“尽可能保留大块内存，尽可能减小切割剩余大小”</strong></p><p><strong>对于物理内存碎片：</strong></p><p>虽然虚拟地址不能被回收，但是物理页是可以回收的。</p><p>1.正常情况下，free后虚拟地址未被回收，页表映射关系依然存在，但是如果长期不访问，则在内存紧张的情况下会被swap out</p><p>2.现代glibc在一些条件下会主动madvise。此时，虚拟地址仍然存在，页表也依然存在，不过物理页被回收</p><p>说完何时回收，下面说说如何合并，减少物理内存碎片：Linux下buddy system对物理页进行管理，在物理页已经被回收，完全脱离进程映射后，buddy system可以合并相邻的free页以减少物理碎片</p><p><mark>为什么allocator不频繁归还内存？</mark></p><p><strong>设计目标是——优先重用，减少无效、昂贵的系统调用。</strong> 一块刚被使用使用过的内存，很大可能会在不久后被再次使用。而如果free时直接把物理内存归还，就意味着需要再次调用brk()&#x2F;mmap()系统调用、建立页表映射，代价太大</p><p><mark>这也解释了为什么free后RSS(Resident Set Size 常驻内存集)可能不下降：</mark>RSS代表当前进程在物理内存(RAM)中实际占用了多少物理页，时操作系统视角下进程的真实消耗的物理内存量</p><p>一是释放内存块并不属于通过mmap映射分配到的大内存块，而是brk()分配的</p><p>二是释放的内存块并不在堆顶</p><p>三是，就算是堆顶的内存块，堆的大小小于trim_threshold(一般是128KB)，glibc就不会调用systrim()来执行brk系统调用收缩堆。</p><p>这三种情况下，在操作系统看来这些内存块依然被进程占用着</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>ptmalloc是glibc默认的内存分配器，通过arena机制降低多线程下的锁竞争，每个线程独立管理堆内存。它以chunk为最小单位，采用fastbin、smallbin、unsorted bin、large bin多级缓存策略，兼顾分配效率和碎片控制。free通常只将内存归还给分配器而非操作系统，只有大块mmap内存或堆顶收缩时才会真正释放物理页，这种设计以空间换时间，优先重用避免频繁系统调用。本篇文章主要梳理了malloc的流程，下一篇则主要聚焦于“为什么”的角度，梳理malloc为什么要这样做。在接下来的系列文章中，我们将以malloc为起点，逐步构建完整的内存管理知识体系。</p>]]></content>
      
      
      <categories>
          
          <category> 内存分配与管理 </category>
          
      </categories>
      
      
    </entry>
    
    
  
  
    
    
    <entry>
      <title>关于</title>
      <link href="/about/index.html"/>
      <url>/about/index.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/css/custom.css"/>
      <url>/css/custom.css</url>
      
        <content type="html"><![CDATA[/* 页脚与头图透明 */#footer {  background: transparent !important;}#page-header {  background: transparent !important;}/* 白天模式遮罩透明 */#footer::before {  background: transparent !important;}#page-header::before {  background: transparent !important;}/* 夜间模式遮罩透明 */[data-theme="dark"] #footer::before {  background: transparent !important;}[data-theme="dark"] #page-header::before {  background: transparent !important;}]]></content>
      
    </entry>
    
    
    
    <entry>
      <title>categories</title>
      <link href="/categories/index.html"/>
      <url>/categories/index.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
    
    <entry>
      <title>友链</title>
      <link href="/link/index.html"/>
      <url>/link/index.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
    
    <entry>
      <title>第一个标签</title>
      <link href="/tags/tags.html"/>
      <url>/tags/tags.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
    
    <entry>
      <title>音乐</title>
      <link href="/music/index.html"/>
      <url>/music/index.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
  
</search>
