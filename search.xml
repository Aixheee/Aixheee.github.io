<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>【内存分配与管理】从值语义出发，理解C++内存管理设计逻辑</title>
      <link href="/posts/0.html"/>
      <url>/posts/0.html</url>
      
        <content type="html"><![CDATA[<p>在具体展开C++内存管理之前，本篇文章将从值语义这一C++设计基石出发，理解C++内存管理的根因缘由。你将明白为什么C++选择这条看似复杂但极致高效的道路，以及这种选择如何深刻影响着从对象生命周期到智能指针设计的每一个角落。</p><h1 id="值语义与C"><a href="#值语义与C" class="headerlink" title="值语义与C++"></a>值语义与C++</h1><p><strong>值语义</strong>是指对象的复制会创建一个独立的副本，每个副本都有自己的数据，修改一个副本不会影响其他副本。简单来说，值语义对象就像是数学中的数值，彼此独立，互不干扰。相对的，<strong>引用语义</strong>指的是对象不直接存储数据，而是持有对数据的引用（指针、引用等），多个对象可以共享相同的数据，修改其中一个会影响其他所有共享该数据的对象。</p><p>C++的设计初衷是让用户定义的类型（class）可以像内置类型一样工作，具有同等的地位，让class本质上是值语义</p><blockquote><p>Bjarne Stroustrup在《C++语言的设计与演化》中多次强调这一点：</p><p>“在C++中，用户定义类型应与内置类型具有相同的支持、相同的语法使用方式和相同的效率。”</p><p>“让用户定义的类型与内置类型一样工作良好的目标，是C++抽象机制设计的中心思想。”</p><p>“C++的类机制允许程序员定义与内置类型在使用方式、行为复杂性和效率方面都难以区分的类型……<strong>这是我设计C++的主要目标</strong>：提供一种工具，使得出色的抽象能够以内置类型一样的代价被使用。”</p><p>                                        ——《What is “Object-Oriented Programming”?》</p></blockquote><p><strong>为什么C++要这样设计？</strong></p><p>C++的诞生背景直接决定了这一点，其是在C语言的基础上进行开发的，而C语言天然就是值语义。当然，这里不是说是特地为了兼容C所以才要保留值语义，而是因为C++和C都在追求高性能，值语义恰恰可以满足这一点，且不止满足这一点——值语义的对象可像内置类型一样，直接分配在栈上，而分配在栈上带来了很多好处：</p><ul><li><p>栈的内存的分配&#x2F;释放都是基于移动栈指针完成的，其效率较高</p></li><li><p>栈在内存上是连续的，不需要指针跳转，可以减少间接访问，提高缓存命中率；</p></li><li><p>栈基于局部作用域来管理对象的生命周期，这让内存泄露可能性降低，并可明确地管理对象的生命周期（而不是像GC一样）</p></li></ul><p>我们似乎体会不到”class本质上就是值语义“这一点，但其实际上无处不在，C++设计的背后也为实现这一点做出了大量努力，甚至可以说大量的C++独有的“特性”都和这一点有关（这里的特性指的是，在C#&#x2F;Java这些语言下你从来不需要考虑的内容）：</p><ul><li><p>C++一直在追求的零开销抽象：class作为值语义对象，其不应该有额外的开销；而C#class对象是引用语义通常需要额外的GC信息，具有额外的开销</p><pre><code>class A{    int x;};sizeof(A) == sizeof(int)</code></pre></li><li><p>class对象作为成员时会直接嵌入</p></li><li><p>class的成员的初始化：class成员默认不会初始化，比如值语义的int从来都是未初始化的；C# class对象则是默认初始化的</p></li><li><p>拷贝构造和拷贝赋值：class对象作为值语义对象，必须支持拷贝，所以C++必须定义”class如何复制“，为我们生成默认按照值语义来进行拷贝的成员函数，也就是常说的”浅拷贝“。但是有些class并不符合值语义的要求，比如File、thraed、socket、mutex等资源（这一点下面再具体展开），如果class中存在这些成员，那么就不能按照值语义来拷贝，而是需要我们自定义规则，即Rule of 5，甚至有的时候都不应该支持拷贝，即 &#x3D;delete。而C#中的class是引用语义，永远都是在复制引用（地址），而不是对象本身复制，所以完全不需要考虑这些东西，但是代价就是一切默认共享</p></li><li><p>传参时需要考虑pass-by-value还是pass-by-const-reference：因为值语义对象相互独立，所以拷贝后需要成为一个独立的对象，比如函数传参int时，永远都是拷贝。而如果class对象是一个大对象，那么可能需要通过pass-by-const-reference来避免复制；而C#的class对象是引用语义，天然避免该问题</p></li><li><p>函数返回值的RVO&#x2F;NRVO优化：同样的，因为值语义对象相互独立，所以对象拷贝后需要成为一个独立的对象，比如函数返回int时，永远都是拷贝。而如果class对象是一个大对象，那么需要相应的优化来尽可能避免这次不必要的拷贝；而C#的class对象是引用语义，天然避免该问题</p></li><li><p>继承会出现切片问题，并且多态必须使用引用&#x2F;指针：在C++中Base b &#x3D; d时，这是真的在创建一个新的Base对象，而不止是一个“访问句柄”，但Base对象的内容里只能存放Base部分，所以Derived部分会被裁掉，即切片问题。而如果Deirved部分被裁掉了，那么自然无法发挥多态的作用，所以C++多态必须用引用&#x2F;指针，Base&amp; b &#x3D; d; 没有创建新对象，只是引用了旧对象，Deirved部分仍然存在，多态才成立而C#中Base b &#x3D; new Derived()则是在让Base引用的指向Derived对象而已，对象本体仍然完整，不会发生切割，也就不需要有引用&#x2F;指针才能使用多态的要求</p><pre><code>class Base{    int a;};class Derived : public Base{    int b;};Derived d;Base b = d;</code></pre></li></ul><h1 id="值语义下的问题"><a href="#值语义下的问题" class="headerlink" title="值语义下的问题"></a>值语义下的问题</h1><p>将class作为值语义固然高效，但实际上会有问题。</p><p>回顾一下值语义的定义：值语义对象的复制会创建一个独立的副本，每个副本都有自己的数据，修改一个副本不会影响其他副本。由于需要独立的副本，那么必然要发生拷贝，<strong>那什么class对象的拷贝才是是合理的？拷贝的开销会不会过大呢？</strong> 这两点直接导致C++复杂度增大。</p><h2 id="问题一：值语义的合理性"><a href="#问题一：值语义的合理性" class="headerlink" title="问题一：值语义的合理性"></a>问题一：值语义的合理性</h2><p>对于第一个问题：并不是所有class都能够按照值语义来解释。比如thread线程，难道拷贝一个thread可以让操作系统增加一个一模一样的线程吗？再比如，一个socket链接，难道拷贝一个socket可以让操作系统再增加一条网络连接吗？类似的例子还有很多，比如file、mutex以及内存资源等。</p><p>于是，C++需要引用语义来描述这些具有唯一实体的class——其不允许复制，多个地方需要通过指针&#x2F;引用作为“句柄”来<strong>共享访问</strong>。而这就意味着这些引用语义的对象需要分配在堆上。而堆上对象特点是，生命周期不受约束、内存布局不受约束，即在时间和空间上都是自由的，其自由背后的代价是，内存分配涉及到系统调用，<strong>分配和释放成本较高</strong>；由于生命周期不受控制，所以可能发生<strong>内存泄露</strong>的问题，并存在内存碎片的问题。</p><p>那为什么引用语义的对象需要分配在堆上呢？这两个缺点C++又是怎么尽可能避免的呢？</p><p>先来回答前者：<strong>这里就引出了一个经典的问题：为什么值语义对象默认在栈上，引用语义对象默认在堆上？</strong> 这是因为，引用语义对象的需要被多处共享，对象需要支持跨作用域存在。比如下面这个例子，tex如果分配在栈上，那么随着作用域的结束，tex的生命周期结束，无法被其他对象共享；而如果tex分配在堆上，则不会再受到作用域的限制。</p><pre><code>Texture* Create(){    Texture tex;    return &amp;tex;}</code></pre><p><strong>总的来说，之所以引用语义对象分配在堆上，是因为栈无法支持这种跨作用生命周期的需求，所以往往借助堆分配来获得独立生命周期。而对于值语义对象来说，其并不需要被共享，也就没有这层限制，其既可以分配在栈上，也可以分配在堆上，而栈的效率更高且生命周期管理更简单，所以默认分配在栈上。值语义引用语义和栈堆分配之间没有必然关系，其实际上是由生命周期管理、效率等各方面需求决定的</strong></p><p>对于后者，缺点之一为———堆内存分配&#x2F;释放成本较高的问题，则需要通过allocator来对堆内存的分配进行优化，比如STL allocator、ptmalloc等，一是在时间上提高分配&#x2F;释放时的效率，减少锁竞争，并尽可能地提高缓存局部性，二是在空间上提高利用率，减少内存碎片问题。ptmalloc的设计已在前几篇文章中展开，对于STL allocator会在后续文章中讲解</p><p>缺点之二为——堆上资源（不止是内存，还有File、thread、socket、mutex等资源）复杂的生命周期问题。其是C++必须投入大量设计精力解决的部分，也是平时我们写代码时直接接触到的</p><p>引用语义相比于值语义：值语义不存在共享对象的概念，资源对象只属于该class自己，对象被分配在栈上，作用域结束，对象生命周期结束，资源对象的生命周期也结束，资源被回收，万事大吉。这十分便捷，就像你从来不会关心一个int变量的生命周期一样，而这实际上就是RAII——将资源的生命周期和对象的生命周期绑定</p><pre><code>void Func(){    std::string s = &quot;abc&quot;;}</code></pre><p><strong>而引用语义下不一样，由于对象被共享，所以此时对象的生命周期不再简单地跟着作用域走，而是取决于最后一个使用它的class。</strong> 为此，我们需要额外考虑许多问题：对于一个裸指针&#x2F;引用，其指向的资源对象什么时候释放？被谁释放？是否是共享的？而由于这些问题的存在，所以才出现了资源泄露（我以为其他class负责释放资源，所以我不释放，结果没有任何一个class释放资源）、悬空指针（我明明还在访问该资源，其他class却给释放了）、double free（我以为其他class都不会释放该资源，所以我来释放，结果其他class同样也释放了）、循环引用等等问题。</p><pre><code>TcpConnection* conn = Create();ModuleA(conn);ModuleB(conn);ModuleC(conn);</code></pre><p><strong>而解决这些问题的根源，便是明确规定一个资源到底由谁来管理，即确定资源的所有权。</strong> 而最简单的资源所有权模型，便是unique_ptr，其明确了该资源对象只由该智能指针对象来管理，将资源包装进值对象中。比如下面的例子，一离开作用域则自动delete，RAII便又回来了。这可以说是上C++最重要的思想之一，比如vector、string、lock_guard等等都遵循该规则</p><pre><code>{    std::unique_ptr&lt;Texture&gt; tex = Load();}</code></pre><p>再比如，让类成员unique_ptr持有资源，明确该资源只属于该类对象，不需要被其他对象共享，从而保证资源的正确释放</p><pre><code>class TextureManager{   std::unique_ptr&lt;Texture&gt; textures;};</code></pre><p>既然提到了unique_ptr，那么不得不再说说shared_ptr：程序中确实存在资源对象需要被共享的情况，比如纹理资源、场景资源等，所以需要shared_ptr，其通过引用计数的方式<strong>尽可能</strong>地保证资源的正确释放。</p><p>但其缺点在于，资源的释放时机不再确定，需要额外的内存空间用于存储控制块，并且存在循环引用，在动态环的场景下束手无策，只能通过类似于GC的思路来解决。这也是为什么推荐优先使用unique_ptr。</p><p><strong>此外，通过以上方式来明确资源所有权的另一个好处在于提高代码的可读性——你完全可以通过局部代码来推导出谁负责管理资源，而这实际上间接地防止了资源泄漏问题。</strong></p><p>考虑这样一个函数，在多人协作的场景下，如果你不去看该函数的具体实现，你完全没法判断这个函数会完全接管obj并负责删除释放它，还是只是读取&#x2F;修改资源，容易导致资源泄露或是double free。这个问题的根源同样是资源所有权——MyObj*无法体现出所有权关系</p><pre><code>void Process(MyObj* obj)</code></pre><p>而在有了智能指针后，完全可以通过其来显示地传递所有权信息——unique_ptr&#x2F;shared_ptr代表我（站在函数的角度）拥有资源的所有权，负责其生命周期，即“拥有者”；</p><pre><code>void Process(std::unique_ptr&lt;MyObj&gt; obj)</code></pre><p>T*&#x2F;T&amp;&#x2F;const T&amp;代表我只是临时访问、修改该对象，但是不负责管理其生命周期，即“借用者”，需要外界来管理其生命周期；weak_ptr同样也是代表访问、修改该对象，且不影响其生命周期，但是其和指针&#x2F;引用的区别在于，weak_ptr可以感知到资源对象是否被释放了（lock()），而指针&#x2F;引用无法感知，其默认资源对象始终有效，完全依赖于外部来保证在调用期间该资源对象不会被释放</p><p>以上是引用语义的问题之一——资源所有权不确定导致的资源生命周期的复杂性。<strong>引用语义的另一个问题，更准确说是特点，是引用语义的对象需要分配在堆上，而分配在堆上就导致了效率的降低。这里的效率，即是缓存局部性导致的访问效率降低，也是分配&#x2F;释放对象时的效率降低。</strong></p><h2 id="问题二：值语义的拷贝代价"><a href="#问题二：值语义的拷贝代价" class="headerlink" title="问题二：值语义的拷贝代价"></a>问题二：值语义的拷贝代价</h2><p><strong>现在，让我们来看看之前提到的第二个问题——值语义所带来的拷贝开销问题。</strong></p><p>为了解决该问题，编译器首先出现了RVO&#x2F;NRVO优化，并在C++17之后RVO被规定为强制优化。RVO&#x2F;NRVO即直接将返回值对象在目标位置上构造，而不需要中间临时对象的参与，减少不必要的拷贝。这使得我们既能够返回值保留值语义，又能有效降低拷贝开销。</p><pre><code>MyClass Func(){    MyClass obj;    return obj;}</code></pre><p>而对于传参时的拷贝代价，则通过引入了移动语义来解决。move的本质是将资源的所有权直接转移，避免了class对象被复制。这个过程中，move依然属于值语义，因为该class对象中的资源对象依然只有一个所有者，只不过所有权被转移了</p><p>而为了能够让move移动语义成立，从而引入了右值引用、万能引用、完美转发等一系列机制，这里不再详细说明它们的底层机制以及彼此间的逻辑关系，后续文章再具体展开</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>总结一下，C++追求class作为值语义对象，其有很多优点，但是现实情况下并不是所有对象都能简单地复制、按照值语义的方式来理解，所以需要引用语义。但是引用语义带来了生命周期的复杂性，资源对象的释放不再简单由作用域决定，而是由引用关系决定，从而出现了资源泄漏等各种问题。而C++便通过资源所有权来解决该问题，产生了unique_ptr&#x2F;shared_ptr等机制，重新通过RAII的思路来管理资源对象。而对于堆分配的缺点，则采用了allocator策略在事件和空间上尽可能地优化。现代C++所有的努力，本质上都是在保证值语义，这也是C++的核心目标之一，其带来了优秀的缓存局部性、确定性的资源管理和较低的运行时开销，但是也引入了复杂的生命周期管理问题以及拷贝问题</p><p>那C#呢？C#的目标是提高开发效率，尽可能降低内存管理的复杂度，所以选择了默认引用语义并通过GC自动追踪引用，大大降低了开发难度，不需要再考虑引用语义下生命周期的问题，但其局部缓存性较差，且GC带来了不确定的析构、运行时较大的额外开销。并且实际上，GC只解决了内存资源的管理，而对于其他资源，C#依然采取了类似于RAII的解决思路，比如using IDisposable等</p><pre><code>using(var file = ...){}</code></pre><p>理解这套哲学，就理解了为什么C++没有选择Java&#x2F;C#那样的“简单”引用语义+GC模型。C++的设计者相信，程序员应该有权在“自动化的便利”和“手动的控制”之间找到最佳平衡点。这种选择让C++变得更复杂，但也让它能够在性能敏感的领域大放异彩。在后续的系列文章中，我们将具体展开C++的内存管理方式。</p>]]></content>
      
      
      <categories>
          
          <category> 内存分配与管理 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>【内存分配与管理】理解new与placement new</title>
      <link href="/posts/0.html"/>
      <url>/posts/0.html</url>
      
        <content type="html"><![CDATA[<p>前三篇文章中，我们已经了解了malloc的底层实现以及与我们平时接触最为密切的的堆栈内存分配，本篇文章将从malloc的底层机制到new的面向对象封装，了解new的设计理念、底层实现以及正确用法</p><h1 id="new表达式"><a href="#new表达式" class="headerlink" title="new表达式"></a>new表达式</h1><p>我们先来从最表层来对比一下malloc与new：<br><strong>1.概念上的区别</strong></p><p>        malloc&#x2F;free是C&#x2F;C++标准库函数，而new&#x2F;delete是C++关键字，或者说是new表达式。因此malloc仅仅只分配内存，而不会进行初始化类成员的工作，new不止分配内存，而且还调用类的构造函数。</p><p><strong>2.返回类型的不同</strong></p><p>        new内存分配成功时候，返回的是对象类型的指针，不需要进行类型转换，从这个角度来说比较安全malloc内存分配成功则返回 void类型（泛型指针），必须通过强制类型转换将 void* 转换成需要的类型。</p><p><strong>3.分配失败后的返回值不同</strong></p><p>        malloc失败，会返回空指针。new失败，默认是抛出异常，要捕获异常bad_alloc</p><p><strong>4.是否需要指定内存大小</strong></p><p>        new操作符申请内存的时候不需要指定内存块的大小，编译器会自动根据类型信息来计算malloc需要显示的指出内存的大小。</p><p>这只是浅层表象，我们该如何理解new呢？</p><h2 id="理解new"><a href="#理解new" class="headerlink" title="理解new"></a>理解new</h2><p><strong>new不仅仅是”申请内存“，本质上是一个将”内存分配“和”对象构造“绑定在一起的语法糖。</strong></p><p>从<strong>底层</strong>来看，其首先调用全局的operator new分配原始内存（通常基于malloc实现），然后通过在这块内存上调用构造函数，对象的生命周期开始。相对的，delete则先调用析构函数结束对象的生命周期，再调用opeartor delete释放内存。</p><p><strong>这背后反应了C++对对象的一个核心理解：对象由”内存存储“和”生命周期“两部分组成，二者在默认情况下是绑定的，目的是提供简单对象语义。</strong></p><p>但是，C++也同样允许我们将二者拆开单独控制，将“内存的生命周期”和”对象的生命周期“解耦，实现更为精细的控制。</p><p>比如，在智能指针make_shared的实现中，对象和控制块是统一分配的，因此内存无法单独释放，但是对象的生命周期需要在强引用归零时结束。此时，就需要通过”字符数组+placement new“将“内存的生命周期”和”对象的生命周期“解耦，并通过显式析构结束对象生命周期。这既保证了及时结束对象生命周期，又不会在释放控制块时导致对资源对象的双重析构</p><p>所以可以说，<strong>new&#x2F;delete 的设计本质是：在提供简单对象语义的同时，也允许程序员在需要时精细控制内存和对象生命周期</strong>，这是 C++ 能实现智能指针、内存池等高级机制的基础。</p><p><strong>现在，我们再来对比一下malloc和new，理解为什么有了malloc&#x2F;free为什么还要new&#x2F;delete？</strong></p><p><strong>从C++面向对象的特点出发：</strong> C语言是面向过程的，其关注的是“数据+操作”，并不严格区分“内存”和“对象”，所以只需要通过malloc&#x2F;free来管理内存的存储期即可。而C++是面向对象的，内存仅仅是对象的一部分，对象还包括构造、析构等生命周期的管理，<strong>需要区分“内存生命周期”和“对象的生命周期”</strong>。<strong>而new&#x2F;delete将内存分配和对象的生命周期绑定起来，做面向对象的封装，从而支持RAII思想与异常安全，符合C++语义要求</strong></p><p>具体来说，RAII要求资源创建即绑定到对象的生命周期中，在构造时申请资源，析构时释放资源，以防止资源的泄露。如果没有new&#x2F;delete，那么用户需要手动malloc+construct+destruct+free，这一流程既繁琐又容易出错，破坏了面向对象的抽象和封装的核心原则，内存管理负担过重</p><p><strong>从异常安全的角度来说：</strong> new&#x2F;delete通过try-catch+回滚析构，保证了在对象构造时能正确调用operator delete来释放申请资源，防止内存泄露。如果没有new&#x2F;delete，那么用户始终都需要手动try-catch来保证对象构造时的异常安全，这使得资源管理逻辑与业务逻辑深度耦合，代码复杂度急剧上升，且难以在所有可能的分支上保证正确的资源释放。</p><h2 id="new底层原理"><a href="#new底层原理" class="headerlink" title="new底层原理"></a>new底层原理</h2><p><strong>new表达式在编译器展开后，首先调用operator new分配原始内存（通常基于malloc实现），然后调用构造函数完成对象的初始化。如果构造函数抛出异常，则会被try-catch捕获，并调用对应参数的operator delete，将operator new申请的内存释放，防止内存泄露</strong></p><pre><code>T* p = new T(args);//类似于下面逻辑：void* mem = operator new(sizeof(T));  // ① 分配try {    p = static_cast&lt;T*&gt;(mem);    p-&gt;T::T(1, 2);                   // ② 构造} catch (...) {    operator delete(mem);            // ③ 构造失败回滚    throw;};</code></pre><p><strong>operator new是C++提供的可重载内存分配函数，用于为new分配原始内存，其默认调用的就是全局的::operator new</strong></p><p>其逻辑是，尝试通过malloc进行内存分配，<mark>如果分配失败</mark>，则会循环调用new-handler处理函数——如果用户没有手动设置，则会直接抛出std::bad_alloc异常；如果设置了，则会在调用后再次尝试malloc，为一个死循环</p><pre><code>// libstdc++-v3/libsupc++/new_op.ccvoid* operator new(std::size_t size) _GLIBCXX_THROW (std::bad_alloc) {    if (size == 0)        size = 1;    void* p;    while ((p = malloc(size)) == 0) {  // 核心：调用 malloc        // 如果 malloc 失败，尝试调用 new_handler        std::new_handler nh = std::get_new_handler();        if (nh)            nh();  // 用户可能释放一些内存        else            throw std::bad_alloc();    }    return p;}</code></pre><h2 id="operator-new重载"><a href="#operator-new重载" class="headerlink" title="operator new重载"></a>operator new重载</h2><p><strong>为什么需要重载？</strong></p><ul><li><p><strong>用于检测使用上的错误。</strong> 比如，对于越界访问new分配的内存的情况，我们可以在内存空间的头部和尾部的额外的空间，填充特定的标志位置，并在operator delete时便可以检查出标志内容是否被修改</p><pre><code>void* operator new(size_t size) {    void* p = std::malloc(size + extra);    // 写 guard bytes    return p;}</code></pre></li><li><p><strong>提高效率。</strong> operator new和operator delete针对的是“通用分配”，其需要对各种内存分配需求做适配，所以表现更为均衡。所以，如果程序大部分情况下，对内存的分配要求都是相同的，则可以制定相应的分配策略</p></li><li><p><strong>统计使用信息</strong></p></li></ul><p><strong>如何写一个正确的operator new？</strong></p><ul><li><p><strong>正确处理size &#x3D;&#x3D; 0的情况</strong>。operator new(0)必须返回一个“可区分”的空指针，常见做法是将size设置为1，并分配一块大小为1的内存块</p></li><li><p><strong>支持new-handler</strong>。在分配失败时，循环调用std::get_new_handler()获取到设定的new_headler函数，让handler决定如何处理，比如释放内存&#x2F;终止程序&#x2F;抛异常等，然后重新尝试分配</p><pre><code>void* operator new(std::size_t size) {    if (size == 0) size = 1;    while (true) {        if (void* p = std::malloc(size)) {            return p;        }        std::new_handler handler = std::get_new_handler();        if (!handler) {            throw std::bad_alloc();        }        handler();  // 可能释放内存或终止    }}</code></pre></li><li><p><strong>处理继承下的operator new。</strong> 类专属的静态operator new默认会被派生类继承使用，但是其往往只是适用于当前类，因此必须在分配内存前先检查size大小是否和当前类类型大小相同，如果不同，则交给默认的全局operator new处理。</p><pre><code>void* Base::operator new(std::size_t size) {    if (size != sizeof(Base))        return ::operator new(size);  // 交给全局 new    // 否则：走 Base 专属逻辑}</code></pre></li></ul><p><strong>相对的，只要我们自定义了operator new，就必须提供对应的operator delete，否则在构造失败的路径下会导致未定义行为，如下面代码所示：</strong></p><pre><code>class Widget {public:    static void* operator new(std::size_t size) {        // 自定义分配    }    static void operator delete(void* p) noexcept {        // 对应释放    }    //placement new 版本    static void* operator new(std::size_t size, MemoryPool&amp; pool) {        return pool.allocate(size);    }    static void operator delete(void* p, MemoryPool&amp; pool) {        pool.deallocate(p);    }};</code></pre><p>这是因为，我们在new创建对象时，<strong>如果构造函数抛出异常，会选择调用对应参数版本的operator delete来回收内存</strong>，但是如果找不到相应版本的operator delete，则会回退到全局的operator delete。这就会出问题：new使用的是自定义的策略，而delete使用的却是标准的释放，<strong>分配和释放机制并不匹配</strong>，可能导致内存泄露、破坏</p><pre><code>void* raw = Widget::operator new(sizeof(Widget)); // 成功try {    new (raw) Widget();                           // 构造失败 ❌} catch (...) {    Widget::operator delete(raw);                 // ❗回收内存    throw;}</code></pre><p><strong>那如何写一个正确的operator delete呢？</strong></p><p>其需要注意的类似，一是要正确处理删除空指针，直接return即可；二是同样要检查size大小，防止大小错误的删除</p><pre><code>class Base {        //一如以往，但此刻重点在 operator deletepublic:    static void* operator new(std::size_t size) throw(std::bad_alloc);    static void operator delete(void* rawMemory, std::size_t size) throw();    ...};void Base::operator delete(void* rawMemory, std::size_t size) throw(){    if (rawMemory == 0) return;           //检查 null 指针。    if (size != sizeof(Base)) {           //如果大小错误，令标准版        ::operator delete(rawMemory);     //operator delete 处理此一申请。        return;    }    //现在，归还 rawMemory 所指的内存；    return;}</code></pre><h1 id="placement-new表达式"><a href="#placement-new表达式" class="headerlink" title="placement new表达式"></a>placement new表达式</h1><p>【new、placement new、operator new(size_t, void*)重载之间的关系是什么？】</p><p><strong>new表达式、placement new表达式更像是一种“语法糖”，而operator new重载是底层实现。new和placement new在语法上都是new表达式，不是相互独立的两种语法，编译器生成的代码结构类似，区别在于二者调用到的operator new重载版本不同，进而影响到在构造异常时调用到的opeartor delete版本及其回滚行为。</strong></p><blockquote><p>C++标准：如果内存分配成功但对象构造失败（构造函数抛出异常），且存在与 placement new 参数匹配的 placement delete 函数，那么<strong>编译器应该调用这个 placement delete</strong>​ 来释放内存。</p></blockquote><p><strong>再本质一些，对于普通的new具有资源所有权，由C++保证自动回滚；而placement new需要用户自主决定所有权和回滚行为</strong></p><pre><code>// 二者共同的伪代码void* mem = operator new(...); //调用到的operator new 版本不同try {    construct(mem); //都会构造} catch (...) {    operator delete(...); //构造失败时调用对应的operator delete回滚    throw;}</code></pre><p>具体来说，二者的区别在于：</p><ul><li><p><strong>调用的operator new重载版本不同。</strong> new表达式调用的版本是 operator new(std::size_t size)，其回调用malloc进行内存分配，具体原理见“new底层原理”；<em><em>而默认placement new表达式调用的重载版本是 operator new(std::size_t, void</em> __p)，其只是单纯地返回指针，不会分配内存，不会调用new-handler，不会抛异常。</em>* （当然，也可以调用你自定义的placement operator new版本，以上讨论都是针对C++默认提供的默认版本的placement new）</p><pre><code>// placement new底层调用到的operator new重载，实现简化为：void* operator new(std::size_t, void* ptr) noexcept {    return ptr;  // 只是原样返回指针，不做任何处理！}</code></pre><p>二者实际上都是C++在全局作用于下提供的operator new重载版本</p><pre><code>void* operator new(std::size_t) throw(std::bad_alloc); //normal new.void* operator new(std::size_t, void*) throw();   //placement new.void* operator new(std::size_t,                   //nothrow new.                   const std::nothrow_t&amp;) throw(); </code></pre></li><li><p><strong>异常处理机制不同。</strong> new表达式会调用构造函数，在构造函数抛出异常时，会通过try-catch捕获并通过operator delete释放new表达式通过operator new(std::size_t size)申请的内存，自动完成回滚，防止内存泄露</p><p>而<strong>placement new表达式在面对构造函数抛出异常时，不会做任何事，因为这块内存根本不是它分配的，其不拥有该资源的所有权，所以C++提供相匹配的operator delete重载函数中没有做任何事，需要由用户完成资源的释放。</strong> 换句话说，如果placement new的构造出现了异常，需要用户手动兜底</p></li></ul><p><mark>我们该如何使用？</mark></p><p><strong>基本语法</strong>：new (address) Type (initializer-list) address必须是一个指针，initializer-list是类型的初始化列表</p><pre><code>void* memory = operator new(sizeof(MyClass)); // 1.分配原始内存 MyClass* obj = new (memory) MyClass(/* 构造函数参数 */);  // 2.在指定内存构造对象</code></pre><p><strong>使用注意点</strong>：</p><p><mark>placement new构造出来的对象，其析构与内存释放必须由用户自己负责，而不能直接delete，以保证内存来源和释放相匹配</mark></p><p>从内存管理的角度来说，因为placement new，T* obj &#x3D; new(buffer) T()所初始化的内存，其可能来自于栈、堆、文件和匿名映射区或是内存池中，即可能对应了不同的operator new来分配内存，而delete无法泛化处理，需要我们手动调用析构，让分配内存的operator new和释放内存的operator delete相匹配</p><p>从语义上来说，由于不是通过普通new表达式创建的对象，而是通过operator new(size_t)首先分配内存，再通过placement new表达式调用operator new(size_t, void*)+构造函数来初始化，则<strong>代表了需要将“内存管理”和“对象生命周期管理”分离</strong>，所以不能直接delete，因为delete的底层实现类似析构+调用对应版本的operator delete，又将“内存管理”和“对象生命周期管理”耦合在了一起，属于未定义行为。</p><pre><code>if (obj != nullptr){    obj-&gt;~MyClass();      // 析构    operator delete(obj); // 释放内存}</code></pre><p>尽管有些场景下混搭使用不会出现问题，比如以下场景，3和4可以合为delete(buffer)。但这种行为的性质就像malloc&#x2F;delete或new&#x2F;free混搭一样，也许不会内存泄露，可依旧属于未定义行为</p><pre><code>int main() {    // 1. 在堆上分配原始内存（未构造）    void* buffer = operator new(sizeof(MyClass));    // 2. placement new 构造对象    MyClass* obj = new (buffer) MyClass(42);    // 3. 手动调用析构函数    obj-&gt;~MyClass();    // 4. 手动释放内存    operator delete(buffer);}</code></pre><p>比如以下场景：对于MyClass类，其分配内存优先从内存池中分配内存，归还内存也要归还到内存池中</p><pre><code>class MemoryPool{public:    static inline char pool[1024];    static inline bool used = false;    static void* Allocate(size_t size)    {        //从内存池中分配    }    static void Free(void* ptr)    {        //归还到内存池    }};class MyClass{public:    int x;    MyClass(int v)        : x(v){}    // 普通new走对象池    static void* operator new(size_t size)    {        return MemoryPool::Allocate(size);    }    static void operator delete(void* ptr)    {        MemoryPool::Free(ptr);    }};</code></pre><p>如果我要求某次对象的构造不从对象池分配，而是从全局的堆内存中分配，即调用全局的operator new来分配内存，并通过placement new对其初始化，如果直接使用delete就会导致将全局的堆内存归还到内存池中，出现错误。因为此时delete调用的operator delete是MyClass类中的operator delete，而没有调用到和全局operator new相匹配的operator delete。</p><pre><code>int main(){    // 调用全局的operator new，而不是类中的，会外部分配原始内存    void* buffer = ::operator new(sizeof(MyClass));    // placement new    MyClass* obj = new(buffer) MyClass(42);    // 错误！    delete obj;}</code></pre><p>正确的做法是析构+全局::operator delete，必须让operator new和operator delete配对使用，而不是使用默认的delete</p><p><strong>如果正确地自定义实现placement new表达式？</strong></p><p>实际上，其和“写一个正确的operator new”的行为是一致的，因为本质上都是在写operator new的重载，只不过说的placement new重载，一般都不会在operator new中分配内存，也不会在operator delete中释放内存</p><ul><li><p><strong>placement new和placement delete必须成对设计。</strong> 换句话说，如果提供了operator new(A, B, C)版本的重载，那么也必须提供对应的operator delete(A, B, C)版本的重载，以保证在构造函数抛异常时可以被正确回滚，防止资源泄漏。</p></li><li><p><strong>在类内重载operator new时，需要避免遮盖了正常版本的operator new。</strong> 正确的方法是，创建一个基类，包含所有C++默认提供的operator new，并让需要自定义operator new的类继承该基类</p><pre><code>class StandardNewDeleteForms {public:    // normal new/delete    static void* operator new(std::size_t size) throw(std::bad_alloc)    { return ::operator new(size); }    static void operator delete(void* pMemory) throw()    { ::operator delete(pMemory); }    // placement new/delete    static void* operator new(std::size_t size, void* ptr) throw()    { return ::operator new(size, ptr); }    static void operator delete(void* pMemory, void* ptr) throw()    { return ::operator delete(pMemory, ptr); }    // nothrow new/delete    static void* operator new(std::size_t size, const std::nothrow_t&amp; nt) throw()    { return ::operator new(size, nt); }    static void operator delete(void *pMemory, const std::nothrow_t&amp;) throw()    { ::operator delete(pMemory); }};class Widget: public StandardNewDeleteForms {             //继承标准形式public:    using StandardNewDeleteForms::operator new;           //让这些形式可见    using StandardNewDeleteForms::operator delete;    static void* operator new(std::size_t size,            //添加一个自定的                              std::ostream&amp; logStream)     //placement new    throw(std::bad_alloc);    static void operator delete(void* pMemory,             //添加一个对应的                                std::ostream&amp; logStream)    //placement delete    throw();    ...};</code></pre></li></ul><p><strong>placement new的应用场景？</strong></p><p>一般应用在自定义的内存池——避免频繁地调用malloc&#x2F;free，重复利用同一块内存区域；STL allocator容器的底层实现，consructor函数的本质就是placement new，支持延迟构造，比如vector支持reserve来提前分配内存而不初始化</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>从malloc的底层机制到new的面向对象封装，我们看到了C++在内存管理哲学上的重大演进。new&#x2F;delete不仅是简单的语法糖，更是C++面向对象核心思想的体现——将内存分配与对象生命周期管理绑定，为RAII和异常安全提供基础支持。<br>对于大多数场景，直接使用new&#x2F;delete（或智能指针）是最安全的选择；但在需要极致性能或特殊内存管理的场合，理解如何正确重载operator new、使用placement new等技术<br>在后续的文章中，我们将继续探索内存管理的更多话题——智能指针、移动语义等机制如何优化对象生命周期管理</p>]]></content>
      
      
      <categories>
          
          <category> 内存分配与管理 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>【内存分配与管理】关于堆、栈的思考</title>
      <link href="/posts/0.html"/>
      <url>/posts/0.html</url>
      
        <content type="html"><![CDATA[<h1 id="总结概括"><a href="#总结概括" class="headerlink" title="总结概括"></a>总结概括</h1><p><strong>【基本管理规则】</strong> 栈是内存连续的，栈上的内存由系统自动管理，其空间较小，栈上对象的生命周期严格受到作用域的限制。<strong>【优点】</strong> 优点在于栈内存的分配和释放基于移动栈指针实现，速度较快不需要担心内存泄露；并且由于永远遵循先进后出的规则回收，所以内存永远是连续的，不会产生内存碎片。<strong>【缺点】</strong> 但是，为了保证栈的高效，栈布局必须在编译期确定，因此栈对象的灵活性较差，不允许动态分配。 <strong>【示例】</strong> 比如函数调用的返回地址、函数参数、局部变量等都会存储在栈上，在函数调用时移动栈指针开辟函数栈帧，函数结束时通过移动栈指针来释放内存。</p><p><strong>【基本管理规则】</strong> 堆是无序内存，需要由用户手动管理或是通过GC来管理，堆空间更大且生命周期更灵活。<strong>【优点从堆本身定义说起】</strong> 优点从其名字可以看出来，其之所以叫做堆（heap）这个名字，是因为heap在英文中有“堆积、杂乱（一堆）”的意思，按照这个角度来看，更准确的翻译是”堆杂物的内存“，和数据结构的堆（优先级队列）没啥关系，其更可能是形容的是“用户的管理方式”——程序员在获取到heap内存后，相比于栈那样必须连续存储、分配释放顺序固定，heap允许你”杂乱无章“地将自己的东西存储在这块内存上——<strong>生命周期不受约束、内存布局不受约束，即在时间和空间上都是自由的，适合存储动态对象</strong>。<strong>【缺点】</strong> 这种自由是有代价的，其内存分配涉及到系统调用，分配和释放成本较高；此外由于生命周期不受控制，所以可能发生内存泄露的问题，并且还带了内存碎片</p><p><strong>从时间和空间的角度来看，栈 &#x3D; 时间空间均受限，栈上分配的内存大小固定、生命周期固定；堆 &#x3D; 空间时间均自由，堆上的内存大小由运行时自由决定，生命周期由程序员手动管理</strong></p><h1 id="一些问题"><a href="#一些问题" class="headerlink" title="一些问题"></a>一些问题</h1><p><strong>为什么选择了“栈”这个结构？</strong></p><p>栈并不是单一原因产生的，而是<strong>函数调用机制</strong>和<strong>结构化编程</strong>需求共同演化的结果。数调用天然具有后进先出的特性，而结构化编程中的作用域嵌套和局部变量生命周期也符合LIFO模式。ALGOL（一种早期的编程语言）将这两种需求相统一，提出了“栈”这种管理内存的概念</p><p><mark>从结构化编程来说</mark>，<strong>为了解决代码难以维护的问题，而引入了作用域的概念</strong>。通过划分出作用域，不让全局变量满天飞，而是让变量局部化——进入每个作用域时，为其开辟一块内存，退出作用域时，再自动将该内存释放掉，<strong>让变量的生命周期可控</strong>。并且，这些作用域是可以相互嵌套的，所以选择了栈。</p><p><mark>从函数调用机制的角度来说</mark>，函数同样调用天然符合嵌套调用结构，而嵌套结构的返回地址和局部信息符合后进先出的特点，所以选择了栈。程序执行的过程实际上就是在调用一个个的函数，而函数调用的本质是保存返回点+跳转，函数返回的本质是跳回到返回点，这和栈没有必然的关系，完全可以只用寄存器来保存返回的地址。<strong>真正的问题在于函数嵌套调用的场景，必须保证函数返回的地址以及局部信息不会被覆盖</strong>，需要将旧地址和局部信息放到一块内存区域，同时不受到后调用函数的影响，保证一个函数向栈中存的数据和拿出来的数据前后是一致的，<strong>这就要求后存的数据必须先拿，先存的数据必须后拿，即FILO，这才是为什么需要使用栈</strong>。而call指令只是利用了这个结构，将push+jmp相融合</p><p><strong>再向深一层说，为什么函数的调用结构天然符合嵌套的特点？</strong></p><p>这是由冯诺依曼体系结构决定的，冯诺依曼机器一次只能执行一个控制流(PC指针唯一)，且是同步执行流(顺序执行+阻塞等待)。比如A-&gt;B-&gt;C-&gt;D，当当前正在执行A的逻辑时，如果顺序执行到B，那么必须跳转到B的逻辑；当当前正在执行D的逻辑时，C B A函数都必须暂停，D返回时必须先回到D自身的调用点，回到调用点后继续执行C的逻辑。同理，在执行C的逻辑时，B A函数都已经暂停，C返回时必须先回到C自身的调用点，回到调用点后继续执行B的逻辑…..所以，函数的调用天然符合嵌套的结构</p><p>从拓扑结构的视角来看，A-&gt;B-&gt;C的函数调用过程，被调用者必须在调用者继续执行之前完成，也就是说，C没返回前，B也不会继续；B返回前，A也不会继续，其拓扑结构是一棵树，执行顺序是DFS，而DFS的实现结构就是栈</p><p><strong>为什么需要堆？</strong></p><p>换个说法，为什么需要有一块内存区域，通过”堆“的方式进行管理？</p><p><strong>栈是为了满足作用域嵌套、避免全局变量乱飞而提出的，其可以让局部变量自动创建和销毁，使其生命周期可控。</strong> 而这个保证是通过栈指针RSP的移动、函数栈帧的创建和销毁提供的（用函数调用流程解释怎么提供的保证）。具体体现为汇编指令中的RSP -&#x3D; size开辟栈帧，栈帧回退时的RSP +&#x3D; size。而为了保证这些操作足够高效，所以这些汇编指令中的偏移值都需要在编译期完全确定。而要求这些偏移值在编译期确定下来，就导致无法在运行时分配内存，因为会直接影响栈帧布局。</p><p><strong>逻辑链条：生命周期可控-&gt;通过栈帧机制保证-&gt;为了高效，编译指令中的偏移值必须在编译时确定-&gt;内存开辟的大小必须在编译时确定</strong></p><pre><code>void f(int n) {    int a[n]; // C++ 标准不允许（VLA）}</code></pre><p>那如果要开辟多大内存根本没法在编译期确定呢，不就导致无法在编译器确定偏移值了吗？比如网络请求响应、用户输入了多少数据、文件有多少数据，会根据运行时信息决定的需要的内存块的大小，该怎么办？<strong>所以，我们需要一块内存区域，允许将内存的分配推移到运行时，但这也意味着，没法再依赖于LIFO对内存进行生命周期管理，需要由程序员手动管理。我们将这块内存区域称作为堆。</strong></p><p>从时间和空间的角度来看，栈 &#x3D; 时间空间均受限，栈上分配的内存大小固定、生命周期固定；堆 &#x3D; 空间时间均自由，堆上的内存大小由运行时自由决定，生命周期由程序员手动管理</p><p>之所以叫做heap这个名字，更可能是形容的是“用户的管理方式”——程序员在获取到heap区域的内存后，相比于栈那样必须连续存储、分配释放顺序固定，heap允许你”杂乱无章“地将自己的东西存储在这块内存上——生命周期不受约束、内存布局不受约束。按照这个角度来看，更准确的翻译是”堆杂物的内存“，和数据结构的堆（优先级队列）没啥关系</p><p><img src="https://img.aixheee.top/80a470d6-1030-4a72-81b4-5df2a14bb8b5.png"></p><p><strong>为什么栈由高地址到低地址增长，堆由低地址到高地址增长？</strong></p><p>从堆栈设计的角度：</p><p>需要综合考虑二者，二者都是动态增长的，相向动态增长是最好的选择，可以尽可能地降低二者的冲突，充分利用内存。如果二者都是向着同一个方向增长，比如都向着高地址增长，出现的问题：如何确定栈的起始位置&#x2F;栈底位置？</p><p>这个位置决定了栈的大小和堆的大小的堆内存的划分分配。但是二者的大小并不是绝对的，在有些情况下，可能堆用的空间更大，有时就是栈用的空间更大。所以，会出现这种情况——一个程序因为栈溢出而崩溃，但是程序却有大量的堆空间没有利用。</p><p>如果要支持动态调整栈底的位置，则涉及到移动整个栈的数据，太过麻烦。</p><p>从硬件的角度：现代CPU指令集大部分都定义好了栈顶指针push pop的指令，默认规定了栈是从高地址向着低地址增长的</p><p><strong>栈是否可以动态增长？</strong></p><p>当栈的空间被耗尽时，会触发缺页异常，通过异常陷入内核态后，异常会被内核的expand_stack() 函数处理，进而调用 acct_stack_growth() 来检查是否还有合适的地方用于栈的增长。如果当前栈的大小小于8M（通常情况下），那么栈会被扩容，否则会发生栈溢出(stack overflow)，进程收到段错误(segmentation fault)信号。</p><p>动态栈增长是唯一一种访问未映射内存区域依然合法的情形，其他情况下都会直接触发页错误，进而导致段错误</p><p><strong>为什么栈内存分配速度快，堆内存分配速度慢？</strong></p><p>栈：</p><p><mark>分配机制的角度</mark>：栈空间的分配极为简单，内核通过rbp指向栈底，rsp指向栈顶，<strong>栈分配内存的本质就是移动rsp栈顶指针预留空间，而释放就是rsp栈顶指针回退，O(1)时间复杂度完成。</strong></p><p><mark>系统调用的角度</mark>：这个过程中几乎不涉及系统调用，程序启动时，内核给进程分配一块固定大小的栈空间，后续完全是在用户态移动rsp指针(除非触发栈增长或是缺页异常)</p><p><mark>硬件层面的角度</mark>：栈空间是连续的，缓存命中率高；栈对应的物理页是少量连续的，TLB局部性好</p><p>堆：</p><p><mark>分配机制的角度</mark>：堆内存是任意顺序分配的，所以malloc首先需要找到合适的空闲块，如果块过大还需要split，涉及到一系列数据结构的增删查改操作</p><p><mark>系统调用的角度</mark>：当堆空间不够用时，如果申请的是小于128K的小块内存——使用do_brk()系统调用来调账堆中brk指针的指针来增加或者回收堆内存；如果申请的是大于128K的大块内存——则会调用mmap在虚拟地址空间中的文件映射与匿名映射区中，创建出一块VMA内存区域(这里是匿名映射)。涉及到了用户态和内核态的切换、页表的修改、TLB flush，成本较高</p><p>维护堆空间：内核需要处理内存碎片，合并相邻空闲块等</p><p><mark>多线程同步问题</mark>：对于栈来说，每个线程独立栈，不需要加锁；对于堆来说，堆是全局共享的，需要加锁</p><p><mark>硬件层面</mark>：堆并不是连续的，是通过双向链表管理起来的，缓存命中率不高；此外，堆可能对于大量的离散页，TLB miss更频繁</p>]]></content>
      
      
      <categories>
          
          <category> 内存分配与管理 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>【内存分配与管理】malloc设计哲学</title>
      <link href="/posts/0.html"/>
      <url>/posts/0.html</url>
      
        <content type="html"><![CDATA[<p><strong>上篇文章中主要讲解了malloc底层的原理机制，本篇文章则侧重于讲解为什么malloc要这样设计</strong></p><h1 id="allocator要解决的核心矛盾在哪？"><a href="#allocator要解决的核心矛盾在哪？" class="headerlink" title="allocator要解决的核心矛盾在哪？"></a>allocator要解决的核心矛盾在哪？</h1><p><mark>allocator需要在有限的时间开销下，尽可能高效地利用虚拟地址空间，其必须在 分配&#x2F;释放的时间复杂度 与 堆空间的碎片率(空间利用率) 之间做权衡</mark></p><p><strong>追求速度意味着：</strong></p><p>为了快，allocator通常会使用固定内存大小块、此时可以做到O(1)分配，减少了遍历、合并和切割操作，更倾向于粗粒度管理内存块，比如fastbin和smallbin。但这样做的结果就是，由于不能切割，用户可能拿到“偏大的块”，导致内部碎片；同时由于不能频繁合并，在短时间内会导致外部碎片。<strong>速度增加—&gt;碎片增多—-&gt;空间利用率下降</strong></p><p><strong>追求空间利用率意味着：</strong></p><p>为了提高空间利用率，allocator必须做精细切割、频繁合并，通过遍历做best-fit并维护更复杂的数据结构(红黑树&#x2F;ordered bins)。这样做的结果就是，malloc&#x2F;free成本增大，多线程下锁竞争更严重。<strong>空间利用率升高—&gt;时间开销增大</strong></p><p><mark>所以，ptmalloc的分层设计，本质上就是把目标拆分到不同层级，用不同的策略尽可能优化。但是，不存在完美通用的allocator，对于allocator的不同实现，本质上就是在对这组平衡做出不同的权衡</mark></p><h1 id="内存池各层设计思路："><a href="#内存池各层设计思路：" class="headerlink" title="内存池各层设计思路："></a>内存池各层设计思路：</h1><p>如何解决矛盾？</p><p><strong>小块追求常数时间，大块追求空间利用率。因为前者是高频查找路径，需要提高查找效率；后者对空间利用率影响大，需要提高空间利用率。通过分层和延迟策略在性能和空间碎片间取得平衡</strong></p><p><strong>fastbin&#x2F;smallbin：热路径优化，对于小内存需要高频查找，设计思想是用空间换时间</strong>，采用exact-fit的思想，“只匹配完全相同大小的块”，虽然不合并会产生内存碎片，但是换来了free和malloc都是O(1)。如果采用best-fit，则势必要进行O(n)查找，并插入排序</p><p><strong>unsorted bin：free后chunk的“缓冲区+热缓存层”，用于延迟分类并提高复用概率。</strong> 程序中的访问的局部性原理——刚释放的内存，大概率会被马上申请，如果把这块内存又放到smallbin&#x2F;largebin中，下次malloc又要去查找，是浪费时间。所以，<strong>采用了经典的系统优化策略——写操作快、读时再整理，将unsorted bin作为一个缓冲区。</strong> 即，在free时不做复杂的分类，在malloc时再做分类。free时，所有合并完成后的内存块都会直接放到unsorted bin中，以利用访问的局部性原理；malloc时检查目标块是否是unsorted bin中的chunk，如果不是，则再把该chunk分类放回到smallbin&#x2F;largebin中，可以理解为访问的局部性原理此次失效</p><p><strong>largebin：属于低频大块路径，设计思想是时间换空间，允许牺牲一部分时间，以O(n)查找来换取更优的空间。</strong> 其选择了best-fit策略，即“选择最接近请求大小的空闲块”，目的是“最小化切割后的剩余空间，保留大块空间”：如果要180B内存，则应该找200B进行切割，而不是1000B。因为1000B切割后，剩余的空间的820B空间不大不小，也许很难被利用，这也是 <strong>外部碎片的常见来源——大块被切割为中等大小，导致难以适配未来请求</strong>；而切割200B剩余的20B内存较小，可以较易被选取.</p><p><strong>为什么不是first-fit？</strong> first-fit就会导致更严重内存碎片问题，例如连续多次的300B请求，会把1000B大块分割，最终剩下100B，难以分配</p><h1 id="brk-与mmap-决策思路"><a href="#brk-与mmap-决策思路" class="headerlink" title="brk()与mmap()决策思路"></a>brk()与mmap()决策思路</h1><p><strong>注意，常常说调用malloc就是直接通过brk()&#x2F;mmap()申请内存，实际上，这并不完全准确。虽然内存分配确实通过二者来实现，但是只有申请的内存较大，无法在bins中找到合适的缓存内存块时，才会调用这两个系统调用来分配内存。</strong></p><h2 id="1-brk-：堆区的线性扩展工具"><a href="#1-brk-：堆区的线性扩展工具" class="headerlink" title="1.brk()：堆区的线性扩展工具"></a>1.brk()：堆区的线性扩展工具</h2><p>核心机制：通过移动进程堆顶指针来动态扩展内存空间。</p><p>特点：</p><p>1.虚拟内存和物理内存的延迟绑定：brk仅仅修改虚拟内存边界，并不会理解分配物理内存。只有在首次访问虚拟内存，才会触发缺页中断，真正分配物理内存并建立映射关系</p><p>2.堆区内存的顺序释放：由于Linux VMA机制以及其分配内存的方式，所以释放内存时必须先释放高地址内存，再释放低地址内存。</p><h2 id="2-mmap-：独立映射"><a href="#2-mmap-：独立映射" class="headerlink" title="2.mmap()：独立映射"></a>2.mmap()：独立映射</h2><p>核心机制：建立虚拟内存区域和物理内存区域的映射关系</p><p>特点：</p><p>1.支持非连续内存分配与大块内存。</p><p>2.零拷贝特性提升效率。</p><p>3.独立释放避免内存碎片化。mmap通过munmap可以独立释放内存，减少内存的碎片化问题</p><h2 id="3-对比与选择"><a href="#3-对比与选择" class="headerlink" title="3.对比与选择"></a>3.对比与选择</h2><p>在内存区域上：brk分配的内存空间位于堆上，而mmap分配的内存空间位于文件映射区上</p><p>内存地址连续性：brk分配的内存区域属于堆的一部分，地址连续；mmap分配的内存是独立的，其通过vm_area_struct独立管理起来</p><p><strong>由这两个特点，我们可以推断出两种分配方式的内存碎片化程度：</strong></p><p>brk()动态调整brk指针，在堆区上进行地址的线性扩展，所有分配的内存块紧密相连的、连续的。这就决定了，在内存释放时，只有堆尾部的连续空闲区域，才能通过brk向回收，会退给操作系统，而虚拟地址碎片不可以从中间回收，其势必会导致内存碎片化程度较高</p><p>mmap()在文件映射区创建独立的内存映射区域，所有mmap的内存区域不一定是连续的，是多VMA模型。这就决定了在内存释放时，可以独立释放掉一整个mmap区域，内存碎片化程度较低，更利于大块释放</p><p><strong>此外，需要考虑两种系统调用本身的开销：</strong></p><p>brk()只需要动态调整指针值，操作简单便捷，系统调用开销较低，大约为100ns；mmap创建内存映射时，需要创建vm_area_struct并立即建立映射关系，开销较大，大于为500ns</p><h2 id="4-malloc中的策略选择"><a href="#4-malloc中的策略选择" class="headerlink" title="4.malloc中的策略选择"></a>4.malloc中的策略选择</h2><p><strong>在所需内存较小时(小于128KB)时：</strong> malloc选择通过brk()来分配内存。原因在于，小块内存的分配非常频繁，对于这种热路径，我们需要用空间换时间，选择<strong>系统调用成本更低</strong>的brk()。<strong>此外，这些小块的生命周期短，容易复用，不需要立即归还给操作系统，通过bins管理起来即可</strong>，似乎发挥不出mmap可立即归还物理内存的优势，反而增加了分配成本(这也是为什么只有小块内存通过bins管理——其容易复用)。brk()的另一个优势在于，其内存空间都是连续的，<strong>缓存命中率更高</strong></p><p><strong>在所需内存较大时(大于等于128KB)：</strong> malloc选择通过mmap()创建独立的内存映射来分配内存。原因在于，大块内存的分配频率较低，但是<strong>如果划分分配不当，就会导致较为严重的内存碎片。</strong> 此外，这些大块相比于小块，<strong>其占用空间大、生命周期长，不容易复用，所以不需要通过bins管理起来，free时需要尽早归还内存，避免大块长期占用heap中部，阻止heap top shrink</strong>。所以，采取mmap来分配内存(为什么只有小块内存通过bins管理)。尽管效率相对较低，但是瑕不掩瑜，可以理解为用时间换空间</p><h1 id="allocator、VMA和buddy-sysem三者的协作"><a href="#allocator、VMA和buddy-sysem三者的协作" class="headerlink" title="allocator、VMA和buddy sysem三者的协作"></a>allocator、VMA和buddy sysem三者的协作</h1><p>allocator只管理chunk，VMA管理虚拟内存区域，二者通过brk()&#x2F;mmap()&#x2F;munmap()系统调用协作关联，chunk并不与物理内存页有直接协作关系；VMA在缺页中断时，驱动物理页的分配，建立二者的协作关联</p><p><strong>malloc时 allocator与VMA的关联：</strong></p><p>如果直接命中bins中的chunk——VMA没有任何变化。如果是brk()扩展heap——则会扩大heap vm_area_struct的end指针与mm_struct中brk指针，并不创建新的vm_area_strct</p><p>如果是mmap()映射——则创建新的vm_area_struct用于描述这块映射区域，并插入到红黑树以及双向链表中管理起来</p><p><strong>free时 allocator与VMA的关联：</strong></p><p>如果free的是brk块且不触发trim——则只在allocator层有操作，VMA没有任何变化，因为VMA描述的是heap整体</p><p>如果free的是brk块且触发trim(heap shrink)——则会减小heap vm_area_struct的end指针与mm_struct中brk指针，并不销毁vm_area_strct</p><p>如果free mmap块——则删除对应的vm_area_struct，释放页表、释放物理页</p><p>VMA与buddy sysem的关联：缺页中断、页表机制，此处不展开</p>]]></content>
      
      
      <categories>
          
          <category> 内存分配与管理 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>【内存分配与管理】malloc原理剖析</title>
      <link href="/posts/0.html"/>
      <url>/posts/0.html</url>
      
        <content type="html"><![CDATA[<p>本文将主要梳理一下malloc的内存块管理方式，以及其内存池是如何实现的。</p><p>在具体说malloc之前，我们先分清楚几个常见的概念：allocator、malloc和ptmalloc：malloc理解为分配内存的接口，allocator是一个负责内存分配和回收的具体策略实现。在Linux + glibc默认环境下，通过ptmalloc策略来实现(ptmalloc是一个名称)；Google实现了tcmalloc策略，微软实现了mimalloc….</p><h1 id="arena"><a href="#arena" class="headerlink" title="arena"></a>arena</h1><p>ptmalloc针对堆管理，最顶层的管理结构为malloc_state，即arena</p><pre><code>arena（分配器实例）  └── heap（一段连续内存区域）        └── chunk（用户分配单位）struct malloc_state{  // 本arena的锁  mutex_t mutex;  /* Flags (formerly in max_fast).  */  int flags;  /* Fastbins */  mfastbinptr fastbinsY[NFASTBINS];  //当前arena中最高地址的、尚未分配的连续内存块  mchunkptr top;  /* The remainder from the most recent split of a small request */  mchunkptr last_remainder; // small/large/unsorted bins  mchunkptr bins[NBINS * 2 - 2];  // Bitmap of bins   unsigned int binmap[BINMAPSIZE];  // 链表连接所有arena  struct malloc_state *next;  /* Linked list for free arenas.  */  struct malloc_state *next_free;  // 本arena从系统分配的总内存  INTERNAL_SIZE_T system_mem;  INTERNAL_SIZE_T max_system_mem;};</code></pre><p><strong>【arena是什么？】</strong></p><p><strong>每个线程都会绑定一个arena，其代表着一整套完整的malloc管理体系，独立着管理多个heap（非主arena的heap来自于多个mmap区域），这些heap通过链表相连，共用一套bins，而arena通过top指针指向当前正在被使用的heap</strong></p><pre><code>arena  ├─ fastbins  ├─ smallbins  ├─ largebins  ├─ unsorted bin  ├─ top chunk  └─ mutex</code></pre><p><strong>【arena的类别有哪些？】</strong></p><ul><li><p>main arena：即对应到进程的初始堆，可通过brk&#x2F;sbrk系统调用扩展堆大小，即来自进程对应的“堆”，每个<strong>进程</strong>有且只有一个main arena。</p></li><li><p>thread arena：即每个线程独有的arena（不超过数量限制的前提下），需要通过mmap单独映射内存以扩展heap，多个内存块是离散的</p></li></ul><p><strong>为什么不用brk给所有arena扩展？</strong></p><p>brk只能线性增长，但是多线程情况下，需要多块独立的内存，可以分别独立扩展、独立释放。如果线程被回收了，则线程申请的内存同样需要被回收，而对于进程地址空间下的heap来说，其无法做到这一点，必须从top开始释放内存，而通过mmap映射的内存可以通过unmmap单独释放</p><p><strong>多线程程序中可能有多个heap VMA是什么意思？不是所有线程共享同一个heap吗？</strong></p><p>准确来说，<strong>多线程程序中可能存在“多个由mmap 创建的 heap-like VMA”</strong> 指的是，多线程中只能使用mmap作为动态内存的分配来源，这些mmap VMA位于虚拟地址空间中的<mark>文件和匿名映射区</mark>，其不属于进程的heap，但是在线程眼中被当作heap使用。此时，就会存在多个匿名mmap VMA，它们分别属于不同的arena，<mark>但是仍然属于同一个进程地址空间中</mark>。</p><p>一个多线程程序的内存分配情况可能如下所示：</p><pre><code>[ text ][ data ][ brk heap ]      ← main_arena[ mmap arena 1 ]  ← thread_arena1[ mmap arena 2 ]  ← thread_arena2[ mmap arena 3 ][ stack thread1 ][ stack thread2 ]线程1 (绑定主arena)             线程2 (绑定arena1)             线程3 (绑定arena2)      |                               |                               |      v                               v                               v  main_arena                    malloc_state1                   malloc_state2  +-------------+               +-------------+               +-------------+  | mutex       |               | mutex       |               | mutex       |  | fastbinsY[] |               | fastbinsY[] |               | fastbinsY[] |  | top ------------&gt; [heap]    | top ------------&gt; heap1    | top ------------&gt; heap2  | bins[]      |    +------+   | bins[]      |    +------+  | bins[]      |    +------+  | ...         |    | chunk|   | ...         |    | chunk|  | ...         |    | chunk|  | system_mem  |    | chunk|   | system_mem  |    | chunk|  | system_mem  |    | chunk|  +-------------+    | ...  |   +-------------+    | ...  |  +-------------+    | ...  |                     +------+                      +------+                     +------+                       brk堆                          mmap区域                    mmap区域</code></pre><p><strong>我们知道了arena是什么，以及如何获取内存，那么arena是怎么和线程交互的？</strong></p><ul><li><p>如果线程第一次分配内存：</p><ul><li><p>对于第一个分配内存的线程：使用main arena</p></li><li><p>对于其他线程：会遍历已有的arena，尝试找到一个未被其他线程使用的arena绑定（即锁是空闲状态），若所有arena都被占用，则会创建新的arena。arena默认数量上限为8*CPU核数，每个线程都会尝试绑定一个独有的arena，如果arena数量达到上限，则多个线程会共享同一个arena</p></li></ul></li><li><p>如果线程后续需要分配内存：则优先复用先前使用过的arena，如果其已被其他线程占用，则去尝试使用其他的arena，而不是干等着。<strong>这是为了提高内存利用效率，防止明明一个进程分配到了大量内存资源，但线程依然无法获取</strong></p></li></ul><p>【为什么要有arena？其优点和缺点是什么？】</p><p><strong>arena的主要设计目的是为了减少多线程下分配内存的锁竞争。</strong> 如果所有线程的malloc&#x2F;free都需要依赖于同一个堆结构，那么每次分配&#x2F;释放内存需要抢同一把锁，性能会非常低下。</p><p>其缺点在于，在多线程下每个线程都有自己的arena，A线程中的空闲块无法被B线程使用，可能导致明明分配给了该进程较多内存，但是依然不够用的情况，这就会导致<strong>内存膨胀</strong>，整体的内存利用率下降。</p><h1 id="内存池内存管理机制"><a href="#内存池内存管理机制" class="headerlink" title="内存池内存管理机制"></a>内存池内存管理机制</h1><h2 id="为什么堆要用内存池管理内存？而栈不需要？"><a href="#为什么堆要用内存池管理内存？而栈不需要？" class="headerlink" title="为什么堆要用内存池管理内存？而栈不需要？"></a>为什么堆要用内存池管理内存？而栈不需要？</h2><p><strong>从操作系统资源粒度来看</strong>：操作系统只提供了“页级别”的粗粒度管理，内核负责建立VMA、建立页表映射关系、分配物理页框，整个过程都是以页为基本单位的。但是用户程序却需要几十几十字节的内存，对于这种远小于4KB页大小的请求，如果没有allocator作为中间层负责页内细粒度的分配，那么会导致内部碎片严重、内存利用率极低。</p><p><strong>从生命周期控制来看</strong>：堆内存的释放允许任意顺序，生命周期不可预测。如果没有allocator，无法记录哪些内存块是空闲&#x2F;已用的。相比于栈，栈内存的分配是严格按照LIFO——后分配、先释放的，生命周期相互嵌套，出作用域后直接释放，不存在该问题</p><p><strong>从碎片控制的角度来看</strong>：由于堆内存的分配在空间上不是连续的，这就会可能出现外部碎片问题，如果没有allocator，就无法合并、切割，否则很快就无法分配大块内存区域</p><p><strong>从系统调用申请内存的角度来看</strong>：brk()&#x2F;sbrk()&#x2F;mmap()属于系统调用，如果每次申请内存都要调用这三个系统调用的其中之一，触发用户态到内核态的切换，是非常影响性能的。相比之下，栈的内存分配只需要通过移动栈指针即可</p><h2 id="内存池具体管理方法"><a href="#内存池具体管理方法" class="headerlink" title="内存池具体管理方法"></a>内存池具体管理方法</h2><h3 id="如何描述堆虚拟内存"><a href="#如何描述堆虚拟内存" class="headerlink" title="如何描述堆虚拟内存"></a>如何描述堆虚拟内存</h3><h4 id="隐式链表技术"><a href="#隐式链表技术" class="headerlink" title="隐式链表技术"></a>隐式链表技术</h4><p><strong>malloc将整个堆内存空间分为连续的、大小不一的chunk，chunk即为堆内存的最小管理单位。</strong></p><p>为了区分不同的内存块的信息，比如已分配、未分配……会将这些信息直接嵌入到chunk内部</p><p>堆内存要求chunk的大小必须是8的整数倍，因此chunk size的后三位是无效的，为了充分利用内存，就将这三位作为chunk的标志位。</p><p>而padding部分则是用于内存对齐，让整个chunk为8的整数倍</p><p>allocated chunk：</p><p><img src="https://img.aixheee.top/de2b88c5-934d-4d50-b180-15606606372c.png"></p><p>free chunk：</p><p><img src="https://img.aixheee.top/c7ceb5b7-2324-422a-a3d3-fa8209fde08d.png"></p><p>此时，整个堆内存组织为一个连续的，已分配或未分配的chunk序列</p><p><img src="https://img.aixheee.top/54cc37b7-bd5e-4f5a-b461-72f0c74e76e4.png"></p><p>这种结构叫做隐式链表，我们只要拿到堆的起始地址，就可以根据每个chunk中存储的size信息，遍历所有chunk，以找到合适的chunk进行分配</p><p>缺点：遍历效率低，难以进行不同chunk之间的合并，久而久之，就会导致堆内存被分割为许多内存碎片。为什么不能合并？假设下面这样的场景，从上到下有3个内存块ABC，假设释放B内存：如果想和C合并，只需要让B内存的起始地址加上记录的chunk size即可得到C内存的起始地址，根据C的标志位即可判断是否可以合并；但是想和A合并较为困难，因为我们没有办法找到上一个内存块(高地址)，无法判断，最坏情况下必须遍历整个堆才可以将AB合并</p><p><img src="https://img.aixheee.top/6ca4b83b-bfe1-460f-89f2-7c73e2b02919.png"></p><h4 id="带边界标记的合并技术"><a href="#带边界标记的合并技术" class="headerlink" title="带边界标记的合并技术"></a>带边界标记的合并技术</h4><p>Kunth在每个chunk的最后添加一个和header的副本——footer，此时，只要拿到了一个chunk的起始地址，将该地址+4就可以获得到高地址的chunk的信息了</p><p>问题：只有free chunk才需要footer，allocated chunk并不需要，此时footer对于allocated chunk是无用的</p><p>优化：用第1位，表示前一个chunk是allocated还是free。如果为1，则代表前一个chunk为allocated，当前chunk地址的前4个字节为前一个allocated chunk的payload或padding，此时，allocated chunk就算不保留footer也不会导致相邻的free chunk访问自身出错；如果为0，则代表前一个chunk为free，则将二者合并</p><p>free chunk：</p><p><img src="https://img.aixheee.top/b3a1f6c5-0d38-4699-89bd-2ff7303834d6.png"></p><h4 id="支持多线程"><a href="#支持多线程" class="headerlink" title="支持多线程"></a>支持多线程</h4><p>随着技术的发展，堆内存管理器需要添加对堆多线程的支持，可是，此时chunk的标志位已经不够用了，我们还需要两个标志位——一个用于判断当前chunk是否属于主线程，另一个判断当前chunk是由mmap得来的还是由brk得到的。</p><p>思考先前chunk的缺点：是否有必要在一个chunk中，同时保存前一个chunk的已分配&#x2F;未分配标志位和当前chunk的已分配&#x2F;未分配标志位？没必要，<strong>只保存前一个chunk的已分配&#x2F;未分配标志位就够了，对于当前chunk是否已经分配，完全可以查下一个chunk的Header得到</strong>。此时满足要求。</p><p>第0位P(PREV_INUSE) 则代表前一个chunk是否已经被分配，如果为1则代表已分配，是一个allocated chunk，为0则代表未分配，是一个free chunk。</p><p>第1位M(IS_MAPPED)，表示当前chunk对应的是哪个区域的虚拟内存，1表示文件和匿名映射区，0表示heap区</p><p>第2位N(NON_MAIN_ARENA)，表示chunk是否是thread arena</p><p>此时的free chunk：</p><p><img src="https://img.aixheee.top/7077ffab-568d-4fb5-9ff4-d2ca421ee8cd.png"></p><p>allocated chunk：</p><p><img src="https://img.aixheee.top/468fb77b-e430-447c-8636-a789d05ca7a5.png"></p><p>同理，发现对于不同的标志位，只需要存储一份即可。所以，将原先的footer中的标志位部分统一为prev_size，并挪动到chunk头部</p><p><img src="https://img.aixheee.top/a36e521b-955b-402f-80a3-061b58a692f9.png"></p><h3 id="如何组织堆虚拟内存"><a href="#如何组织堆虚拟内存" class="headerlink" title="如何组织堆虚拟内存"></a>如何组织堆虚拟内存</h3><p>如果我们通过隐式链表的方式管理chunk，则总会涉及到chunk的遍历，效率极低，<strong>由此引入了半侵入式链表来提高堆内存的分配和释放效率。</strong></p><p><strong>bin中各个free chunk是如何连接在一起的？</strong> free chunk的frea area中会有两个指针，分别指向当前chunk所属链表中的前一个chunk和后一个chunk</p><p>链表中的节点即为free chunk，链表称之为<strong>bin</strong>；为了将这些bin管理起来，通过数组<strong>fast bins和bins</strong>来将所有bin的头结点存储起来，二者同属于malloc_state</p><pre><code>struct malloc_state{  ……  /* Fastbins */  mfastbinptr fastbinsY[NFASTBINS];  ……  /* Normal bins packed as described above */  mchunkptr bins[NBINS * 2 - 2];  // #define NBINS    128  ……};</code></pre><p><img src="https://img.aixheee.top/d7b8c7f9-aaee-4fd6-ba81-9a41a2199500.png"></p><p><img src="https://img.aixheee.top/bdf0150a-a8d7-4494-be35-aa07dda8bdf9.png"></p><h4 id="fast-bins"><a href="#fast-bins" class="headerlink" title="fast bins"></a>fast bins</h4><p>（规定下文中，chunk size代表malloc_chunk的实际大小，chunk unused size为实际可用大小，前者比后者大16字节，用于存储prev_size，size以及两个指针）</p><p>作用：存放小尺寸且快速分配的空闲块</p><p><strong>设计目的：快速地分配和释放小内存</strong></p><p>程序中会经常申请和释放较小的内存空间。如果一释放就被分配器合并为一个较大的chunk，那么一旦又有小块内存的请求，那么分配器又需要把大的空闲内存做切分，无疑是比较低效的。所以，引入了较小块内存fast bin。</p><p><strong>特点：</strong> 链表特点：每个fast bin都是单链表且总数为10，10个fast bin中所包含的fast chunk size是按照递增16字节排序的，比如第一个fast bin中所有的fast chunk size为16字节，第二个fast bin中所有的fast chunk size为32字节</p><blockquote><p>为什么是单链表+LIFO？通过单链表就可以实现删除和添加操作。删除(malloc分配)即进行头删，添加(free释放)操作即头插，不需要对中间的节点进行操作，为O(1)操作。之所以要LIFO，是因为根据temporal locality原理，刚释放的chunk很可能马上再用，LIFO可以提高效率</p></blockquote><p>节点特点：fast bin中的fast chunk大小较小，chunk unused不大于64字节，且所有的fast chunk不会自动合并，有<strong>延迟合并的特点</strong>。</p><p>合并只会发生在malloc请求large chunk、malloc请求smallbin但是smallbin为空、free 非 fastbin chunk或显示地调用malloc_consolidate()，此时会触发malloc_consolidate()，将所有的fastbin chunk取出并与相邻块合并，放入到unsorted bin中</p><p>此外，会将fast free chunk的prev标志位置为1，保证不会对fast free chunk进行合并操作，这是其设计目的决定的。</p><h4 id="unsorted-bin"><a href="#unsorted-bin" class="headerlink" title="unsorted bin"></a>unsorted bin</h4><p>作用：当一个chunk被释放时，它首先被放入unsorted bin，而不是直接放入其他bins。其是free后chunk的“缓冲区+热缓存层”，用于延迟分类并提高复用概率</p><p>设计目的：1.<strong>降低free成本</strong>。如果每次free内存，都立刻判断是smallbin还是largebin，larginbin还要维护有序结构，并加入到其中，效率较低。所以，会首先合并相邻chunk并先放进unsorted bin中</p><p>2.<strong>提高命中率</strong>。程序中有个重要现象——刚释放的内存，大概率会被马上申请，可以理解为访问的局部性原理。所以如果把这块内存又放到smallbin&#x2F;largebin中，相比于将这些热数据放在一个unsorted bin中，其查找时间会更多</p><p>特点：只有一个unsorted bin，其为双向链表且不排序，FIFO——头插(释放内存)，遍历时从尾部开始查，并优先尾删(malloc分配)</p><blockquote><p>为什么是双向链表 + FIFO？因为遍历时可能删除中间节点，而单链表删除中间节点的成本高，为O(n)，在知道要删除的节点的情况下，需要通过遍历获取前驱节点。FIFO的效果是，如果freeABC，则顺序为C–&gt;B–&gt;A，但是malloc检查是从尾部A开始检查的，目的是让最早被释放的内存优先被检查，防止某些块长期滞留</p></blockquote><h4 id="small-bin"><a href="#small-bin" class="headerlink" title="small bin"></a>small bin</h4><p>作用：用于管理比 fast bins 稍大一些的小尺寸空闲块，支持合并</p><p>链表本身特点：共62个small bin，为双向链表，FIFO——内存释放操作就将新的chunk头插，分配操作就尾删</p><p>节点特点：一个small bin中的所有free small chunk大小相等，第一个small bin的size为16字节，后续递增8</p><h4 id="large-bin"><a href="#large-bin" class="headerlink" title="large bin"></a>large bin</h4><p>作用：用于管理超过 small bin 范围的大尺寸空闲块</p><p>链表本身特点：其大体逻辑和small bin类似，区别在于单一large bin中每个chunk的大小不是相同的，从链表头到链表尾部呈现递增趋势，采用范围管理策略，而不是small bin的固定大小管理策略</p><p><strong>关于bins的初始化时机：</strong></p><p>注意，bins不是“预分配内存”的缓存池，而是已经分配过、又被free的chunk回收站。在第一次malloc前，bins完全是空的，直至第一块内存区域被free，如果大小较小则放入fastbins，否则合并并放入到unsorted bin中，再分类放入到smallbin&#x2F;largebin中</p><h2 id="malloc流程梳理"><a href="#malloc流程梳理" class="headerlink" title="malloc流程梳理"></a>malloc流程梳理</h2><p>1.void* p &#x3D; malloc(size)。内部首先内存对齐(8&#x2F;16字节)，并加上chunk header大小，得到normalized size</p><p>2.tcache中查找。tcache为线程本地缓存，按照size class分桶，LIFO，O(1)查找且无锁。但是其容量较小，且只会缓存小对象，在高并发下依然无法替代arena，可能导致频繁的锁竞争</p><pre><code>thread  └── tcache（每线程私有）        ├── size class 1 → 链表        ├── size class 2 → 链表        └── ...</code></pre><p>3.选择arena并获取到其对应的锁，开始从bins中查找何时的chunk，此后的查找都是在持锁的状态下完成的</p><p>4.fastbin中查找。如果nb属于fastbin范围内(80字节左右)，则做<mark>精准匹配</mark>，如果fastbin[index]非空，则返回，否则进入下一次</p><p>5.smallbin。如果nb属于smallbin范围内(小于512字节)，则将nb转换为smallbins索引，做<mark>精准匹配</mark>，并从对应的smallbin中取出一个chunk并返回</p><pre><code>index = size_to_smallbin(nb)if smallbin[index] 非空    取一个 chunk    标记 inuse    返回</code></pre><p>6.unsorted bin延迟分类层。做<mark>最接近匹配</mark>，遍历unsorted bin，在第一次满足nb&gt;&#x3D;chunk.size时，将该chunk从unsorted bin中移除，并将其做切割，将富裕部分根据其大小<strong>插入到smallbin&#x2F;largebin中</strong>，并返回结果。注意，<strong>对于不符合大于等于条件的chunk都会被转移到smallbin或largebin中</strong></p><p>如果遍历完unsorted bin依然没有找到合适块，即nb&lt;所有chunk.size，此时unsorted也会被清空，因为已经被分类</p><pre><code>for (chunk in unsorted):    if chunk.size &gt;= nb:        从 unsorted 摘除        if chunk.size - nb &gt;= MIN_CHUNK_SIZE:            切割            remainder 分类插入 smallbin/largebin        返回前半部分    else:        将该 chunk 分类到 smallbin 或 largebin</code></pre><p>7.largebin。做<mark>最接近匹配</mark>，目标是找到&gt;&#x3D;nb的最小块，会先定位到index，得到largebins[index]，再在其中找到目标快，如果有剩余则做切割，将剩余部分插入到smallbin或largebin中，并返回</p><p>8.top chunk。如果top.size()&gt;&#x3D;nb，则从top中做切割，将剩余部分插入到smallbin或largebin中，更新top指针，并返回</p><p>9.向系统申请内存以扩展堆。如果nb&lt;128KB，则调用brk()扩展heap，更新top chunk并再次尝试分配；如果nb&gt;&#x3D;128KB，则调用mmap()创建匿名映射，返回mmap chunk</p><p>10.返回地址。此时，用户得到的是chunk+header_size，但是还没有物理页</p><p>可能存在的问题：</p><p><strong>既然unsorted bin作为热缓存层，为什么是malloc查找顺序是fastbin-&gt;smallbin，而不是先查unsorted bin？</strong></p><p>因为smallbin是精确匹配层，如果请求在smallbin的范围内，直接通过O(1)时间复杂度即可确定是否存在可用的内存块：</p><pre><code>smallbin[index] 非空 → 直接返回</code></pre><p>相比之下，unsorted bin里chunk大小杂乱，查找unsorted bin的代价是O(n)，且查找到后可能需要进行分割。</p><p>malloc分配内存的设计原则是，先查找“确定性精确命中”的结构，再查找“需要遍历判断”的较低效结构</p><p><strong>是否所有的内存块在分配时，都需要切割？</strong></p><p>不是。如果想要让smallbin命中，必须要请求大小和small chunk大小完全相同，即必须做完全匹配而不是最恰当匹配，由此来避免低效的查找。这是空间利用率与速度的折中。</p><p>如果unsorted bin或者large bin命中，则需要进行切割</p><h2 id="free流程梳理"><a href="#free流程梳理" class="headerlink" title="free流程梳理"></a>free流程梳理</h2><p>1.定位chunk。free(ptr)将根据ptr定位到chunk的Header地址，获取PREV_INUSE、IS_MAPPED和NON_MAIN_ARENA信息</p><p>2.检查是否是mmap分配的chunk。通过查看IS_MAPPED标志位，可以判断是否是mmap分配的——如果是，则通过munmap(chunk,size)<strong>将虚拟地址区间移除、VMA移除、页表项删除并立刻释放物理页，归还给操作系统</strong>，不涉及任何合并以及bin；如果不是，则进行_int_free()函数来合并释放</p><p>3.检查是否属于fastbin大小范围。如果size &lt;&#x3D; 64B&#x2F;128B，则将其头插到fastbin的单链表中，<strong>不做合并</strong>(延迟合并，因为小块频繁分配释放，很可能马上又malloc，合并的成本太高了)，不清除PREV_INUSE</p><p>4.说明属于smallbin或是largebin的大小范围，<strong>进行合并</strong>。合并目标是，尽可能地让当前chunk和前一个chunk、后一个chunk合并。<strong>对于前一个chunk</strong>——通过Header中的PREV_INUSE标志位来判断前一个chunk是否为free，如果是free则通过chunk - prev_size来获取到前一个chunk的起始地址并合并；<strong>对于后一个chunk</strong>——通过chunk + size获取到下一个chunk的Header，检查其是否被使用，如果未被使用，则进行合并。合并的流程——从对应的bin中unlink(nextChunk)，并让当前chunk-&gt;size +&#x3D; nextChunk-&gt;size</p><p>5.合并完成，将合并后的块放入到unsorted bin中。<strong>此时，虚拟地址依然存在、VMA存在、物理页也依然存在，并没有归还给操作系统，allocator认为可重用</strong></p><p>6.特殊情况：合并到了top chunk。如果释放的块后面是top chunk，即chunk + size &#x3D;&#x3D; top，则直接扩展top，不进入unsorted bin。<strong>此时，如果top_size &gt; trim_threshold(一般是128KB)，就会调用systrim，进而调用brk(new_end)，让heap缩小，缩减mm_struct映射的堆区域与对应的vm_area_struct映射的虚拟地址范围，清除对应的页表项，并真正释放物理页，将物理内存归还给操作系统</strong></p><p><strong>重新理解free：</strong></p><p><strong>从进程的视角来看：</strong> free是“进程管理内存资源的行为”，等同于将这块内存的使用权还给了allocator，但是这块内存是否会交还给操作系统，取决于分配器策略，并不是free的必然结果</p><p><strong>从数据结构的视角来看：</strong> allocator维护bins以及chunk状态，free本质上是对这些数据结构的操作，会根据ptr找到chunk并标记为free、尝试进行合并操作并插入到bin中，本质上是对堆管理数据结构的操作</p><p><strong>从虚拟内存的视角来看：</strong> 对于小块内存(brk())，free不等同于删除虚拟页，VMA、页表依然存在且有效、PTE访问权限没有变化，此时并不是硬件层面禁止访问，只是属于未定义行为。比如，下面的代码通常不会立刻报错</p><pre><code>free(p);*p = 1;</code></pre><p>对于大块内存，才会删除vm_area_struct、将物理页归还给操作系统，此时访问是硬件层面的错误</p><p><strong>从内核视角来看：</strong> 绝大多数free并不会立刻进入内核，因为内存块已经交给allocator管理。只有在munmap、brk收缩和进程退出时才会进入内核态，调整映射，真正把物理内存收回</p><p><strong>从硬件的视角来看：</strong> CPU完全不知道，CPU只做虚拟地址到物理地址的映射，free不会修改CR3、不会刷新TLB、不会改变PTE也不会触发page fault，除非munmap</p><p><mark>究竟什么情况下才会真正将物理内存归还给操作系统？</mark></p><p>一是mmap映射的区域直接通过munmap()归还，二是brk()调整收缩堆大小，内核才会调整虚拟映射、归还物理内存，此时的物理内存才可被其他进程使用</p><p><strong>注意，释放内存必须从高地址，即heap的尾部开始释放。</strong> 例如下图中，只有最右边free区域可以被归还，中间的free块永远无法被归还</p><pre><code>| allocated | free | allocated | free(top) |</code></pre><p><strong>不能归还中间部分的虚拟地址，这是由VMA机制决定的。</strong> mm_struct通过heap_start和brk指针分别指向堆底和堆顶，vm_area_struct同样也会用start和end指针指向开始和结束地址。二者都说明了，这块虚拟内存区域是连续的。如果强行删除，会导致VMA被拆成两段，二者的语义被破坏，glibc的连续heap模型崩溃。所以，brk机制天然不支持中间部分裁剪</p><h2 id="内存碎片"><a href="#内存碎片" class="headerlink" title="内存碎片"></a>内存碎片</h2><p><mark>内存碎片是在什么情况下产生的？</mark></p><p><strong>第一层：allocator层碎片</strong></p><p>外部碎片(空间总量够 但不连续)——malloc产生大小不同的chunk，切割时会产生剩余碎片；fastbin延迟合并，以性能换空间</p><p>内部碎片——chunk对齐导致</p><p><strong>第二层：虚拟内存层</strong></p><p>外部碎片：brk heap中的不可裁剪空洞，由于heap只能是单一连续的VMA 只可以尾部shrink，中间free无法通过brk归还</p><p><strong>第三层：物理内存层</strong></p><p>外部碎片：缺页中断触发物理页的分配，但是一张张4KB的页被打散分配给了不同的进程使用，如果此时某个进程需要2MB的连续页，可能难以分配，需要swap out</p><p><mark>内存碎片如何处理？何时处理？由谁处理？</mark></p><p><strong>对于虚拟地址碎片：</strong></p><p>heap空洞：中间的free chunk的虚拟地址仍然要保留，在一定时间内不能归还。其<strong>由ptmalloc来管理，这部分碎片的处理方式，需要等待未来被malloc复用——要不就是best-fit减少碎片，要不就是与相邻区间合并</strong></p><p><strong>总结一下，对于虚拟地址碎片，ptmalloc都有哪些处理手段——free时对smallchunk&#x2F;largechunk立即合并、fastbin的延迟合并、unsortedbin的优先复用以避免频繁分割、trim的尾部处理；malloc时的best-fit策略，“尽可能保留大块内存，尽可能减小切割剩余大小”</strong></p><p><strong>对于物理内存碎片：</strong></p><p>虽然虚拟地址不能被回收，但是物理页是可以回收的。</p><p>1.正常情况下，free后虚拟地址未被回收，页表映射关系依然存在，但是如果长期不访问，则在内存紧张的情况下会被swap out</p><p>2.现代glibc在一些条件下会主动madvise。此时，虚拟地址仍然存在，页表也依然存在，不过物理页被回收</p><p>说完何时回收，下面说说如何合并，减少物理内存碎片：Linux下buddy system对物理页进行管理，在物理页已经被回收，完全脱离进程映射后，buddy system可以合并相邻的free页以减少物理碎片</p><p><mark>为什么allocator不频繁归还内存？</mark></p><p><strong>设计目标是——优先重用，减少无效、昂贵的系统调用。</strong> 一块刚被使用使用过的内存，很大可能会在不久后被再次使用。而如果free时直接把物理内存归还，就意味着需要再次调用brk()&#x2F;mmap()系统调用、建立页表映射，代价太大</p><p><mark>这也解释了为什么free后RSS(Resident Set Size 常驻内存集)可能不下降：</mark>RSS代表当前进程在物理内存(RAM)中实际占用了多少物理页，时操作系统视角下进程的真实消耗的物理内存量</p><p>一是释放内存块并不属于通过mmap映射分配到的大内存块，而是brk()分配的</p><p>二是释放的内存块并不在堆顶</p><p>三是，就算是堆顶的内存块，堆的大小小于trim_threshold(一般是128KB)，glibc就不会调用systrim()来执行brk系统调用收缩堆。</p><p>这三种情况下，在操作系统看来这些内存块依然被进程占用着</p><p><strong>本篇文章主要梳理了malloc的流程，下一篇则主要聚焦于“为什么”的角度，梳理malloc为什么要这样做</strong></p>]]></content>
      
      
      <categories>
          
          <category> 内存分配与管理 </category>
          
      </categories>
      
      
    </entry>
    
    
  
  
    
    
    <entry>
      <title>关于</title>
      <link href="/about/index.html"/>
      <url>/about/index.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
    
    <entry>
      <title>友链</title>
      <link href="/link/index.html"/>
      <url>/link/index.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
    
    <entry>
      <title>音乐</title>
      <link href="/music/index.html"/>
      <url>/music/index.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
    
    <entry>
      <title>categories</title>
      <link href="/categories/index.html"/>
      <url>/categories/index.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/css/custom.css"/>
      <url>/css/custom.css</url>
      
        <content type="html"><![CDATA[/* 页脚与头图透明 */#footer {  background: transparent !important;}#page-header {  background: transparent !important;}/* 白天模式遮罩透明 */#footer::before {  background: transparent !important;}#page-header::before {  background: transparent !important;}/* 夜间模式遮罩透明 */[data-theme="dark"] #footer::before {  background: transparent !important;}[data-theme="dark"] #page-header::before {  background: transparent !important;}]]></content>
      
    </entry>
    
    
    
    <entry>
      <title>第一个标签</title>
      <link href="/tags/tags.html"/>
      <url>/tags/tags.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
  
</search>
